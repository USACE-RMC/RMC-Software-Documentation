---
title: React Components Reference
sidebar_label: React Components
---

import TableVerticalNoRef from '@site/src/components/TableVerticalNoRef';

# React Components Reference

## Introduction

This comprehensive guide documents all React components available for use in your MDX documentation. Components are organized by category to help you quickly find the right tool for your needs.

### What Are React Components?

**React components** are reusable UI elements that you can insert into your MDX files. They handle complex layouts, automatic numbering, and consistent styling without requiring you to write HTML or CSS.

**Benefits:**

- ✅ Automatic numbering (figures, tables, equations, videos)
- ✅ Cross-referencing between elements
- ✅ Consistent formatting across all documentation
- ✅ Professional appearance
- ✅ Responsive design (works on all screen sizes)

### How to Use Components

**Basic workflow:**

1. **Import** the component at the top of your MDX file
2. **Use** the component with JSX syntax
3. **Configure** with props (properties)

**Example:**

```jsx
---
title: My Chapter
---

import Figure from '@site/src/components/Figure';

# My Chapter

<Figure
  figKey="fig-workflow"
  src="/figures/my-document/workflow.png"
  alt="Workflow diagram"
  caption="Overall analysis workflow"
  width="80%"
/>
```

---

## Component Categories

Components are organized into these categories:

- **[Document Metadata](#document-metadata)** - Document info and version history
- **[Figures & Images](#figures--images)** - Numbered figures, inline images, references
- **[Tables](#tables)** - Various table layouts and references
- **[Equations](#equations)** - LaTeX equations with automatic numbering
- **[Citations](#citations)** - Bibliography and citation management
- **[Content Organization](#content-organization)** - Tabs, lists, processes
- **[Multimedia](#multimedia)** - Videos with captions
- **[Navigation](#navigation)** - Buttons, links, containers
- **[Specialized](#specialized)** - Event trees and domain-specific components

---

## Quick Component Selector

**Need to display an image?**

- Numbered figure with caption → [`<Figure>`](#figure)
- Unnumbered inline image → [`<FigureInline>`](#figureinline)
- Simple image, no caption → [`<FigureNoRef>`](#figurenoref)

**Need to create a table?**

- Standard columnar table → [`<TableVertical>`](#tablevertical)
- Row-based table → [`<TableHorizontal>`](#tablehorizontal)
- Simple two-column list → [`<TableAcronyms>`](#tableacronyms)

**Need to show a process?**

- Step-by-step procedure → [`<ProcessList>`](#processlist)
- Expandable grouped items → [`<CollectionList>`](#collectionlist)
- Tabbed alternatives → [`<DocTabs>`](#doctabs)

**Need to add equations?**

- Numbered equation → [`<Equation>`](#equation)
- Unnumbered equation → [`<EquationNoRef>`](#equationnoref)
- Inline equation badge → [`<EquationChip>`](#equationchip)

---

## Document Metadata

### DocumentMetadata

**Purpose:** Displays report metadata in a professional table format at the beginning of documents.

**When to use:**

- `00-document-info.mdx` files
- Beginning of technical reports
- Any document requiring formal metadata

**Import:**

```jsx
import DocumentMetadata from '@site/src/components/DocumentMetadata';
```

**Usage:**

```jsx
---
reportDate: November 2023
reportType: User's Guide
reportTitle: LifeSim Life Safety Model
reportSubTitle: Version 1.0
reportAuthors: ["John Doe, Risk Management Center", "Jane Smith, RMC"]
reportAbstract: This guide provides instructions for using LifeSim software...
reportSubjectTerms: ["life safety", "dam failure", "risk analysis"]
responsiblePersonName: John Doe
responsiblePersonNumber: 555-0100
---

import DocumentMetadata from '@site/src/components/DocumentMetadata';

<DocumentMetadata />
```

**Props:** None - reads from front matter

**Front Matter Fields:**

<TableVerticalNoRef
  alt="Table: Field, Type, Description"
  headers={[[{ value: 'Field' }, { value: 'Type' }, { value: 'Description' }]]}
  columns={[
    ['<code>reportDate</code>', '<code>reportType</code>', '<code>reportTitle</code>', '<code>reportSubTitle</code>', '<code>reportAuthors</code>', '<code>reportAbstract</code>', '<code>reportSubjectTerms</code>', '<code>responsiblePersonName</code>', '<code>responsiblePersonNumber</code>'],
    ['String', 'String', 'String', 'String', 'Array', 'String', 'Array', 'String', 'String'],
    ['Publication date', 'Document type (User&#x27;s Guide, Technical Manual, etc.)', 'Full document title', 'Subtitle or version', 'List of author names', 'Document summary', 'Keywords/topics', 'Point of contact', 'Contact phone number'],
  ]}
/>

---

### TableVersionHistory

**Purpose:** Displays document version history in a formatted table.

**When to use:**

- `00-version-history.mdx` files
- Tracking document revisions

**Import:**

```jsx
import TableVersionHistory from '@site/src/components/TableVersionHistory';
```

**Usage:**

```jsx
<TableVersionHistory
  versionHistory={[
    {
      version: '1.0',
      date: 'November 2023',
      changes: 'Initial release',
      author: 'J. Doe',
      reviewer: 'J. Smith',
      approver: 'A. Manager',
    },
    {
      version: '1.1',
      date: 'March 2024',
      changes: 'Updated methodology section, added new examples',
      author: 'J. Doe',
      reviewer: 'J. Smith',
      approver: 'A. Manager',
    },
  ]}
/>
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>versionHistory</code>'],
    ['Array'],
    ['Yes'],
    ['Array of version objects'],
  ]}
/>

**Version Object Fields:**

<TableVerticalNoRef
  alt="Table: Field, Type, Description"
  headers={[[{ value: 'Field' }, { value: 'Type' }, { value: 'Description' }]]}
  columns={[
    ['<code>version</code>', '<code>date</code>', '<code>changes</code>', '<code>author</code>', '<code>reviewer</code>', '<code>approver</code>'],
    ['String', 'String', 'String', 'String', 'String', 'String'],
    ['Version number (e.g., &quot;1.0&quot;)', 'Release date', 'Description of changes', 'Document author', 'Technical reviewer', 'Approving authority'],
  ]}
/>

---

## Figures & Images

### Figure

**Purpose:** Numbered figure with caption and automatic cross-referencing.

⭐ **MOST COMMONLY USED COMPONENT**

**When to use:**

- Any diagram, chart, or image that needs a figure number
- Images that will be referenced from text
- Professional documentation figures

**Import:**

```jsx
import Figure from '@site/src/components/Figure';
```

**Basic Usage:**

```jsx
<Figure
  figKey="fig-workflow"
  src="/figures/desktop-applications/lifesim/users-guide/v1.0/workflow.png"
  alt="Flowchart showing three-step analysis process"
  caption="LifeSim analysis workflow"
/>
```

**Advanced Usage:**

```jsx
<Figure
  figKey="fig-results"
  src="/figures/my-software/results-graph.png"
  alt="Bar chart comparing model results across three scenarios"
  caption="Comparison of model results for baseline, alternative A, and alternative B scenarios"
  width="90%"
  credit="Source: USACE Risk Management Center (2024)"
/>
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Default, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Default' }, { value: 'Description' }]]}
  columns={[
    ['<code>figKey</code>', '<code>src</code>', '<code>alt</code>', '<code>caption</code>', '<code>width</code>', '<code>credit</code>'],
    ['String', 'String', 'String', 'String', 'String', 'String'],
    ['Yes', 'Yes', 'Yes', 'Yes', 'No', 'No'],
    ['-', '-', '-', '-', '<code>&quot;80%&quot;</code>', '-'],
    ['Unique identifier for numbering and cross-refs', 'Image path (relative to static folder)', 'Accessibility description', 'Figure caption text', 'Display width', 'Image source/credit line'],
  ]}
/>

**Tips:**

- Use descriptive `figKey` values: `fig-workflow`, `fig-results-1`
- Always start paths with `/figures/`
- Write detailed `alt` text for accessibility
- Keep captions concise but descriptive

**Common Mistakes:**

- ❌ Forgetting leading slash: `figures/image.png`
- ✅ Correct: `/figures/image.png`
- ❌ Duplicate `figKey` values
- ✅ Each figure needs unique key

---

### FigureReference

**Purpose:** Creates cross-references to numbered figures.

**When to use:**

- Referencing figures from body text
- Creating figure call-outs

**Import:**

```jsx
import FigureReference from '@site/src/components/FigureReference';
```

**Usage:**

```jsx
The analysis workflow is shown in <FigureReference figKey="fig-workflow" />.

See <FigureReference figKey="fig-results" /> for detailed results.
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>figKey</code>'],
    ['String'],
    ['Yes'],
    ['Must match a Figure&#x27;s figKey'],
  ]}
/>

**Output:** Renders as clickable link: "Figure 1", "Figure 2", etc.

---

### FigureInline

**Purpose:** Unnumbered inline image with caption (no figure number).

**When to use:**

- Supporting images that don't need numbering
- Decorative or supplementary images
- Tutorial screenshots

**Import:**

```jsx
import FigureInline from '@site/src/components/FigureInline';
```

**Usage:**

```jsx
<FigureInline src="/figures/tutorial/screenshot-1.png" alt="Screenshot of software main window" caption="The main interface window" width="70%" />
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Default, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Default' }, { value: 'Description' }]]}
  columns={[
    ['<code>src</code>', '<code>alt</code>', '<code>caption</code>', '<code>width</code>'],
    ['String', 'String', 'String', 'String'],
    ['Yes', 'Yes', 'No', 'No'],
    ['-', '-', '-', '<code>&quot;80%&quot;</code>'],
    ['Image path', 'Accessibility description', 'Optional caption text', 'Display width'],
  ]}
/>

---

### FigureNoRef

**Purpose:** Simple image display without caption or numbering.

**When to use:**

- Quick image insertion
- Screenshots in tutorials
- Images that don't need captions

**Import:**

```jsx
import FigureNoRef from '@site/src/components/FigureNoRef';
```

**Usage:**

```jsx
<FigureNoRef src="/figures/icons/warning-icon.png" alt="Warning icon" width="100px" />
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Default, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Default' }, { value: 'Description' }]]}
  columns={[
    ['<code>src</code>', '<code>alt</code>', '<code>width</code>', '<code>background</code>'],
    ['String', 'String', 'String', 'String'],
    ['Yes', 'Yes', 'No', 'No'],
    ['-', '-', '<code>&quot;80%&quot;</code>', '<code>&quot;filled&quot;</code>'],
    ['Image path', 'Accessibility description', 'Display width', '<code>&quot;filled&quot;</code> or <code>&quot;transparent&quot;</code>'],
  ]}
/>

---

## Tables

### TableVertical

**Purpose:** Standard columnar table with automatic numbering.

⭐ **MOST COMMONLY USED TABLE COMPONENT**

**When to use:**

- Standard data tables
- Comparison tables
- Results tables
- Most tabular data

**Import:**

```jsx
import TableVertical from '@site/src/components/TableVertical';
```

**Basic Usage:**

```jsx
<TableVertical
  tableKey="table-parameters"
  alt="Model input parameters"
  headers={[[{ value: 'Parameter' }, { value: 'Value' }, { value: 'Units' }]]}
  columns={[
    [{ value: 'Population' }, { value: 'Flow Rate' }, { value: 'Duration' }],
    [{ value: '10,000' }, { value: '5,000' }, { value: '24' }],
    [{ value: 'people' }, { value: 'cfs' }, { value: 'hours' }],
  ]}
  caption="Model input parameters"
/>
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Default, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Default' }, { value: 'Description' }]]}
  columns={[
    ['<code>tableKey</code>', '<code>alt</code>', '<code>headers</code>', '<code>columns</code>', '<code>caption</code>', '<code>widthMode</code>', '<code>leftAlignHeaders</code>'],
    ['String', 'String', 'Array', 'Array', 'String', 'String', 'Boolean'],
    ['Yes', 'Yes', 'Yes', 'Yes', 'Yes', 'No', 'No'],
    ['-', '-', '-', '-', '-', '<code>&quot;full&quot;</code>', '<code>false</code>'],
    ['Unique identifier', 'Accessibility description', 'Array of header row arrays', 'Array of column arrays', 'Table caption', '<code>&quot;full&quot;</code>, <code>&quot;intrinsic&quot;</code>, or <code>&quot;responsive&quot;</code>', 'Left-align header text'],
  ]}
/>

**Advanced Features:**

**Cell Spanning:**

```jsx
headers={[[
  { value: 'Name' },
  { value: 'Statistics', colSpan: 2 } // Spans 2 columns
]]}
```

**Row Spanning:**

```jsx
columns={[
  [
    { value: 'Category A', rowSpan: 2 }, // Spans 2 rows
    { value: 'Item 1' },
    { value: 'Item 2' }
  ]
]}
```

---

### TableReference

**Purpose:** Cross-references to numbered tables.

**When to use:**

- Referencing tables from text

**Import:**

```jsx
import TableReference from '@site/src/components/TableReference';
```

**Usage:**

```jsx
Input parameters are listed in <TableReference tableKey="table-parameters" />.
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>tableKey</code>'],
    ['String'],
    ['Yes'],
    ['Must match a table&#x27;s tableKey'],
  ]}
/>

---

### TableHorizontal

**Purpose:** Row-based table where each row represents a record.

**When to use:**

- Entity descriptions
- Feature comparisons
- Record-based data

**Import:**

```jsx
import TableHorizontal from '@site/src/components/TableHorizontal';
```

**Usage:**

```jsx
<TableHorizontal
  tableKey="table-software-comparison"
  alt="Software feature comparison"
  rows={[
    {
      header: 'Software A',
      data: ['Windows', 'Yes', '< 1 hour'],
    },
    {
      header: 'Software B',
      data: ['Windows, Mac', 'No', '2-3 hours'],
    },
  ]}
  columnHeaders={['Platform', 'Open Source', 'Runtime']}
  caption="Comparison of software features"
/>
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>tableKey</code>', '<code>alt</code>', '<code>rows</code>', '<code>columnHeaders</code>', '<code>caption</code>'],
    ['String', 'String', 'Array', 'Array', 'String'],
    ['Yes', 'Yes', 'Yes', 'Yes', 'Yes'],
    ['Unique identifier', 'Accessibility description', 'Array of row objects', 'Column header labels', 'Table caption'],
  ]}
/>

---

### TableAcronyms

**Purpose:** Two-column table specifically for acronyms and abbreviations.

**When to use:**

- Appendix for acronyms
- Abbreviation lists

**Import:**

```jsx
import TableAcronyms from '@site/src/components/TableAcronyms';
```

**Usage:**

```jsx
<TableAcronyms
  acronyms={[
    { acronym: 'USACE', definition: 'U.S. Army Corps of Engineers' },
    { acronym: 'RMC', definition: 'Risk Management Center' },
    { acronym: 'DST', definition: 'Dam Safety Toolbox' },
  ]}
/>
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>acronyms</code>'],
    ['Array'],
    ['Yes'],
    ['Array of <code>{acronym, definition}</code> objects'],
  ]}
/>

---

### TableVerticalLeftAlign

**Purpose:** Left-aligned variant of TableVertical.

**When to use:**

- Tables with long text entries
- Text-heavy tables

**Usage:** Same as TableVertical with left-aligned content.

---

## Equations

### Equation

**Purpose:** Numbered LaTeX equation with automatic numbering.

⭐ **MOST COMMONLY USED EQUATION COMPONENT**

**When to use:**

- Mathematical formulas that need numbering
- Equations referenced from text

**Import:**

```jsx
import Equation from '@site/src/components/Equation';
```

**Basic Usage:**

```jsx
<Equation equationKey="eq-risk" equation="R = P \times C" />
```

**Advanced Usage:**

```jsx
<Equation equationKey="eq-quadratic" equation="\frac{-b \pm \sqrt{b^2 - 4ac}}{2a}" />
```

**Complex Example:**

```jsx
<Equation equationKey="eq-total-risk" equation="R_{total} = \sum_{i=1}^{n} P_i \times C_i" />
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>equationKey</code>', '<code>equation</code>'],
    ['String', 'String'],
    ['Yes', 'Yes'],
    ['Unique identifier', 'LaTeX equation string'],
  ]}
/>

**LaTeX Quick Reference:**

<TableVerticalNoRef
  alt="Table: Symbol, LaTeX, Symbol, LaTeX"
  headers={[[{ value: 'Symbol' }, { value: 'LaTeX' }, { value: 'Symbol' }, { value: 'LaTeX' }]]}
  columns={[
    ['Fraction', 'Summation', 'Greek α', 'Subscript', '≤', '×'],
    ['<code>\\frac{a}{b}</code>', '<code>\\sum_{i=1}^{n}</code>', '<code>\\alpha</code>', '<code>x_i</code>', '<code>\\leq</code>', '<code>\\times</code>'],
    ['Square root', 'Integral', 'Greek β', 'Superscript', '≥', '÷'],
    ['<code>\\sqrt{x}</code>', '<code>\\int_a^b</code>', '<code>\\beta</code>', '<code>x^2</code>', '<code>\\geq</code>', '<code>\\div</code>'],
  ]}
/>

---

### EquationReference

**Purpose:** Cross-references to numbered equations.

**When to use:**

- Referencing equations from text

**Import:**

```jsx
import EquationReference from '@site/src/components/EquationReference';
```

**Usage:**

```jsx
Using <EquationReference equationKey="eq-risk" />, we calculate total risk.
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>equationKey</code>'],
    ['String'],
    ['Yes'],
    ['Must match an Equation&#x27;s equationKey'],
  ]}
/>

---

### EquationNoRef

**Purpose:** Unnumbered LaTeX equation (display only).

**When to use:**

- Supporting equations without numbering
- Equation examples

**Import:**

```jsx
import EquationNoRef from '@site/src/components/EquationNoRef';
```

**Usage:**

```jsx
<EquationNoRef equation="E = mc^2" />
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>equation</code>'],
    ['String'],
    ['Yes'],
    ['LaTeX equation string'],
  ]}
/>

---

### EquationChip

**Purpose:** Inline badge/chip for short equations or variable names.

**When to use:**

- Inline variable references
- Short formulas in text
- Mathematical notation badges

**Import:**

```jsx
import EquationChip from '@site/src/components/EquationChip';
```

**Usage:**

```jsx
The variable <EquationChip>$P$</EquationChip> represents probability, while <EquationChip>$C$</EquationChip> represents consequence.
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>children</code>'],
    ['ReactNode'],
    ['Yes'],
    ['Content to display (typically LaTeX in $...$)'],
  ]}
/>

---

## Citations

### Citation

**Purpose:** Inline citation reference.

**When to use:**

- Citing sources in text
- Referencing bibliography entries

**Import:**

```jsx
import Citation from '@site/src/components/Citation';
```

**Usage:**

```jsx
The methodology was developed by Smith <Citation citationKey="Smith2020" />.

Multiple authors have studied this topic <Citation citationKey="Jones2018" /> <Citation citationKey="Brown2019" />.
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>citationKey</code>'],
    ['String'],
    ['Yes'],
    ['Must match key in bib.json'],
  ]}
/>

**Output:** Renders as: [1], [2], etc. (clickable)

---

### CitationFootnote

**Purpose:** Displays list of citations used on current page.

**When to use:**

- End of each page with citations
- Before full bibliography

**Import:**

```jsx
import CitationFootnote from '@site/src/components/CitationFootnote';
```

**Usage:**

```jsx
## References

<CitationFootnote />
```

**Props:** None

---

### Bibliography

**Purpose:** Full bibliography listing all references.

**When to use:**

- References appendix
- End of document

**Import:**

```jsx
import Bibliography from '@site/src/components/Bibliography';
```

**Usage:**

```jsx
## Appendix: Bibliography

<Bibliography />
```

**Props:** None - automatically loads from bib.json

---

## Content Organization

### ProcessList

**Purpose:** Step-by-step numbered procedures with optional sub-steps.

**When to use:**

- Installation instructions
- Workflows
- Procedures
- Multi-step tutorials

**Import:**

```jsx
import ProcessList from '@site/src/components/ProcessList';
```

**Basic Usage:**

```jsx
<ProcessList items={[{ title: 'Install Python 3.8 or later' }, { title: 'Download the software package' }, { title: 'Run the installer' }]} />
```

**With Sub-steps:**

```jsx
<ProcessList
  items={[
    {
      title: 'Prepare your environment',
      child: [{ title: 'Install Node.js' }, { title: 'Install VS Code' }, { title: 'Clone repository' }],
    },
    {
      title: 'Configure settings',
      child: [{ title: 'Edit config file' }, { title: 'Set environment variables' }],
    },
    { title: 'Run the application' },
  ]}
/>
```

**With Details Panels:**

```jsx
<ProcessList
  items={[
    {
      title: 'Install dependencies',
      details: (
        <>
          <p>Run the following command:</p>
          <code>npm install</code>
        </>
      ),
    },
  ]}
/>
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Default, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Default' }, { value: 'Description' }]]}
  columns={[
    ['<code>items</code>', '<code>startAt</code>', '<code>indentPx</code>', '<code>bubbleSizePx</code>', '<code>nowrap</code>'],
    ['Array', 'Number', 'Number', 'Number', 'Boolean'],
    ['Yes', 'No', 'No', 'No', 'No'],
    ['-', '<code>1</code>', '<code>0</code>', '<code>28</code>', '<code>false</code>'],
    ['Array of step objects', 'Starting number', 'Indent per level (px)', 'Size of numbered bubbles', 'Prevent text wrapping'],
  ]}
/>

**Item Object:**

<TableVerticalNoRef
  alt="Table: Field, Type, Description"
  headers={[[{ value: 'Field' }, { value: 'Type' }, { value: 'Description' }]]}
  columns={[
    ['<code>title</code>', '<code>child</code>', '<code>details</code>', '<code>freeform</code>'],
    ['String/ReactNode', 'Array', 'ReactNode', 'ReactNode'],
    ['Step text', 'Sub-steps (a, b, c...)', 'Expandable details panel', 'Un-numbered content block'],
  ]}
/>

---

### DocTabs

**Purpose:** Tabbed content panels.

**When to use:**

- Multi-platform instructions
- Alternative methods
- Version comparisons
- OS-specific guidance

**Import:**

```jsx
import DocTabs from '@site/src/components/DocTabs';
```

**Usage:**

```jsx
<DocTabs
  items={[
    {
      title: 'Windows',
      label: 'Windows',
      child: (
        <>
          <p>Installation instructions for Windows:</p>
          <ol>
            <li>Download the .exe installer</li>
            <li>Run the installer</li>
            <li>Follow the prompts</li>
          </ol>
        </>
      ),
    },
    {
      title: 'macOS',
      label: 'macOS',
      child: (
        <>
          <p>Installation instructions for macOS:</p>
          <ol>
            <li>Download the .dmg file</li>
            <li>Open the disk image</li>
            <li>Drag to Applications</li>
          </ol>
        </>
      ),
    },
    {
      title: 'Linux',
      label: 'Linux',
      child: (
        <>
          <p>Installation instructions for Linux:</p>
          <code>sudo apt install software-name</code>
        </>
      ),
    },
  ]}
  defaultValue={0}
/>
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Default, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Default' }, { value: 'Description' }]]}
  columns={[
    ['<code>items</code>', '<code>defaultValue</code>', '<code>equalWidth</code>', '<code>showDividers</code>'],
    ['Array', 'Number/String', 'Boolean', 'Boolean'],
    ['Yes', 'No', 'No', 'No'],
    ['-', '<code>0</code>', '<code>true</code>', '<code>true</code>'],
    ['Array of tab objects', 'Default tab (index or value)', 'Equal-width tabs', 'Show dividers between tabs'],
  ]}
/>

**Tab Object:**

<TableVerticalNoRef
  alt="Table: Field, Type, Description"
  headers={[[{ value: 'Field' }, { value: 'Type' }, { value: 'Description' }]]}
  columns={[
    ['<code>title</code>', '<code>label</code>', '<code>value</code>', '<code>child</code>'],
    ['String', 'String', 'String', 'ReactNode'],
    ['Tab title (internal)', 'Tab display label', 'Tab value (optional)', 'Tab content'],
  ]}
/>

---

### CollectionList

**Purpose:** Grouped expandable items with optional numbering.

**When to use:**

- FAQs
- Feature lists
- Expandable groups
- Card-based navigation

**Import:**

```jsx
import CollectionList from '@site/src/components/CollectionList';
```

**Basic Usage:**

```jsx
<CollectionList
  title="Frequently Asked Questions"
  items={[
    {
      title: 'What is LifeSim?',
      child: 'LifeSim is a life safety model for dam failure consequence estimation.',
    },
    {
      title: 'What platforms are supported?',
      child: 'LifeSim runs on Windows 10 and later.',
    },
  ]}
  defaultOpen="first"
/>
```

**With Numbered Items:**

```jsx
<CollectionList
  title="Software Features"
  items={[
    {
      title: 'Hydraulic Routing',
      child: 'Advanced flood wave propagation modeling',
    },
    {
      title: 'Population Analysis',
      child: 'Detailed population-at-risk calculations',
    },
    {
      title: 'Evacuation Modeling',
      child: 'Time-dependent evacuation simulations',
    },
  ]}
  autoNumber={true}
  numberStyle="decimal"
  showExpandAll={true}
/>
```

**Grid Layout:**

```jsx
<CollectionList
  title="Software Modules"
  items={[...]}
  layout="grid"
  columns={{ base: 1, sm: 2, md: 3, xl: 4 }}
/>
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Default, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Default' }, { value: 'Description' }]]}
  columns={[
    ['<code>title</code>', '<code>items</code>', '<code>startAt</code>', '<code>autoNumber</code>', '<code>numberStyle</code>', '<code>defaultOpen</code>', '<code>showExpandAll</code>', '<code>layout</code>', '<code>columns</code>'],
    ['String', 'Array', 'Number', 'Boolean', 'String', 'String/Array', 'Boolean', 'String', 'Object'],
    ['No', 'Yes', 'No', 'No', 'No', 'No', 'No', 'No', 'No'],
    ['-', '-', '<code>1</code>', '<code>false</code>', '<code>&quot;decimal&quot;</code>', '<code>&quot;none&quot;</code>', '<code>false</code>', '<code>&quot;stack&quot;</code>', '-'],
    ['List title', 'Array of item objects', 'Starting number', 'Enable numbering', '<code>&quot;decimal&quot;</code>, <code>&quot;upper-alpha&quot;</code>, <code>&quot;lower-alpha&quot;</code>, <code>&quot;upper-roman&quot;</code>, <code>&quot;lower-roman&quot;</code>', '<code>&quot;none&quot;</code>, <code>&quot;first&quot;</code>, <code>&quot;all&quot;</code>, or array of indices', 'Show expand/collapse all buttons', '<code>&quot;stack&quot;</code> or <code>&quot;grid&quot;</code>', 'Grid columns: <code>{ base, sm, md, xl }</code>'],
  ]}
/>

---

### CategoryList

**Purpose:** Navigation tiles or menu items.

**When to use:**

- Landing pages
- Category navigation
- Feature selection

**Import:**

```jsx
import CategoryList from '@site/src/components/CategoryList';
```

**Usage:**

```jsx
<CategoryList
  items={[
    {
      title: 'Desktop Applications',
      description: 'Software tools for desktop analysis',
      href: '/docs/desktop-applications',
    },
    {
      title: 'Web Applications',
      description: 'Online analysis tools',
      href: '/docs/web-applications',
    },
    {
      title: 'Technical Manuals',
      description: 'Documentation and guides',
      href: '/docs/technical-manuals',
    },
  ]}
  variant="accentTiles"
  columns={{ base: 1, md: 2, xl: 3 }}
/>
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Default, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Default' }, { value: 'Description' }]]}
  columns={[
    ['<code>items</code>', '<code>variant</code>', '<code>columns</code>'],
    ['Array', 'String', 'Object'],
    ['Yes', 'No', 'No'],
    ['-', '<code>&quot;accentTiles&quot;</code>', '-'],
    ['Array of item objects', '<code>&quot;accentTiles&quot;</code>, <code>&quot;pills&quot;</code>, or <code>&quot;menu&quot;</code>', 'Grid columns'],
  ]}
/>

**Item Object:**

<TableVerticalNoRef
  alt="Table: Field, Type, Description"
  headers={[[{ value: 'Field' }, { value: 'Type' }, { value: 'Description' }]]}
  columns={[
    ['<code>title</code>', '<code>description</code>', '<code>href</code>', '<code>onClick</code>', '<code>icon</code>', '<code>badge</code>'],
    ['String', 'String', 'String', 'Function', 'String', 'String'],
    ['Item title', 'Description text (optional)', 'Link URL (optional)', 'Click handler (optional)', 'Icon path (optional)', 'Badge text (optional)'],
  ]}
/>

---

## Multimedia

### Video

**Purpose:** Numbered video component with automatic numbering.

**When to use:**

- Tutorial videos
- Demonstration videos
- Multimedia documentation

**Import:**

```jsx
import Video from '@site/src/components/Video';
```

**Basic Usage:**

```jsx
<Video
  videoKey="video-tutorial"
  caption="Software installation tutorial"
  src="/videos/installation-tutorial.mp4"
  poster="/videos/installation-poster.jpg"
  width="90%"
/>
```

**Multiple Sources:**

```jsx
<Video
  videoKey="video-demo"
  caption="Feature demonstration"
  sources={[
    { src: '/videos/demo.mp4', type: 'video/mp4' },
    { src: '/videos/demo.webm', type: 'video/webm' },
  ]}
  poster="/videos/demo-poster.jpg"
  controls={true}
/>
```

**With Captions:**

```jsx
<Video
  videoKey="video-presentation"
  caption="Conference presentation"
  src="/videos/presentation.mp4"
  trackSrc="/videos/presentation-captions.vtt"
  trackLang="en"
  trackLabel="English"
/>
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Default, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Default' }, { value: 'Description' }]]}
  columns={[
    ['<code>videoKey</code>', '<code>caption</code>', '<code>poster</code>', '<code>src</code>', '<code>sources</code>', '<code>width</code>', '<code>controls</code>', '<code>autoPlay</code>', '<code>loop</code>', '<code>muted</code>', '<code>playsInline</code>', '<code>preload</code>', '<code>trackSrc</code>', '<code>trackLang</code>', '<code>trackLabel</code>', '<code>credit</code>'],
    ['String', 'String', 'String', 'String', 'Array', 'String', 'Boolean', 'Boolean', 'Boolean', 'Boolean', 'Boolean', 'String', 'String', 'String', 'String', 'String'],
    ['Yes', 'Yes', 'No', 'No', 'No', 'No', 'No', 'No', 'No', 'No', 'No', 'No', 'No', 'No', 'No', 'No'],
    ['-', '-', '-', '-', '<code>[]</code>', '<code>&quot;80%&quot;</code>', '<code>true</code>', '<code>false</code>', '<code>false</code>', 'Auto', '<code>true</code>', '<code>&quot;metadata&quot;</code>', '-', '<code>&quot;en&quot;</code>', '<code>&quot;English&quot;</code>', '-'],
    ['Unique identifier', 'Video caption', 'Poster image path', 'Single video source', 'Multiple sources <code>[{src, type}]</code>', 'Display width', 'Show video controls', 'Auto-play on load', 'Loop playback', 'Mute audio (auto=true if autoPlay)', 'iOS inline playback', 'Preload strategy', 'VTT captions file', 'Caption language', 'Caption label', 'Video source/credit'],
  ]}
/>

---

### VideoReference

**Purpose:** Cross-references to numbered videos.

**When to use:**

- Referencing videos from text

**Import:**

```jsx
import VideoReference from '@site/src/components/VideoReference';
```

**Usage:**

```jsx
See <VideoReference videoKey="video-tutorial" /> for installation instructions.
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>videoKey</code>'],
    ['String'],
    ['Yes'],
    ['Must match a Video&#x27;s videoKey'],
  ]}
/>

---

## Navigation

### Button

**Purpose:** Styled button or link.

**When to use:**

- Call-to-action links
- Navigation buttons
- Download buttons

**Import:**

```jsx
import Button from '@site/src/components/Button';
```

**Link Button:**

```jsx
<Button href="/docs/getting-started">Get Started</Button>
```

**External Link:**

```jsx
<Button href="https://example.com">Visit Website</Button>
```

**Click Handler:**

```jsx
<Button onClick={() => alert('Clicked!')}>Click Me</Button>
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>children</code>', '<code>href</code>', '<code>to</code>', '<code>onClick</code>', '<code>className</code>'],
    ['ReactNode', 'String', 'String', 'Function', 'String'],
    ['Yes', 'No', 'No', 'No', 'No'],
    ['Button text/content', 'Link URL (renders as <code>&lt;a&gt;</code>)', 'Internal link (alias for href)', 'Click handler (renders as <code>&lt;button&gt;</code>)', 'Additional CSS classes'],
  ]}
/>

---

### NavLink

**Purpose:** Styled navigation link with arrow.

**When to use:**

- Section navigation
- Related page links

**Import:**

```jsx
import NavLink from '@site/src/components/NavLink';
```

**Usage:**

```jsx
<NavLink href="/docs/next-chapter">Continue to Next Chapter →</NavLink>
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>href</code>', '<code>children</code>'],
    ['String', 'ReactNode'],
    ['Yes', 'Yes'],
    ['Link URL', 'Link text'],
  ]}
/>

---

### NavContainer

**Purpose:** Navigation layout with version selector.

**When to use:**

- Page navigation sections
- Version-aware navigation

**Import:**

```jsx
import NavContainer from '@site/src/components/NavContainer';
```

**Usage:**

```jsx
<NavContainer navLink="../" navTitle="User's Guide" navDoc="lifesim-users-guide">
  <p>Navigation content here</p>
</NavContainer>
```

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>navLink</code>', '<code>navTitle</code>', '<code>navDoc</code>', '<code>children</code>'],
    ['String', 'String', 'String', 'ReactNode'],
    ['Yes', 'Yes', 'Yes', 'Yes'],
    ['Parent link URL', 'Navigation title', 'Document identifier', 'Navigation content'],
  ]}
/>

---

### VersionSelector

**Purpose:** Dropdown for switching document versions.

**When to use:**

- Included automatically in NavContainer
- Rarely used standalone

**Import:**

```jsx
import VersionSelector from '@site/src/components/VersionSelector';
```

**Usage:** Typically used internally by NavContainer.

---

## Specialized

### EventTree

**Purpose:** Interactive event tree diagrams with download capabilities.

**When to use:**

- Risk analysis event trees
- Fault tree diagrams
- Decision trees

**Import:**

```jsx
import EventTree from '@site/src/components/EventTree';
```

**Usage:**

```jsx
<EventTree
  base="generic-internal-erosion"
  text="Generic Internal Erosion Event Tree"
  imgDir="/event-trees/v1.0/"
  sheetDir="/event-trees/v1.0/"
  width="100%"
/>
```

**Features:**

- Copy to clipboard
- Download as PNG
- Download spreadsheet

**Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Default, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Default' }, { value: 'Description' }]]}
  columns={[
    ['<code>base</code>', '<code>text</code>', '<code>imgExt</code>', '<code>sheetExt</code>', '<code>imgDir</code>', '<code>sheetDir</code>', '<code>width</code>', '<code>pngScale</code>'],
    ['String', 'String', 'String', 'String', 'String', 'String', 'String', 'Number'],
    ['Yes', 'No', 'No', 'No', 'No', 'No', 'No', 'No'],
    ['-', 'Auto', '<code>&quot;png&quot;</code>', '<code>&quot;xlsm&quot;</code>', '-', '-', '<code>&quot;100%&quot;</code>', '<code>4</code>'],
    ['Base filename (without extension)', 'Display text', 'Image file extension', 'Spreadsheet extension', 'Image directory path', 'Spreadsheet directory path', 'Display width', 'PNG export scale factor'],
  ]}
/>

---

### ContentBubble & ContentBox

**Purpose:** Dashboard navigation cards.

**When to use:**

- Landing pages
- Software selection dashboards

**Import:**

```jsx
import ContentBubble from '@site/src/components/ContentBubble';
import ContentBox from '@site/src/components/ContentBox';
```

**ContentBubble Usage:**

```jsx
<ContentBubble
  iconLight="/img/software-icon-light.svg"
  iconDark="/img/software-icon-dark.svg"
  doc_location="/docs/software/users-guide"
  doc_name="Software Name"
  active={true}
/>
```

**ContentBox Usage:**

```jsx
<ContentBox
  contentData={[
    {
      iconLight: '/img/icon1-light.svg',
      iconDark: '/img/icon1-dark.svg',
      doc_location: '/docs/app1',
      doc_name: 'Application 1',
      active: true,
    },
    {
      iconLight: '/img/icon2-light.svg',
      iconDark: '/img/icon2-dark.svg',
      doc_location: '/docs/app2',
      doc_name: 'Application 2',
      active: false,
    },
  ]}
/>
```

**ContentBubble Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>iconLight</code>', '<code>iconDark</code>', '<code>doc_location</code>', '<code>doc_name</code>', '<code>active</code>'],
    ['String', 'String', 'String', 'String', 'Boolean'],
    ['Yes', 'Yes', 'Yes', 'Yes', 'No'],
    ['Light theme icon path', 'Dark theme icon path', 'Link destination', 'Display name', 'If false, shows &quot;Coming soon&quot;'],
  ]}
/>

**ContentBox Props:**

<TableVerticalNoRef
  alt="Table: Prop, Type, Required, Description"
  headers={[[{ value: 'Prop' }, { value: 'Type' }, { value: 'Required' }, { value: 'Description' }]]}
  columns={[
    ['<code>contentData</code>'],
    ['Array'],
    ['Yes'],
    ['Array of ContentBubble data objects'],
  ]}
/>

---

## Component Best Practices

### General Guidelines

**Always provide required props:**

```jsx
✅ Good:
<Figure
  figKey="fig-1"
  src="/figures/image.png"
  alt="Detailed description"
  caption="Informative caption"
/>

❌ Bad:
<Figure src="/figures/image.png" />  // Missing required props
```

**Use unique keys:**

```jsx
✅ Good:
<Figure figKey="fig-workflow" ... />
<Figure figKey="fig-results" ... />

❌ Bad:
<Figure figKey="fig-1" ... />
<Figure figKey="fig-1" ... />  // Duplicate key!
```

**Write descriptive alt text:**

```jsx
✅ Good:
alt="Bar chart comparing baseline, alternative A, and alternative B scenarios"

❌ Bad:
alt="Chart"
alt="Figure 1"
```

### Accessibility

**All images need alt text:**

- Describe what's shown in the image
- Don't say "image of" or "picture of"
- Be specific and concise

**Use semantic structure:**

- Headings in logical order
- Proper table markup
- Meaningful link text

### Performance

**Optimize images:**

- Keep file sizes reasonable (< 500KB each)
- Use appropriate formats (PNG for diagrams, JPG for photos)
- Compress images before adding

**Lazy loading:**

- Images and videos load automatically as needed
- No action required from contributors

### Common Mistakes

**Forgetting imports:**

```jsx
❌ Error: Component not defined
// Forgot to import Figure

✅ Fix:
import Figure from '@site/src/components/Figure';
```

**Wrong path format:**

```jsx
❌ Bad:
src="figures/image.png"  // Missing leading slash
src="../figures/image.png"  // Relative path

✅ Good:
src="/figures/desktop-applications/software/v1.0/image.png"
```

**Mismatched keys:**

```jsx
❌ Bad:
<Figure figKey="fig-workflow" ... />
...
<FigureReference figKey="fig-workflo" />  // Typo!

✅ Good:
<Figure figKey="fig-workflow" ... />
...
<FigureReference figKey="fig-workflow" />
```

---

## Troubleshooting

### Component Not Rendering

**Problem:** Component doesn't appear on page.

**Solutions:**

1. Check import statement is correct
2. Verify all required props are provided
3. Check for typos in component name
4. Look in browser console for error messages

### Numbers Are Wrong

**Problem:** Figure/table/equation numbers are incorrect.

**Solutions:**

1. Run `npm run counters` to regenerate numbering
2. Check for duplicate keys
3. Verify files are numbered correctly (`01-`, `02-`, etc.)
4. Restart dev server

### Cross-References Broken

**Problem:** References show "???" or don't link.

**Solutions:**

1. Ensure referenced key exists
2. Check for typos in keys
3. Run counters script
4. Verify component is on a page that's been processed

### Images Not Displaying

**Problem:** Images show broken icon.

**Solutions:**

1. Check path starts with `/figures/`
2. Verify image exists at specified path
3. Check filename matches exactly (case-sensitive)
4. Ensure image format is supported (PNG, JPG, SVG)

---

## Quick Reference Table

<TableVerticalNoRef
  alt="Table: Need to..., Use Component, Import"
  headers={[[{ value: 'Need to...' }, { value: 'Use Component' }, { value: 'Import' }]]}
  columns={[
    ['Show numbered figure', 'Reference a figure', 'Show numbered table', 'Show numbered equation', 'Add citation', 'Step-by-step process', 'Tabbed content', 'Expandable list', 'Video with caption', 'Navigation button', 'Document metadata'],
    ['<code>&lt;Figure&gt;</code>', '<code>&lt;FigureReference&gt;</code>', '<code>&lt;TableVertical&gt;</code>', '<code>&lt;Equation&gt;</code>', '<code>&lt;Citation&gt;</code>', '<code>&lt;ProcessList&gt;</code>', '<code>&lt;DocTabs&gt;</code>', '<code>&lt;CollectionList&gt;</code>', '<code>&lt;Video&gt;</code>', '<code>&lt;Button&gt;</code>', '<code>&lt;DocumentMetadata&gt;</code>'],
    ['<code>@site/src/components/Figure</code>', '<code>@site/src/components/FigureReference</code>', '<code>@site/src/components/TableVertical</code>', '<code>@site/src/components/Equation</code>', '<code>@site/src/components/Citation</code>', '<code>@site/src/components/ProcessList</code>', '<code>@site/src/components/DocTabs</code>', '<code>@site/src/components/CollectionList</code>', '<code>@site/src/components/Video</code>', '<code>@site/src/components/Button</code>', '<code>@site/src/components/DocumentMetadata</code>'],
  ]}
/>

---

## Next Steps

Now that you understand the available components:

1. **Practice:** Try using components in a test MDX file
2. **Explore:** Look at existing documentation for real-world examples
3. **Experiment:** Test different props and configurations
4. **Reference:** Bookmark this page for quick lookup

**Related guides:**

- [Creating and Editing Pages](./06-creating-editing-pages.mdx) - MDX syntax and structure
- [Project Structure](./03-project-structure.mdx) - Where to place images and files
- [Quick Reference](./08-quick-reference.mdx) - Common patterns cheat sheet

**Need help?** See [Troubleshooting & FAQ](./09-troubleshooting-faq.mdx) for solutions to common issues.
