---
title: Overview
---

import NavContainer from '@site/src/components/NavContainer';
import ModuleMap from '@site/src/components/ModuleMap';
import SchemaTag from '@site/src/components/SchemaTag';

<NavContainer
  link="/web-applications/dst"
  linkTitle="Dam Screening Tool"
  document="web-applications/dst/database-schema"
/>

# Overview

The Dam Screening Tool (DST) uses a PostgreSQL database to store all screening data, from dam information and hazard characterization through failure mode analysis and risk results. This document serves as the authoritative reference for the DST database schema.

## Module Map

The following diagram shows all application modules, the number of tables in each, and their implementation status. Click any module to navigate to its chapter.

<ModuleMap
  modules={[
    { id: 'screening', name: 'Screening', subtitle: 'Management', tables: 1, status: 'implemented', href: './screening-management', group: 'core' },
    { id: 'dam-info', name: 'Dam Info', subtitle: 'Dam Metadata', tables: 7, status: 'planned', href: './dam-information', group: 'core' },
    { id: 'hazards', name: 'Hazards', subtitle: 'Hydrologic & Seismic', tables: 4, status: 'planned', href: './hazards', group: 'hazards' },
    { id: 'fm-screening', name: 'FM Screening', subtitle: 'Mode Selection', tables: 2, status: 'planned', href: './failure-mode-screening', group: 'hazards' },
    { id: 'pfm-01', name: 'PFM-01', subtitle: 'Overtopping', tables: 4, status: 'implemented', href: './pfm-01', group: 'response' },
    { id: 'pfm-02', name: 'PFM-02', subtitle: 'BEP', tables: 4, status: 'planned', href: './pfm-02-through-09', group: 'response' },
    { id: 'pfm-03', name: 'PFM-03', subtitle: 'CLE', tables: 4, status: 'planned', href: './pfm-02-through-09', group: 'response' },
    { id: 'pfm-04', name: 'PFM-04', subtitle: 'Contact', tables: 4, status: 'planned', href: './pfm-02-through-09', group: 'response' },
    { id: 'pfm-05', name: 'PFM-05', subtitle: 'Migration', tables: 4, status: 'planned', href: './pfm-02-through-09', group: 'response' },
    { id: 'pfm-06', name: 'PFM-06', subtitle: 'Static Slope', tables: 4, status: 'planned', href: './pfm-02-through-09', group: 'response' },
    { id: 'pfm-07', name: 'PFM-07', subtitle: 'Seismic Slope', tables: 4, status: 'planned', href: './pfm-02-through-09', group: 'response' },
    { id: 'consequences', name: 'Consequences', subtitle: 'Estimation', tables: 3, status: 'planned', href: './consequences', group: 'risk' },
    { id: 'risk', name: 'Risk Analysis', subtitle: 'Computation', tables: 3, status: 'planned', href: './risk-analysis', group: 'risk' },
    { id: 'auth', name: 'Auth & Users', subtitle: 'RBAC', tables: 6, status: 'planned', href: './auth-and-system', group: 'system' },
    { id: 'dashboard', name: 'Dashboard', subtitle: 'Config', tables: 1, status: 'planned', href: './auth-and-system', group: 'system' },
    { id: 'documents', name: 'Documents', subtitle: 'Attachments', tables: 2, status: 'planned', href: './documents', group: 'system' },
  ]}
  relationships={[
    { from: 'screening', to: 'pfm-01', label: '1:N' },
    { from: 'screening', to: 'dam-info', label: '1:1' },
    { from: 'screening', to: 'hazards', label: '1:N' },
    { from: 'screening', to: 'consequences', label: '1:N' },
    { from: 'consequences', to: 'risk', label: '1:1' },
  ]}
  height={420}
/>

## Design Principles

The DST database follows several core design principles:

- **PFM-based table naming** — Tables are prefixed with their failure mode identifier (e.g., `Pfm01`, `Pfm01_DstSrp`), making it immediately clear which module a table belongs to.
- **Three-table SRP pattern** — Each PFM module uses a consistent trio of child tables (`DstSrp`, `UserSrp`, `ActiveSrp`) to separate computed, user-defined, and active SRP curves. Data is never lost when switching modes.
- **GUID primary keys** — All tables use `UUID` primary keys generated via PostgreSQL's `gen_random_uuid()`, enabling distributed ID generation without coordination.
- **JSONB for complex nested data** — Complex structures like multi-point curves and nodal probability tables are stored as JSONB columns, enabling PostgreSQL indexing and querying while avoiding excessive normalization.
- **Clean Architecture alignment** — Database entities map to domain models in the application's Clean Architecture layers, with EF Core configurations in separate `IEntityTypeConfiguration<T>` classes.

## How to Read This Document

Each chapter documents one module of the DST database. Tables are presented as visual cards showing columns, types, constraints, and descriptions.

### Tag Legend

The following tags are used throughout this document to annotate column metadata:

| Tag | Meaning |
|-----|---------|
| <SchemaTag type="pk">PK</SchemaTag> | Primary key |
| <SchemaTag type="fk">&rarr; TableName</SchemaTag> | Foreign key referencing another table |
| <SchemaTag type="unique">UQ</SchemaTag> | Unique constraint |
| <SchemaTag type="indexed">IDX</SchemaTag> | Database index |
| <SchemaTag type="nullable">?</SchemaTag> | Column allows NULL values |
| <SchemaTag type="jsonb">JSONB</SchemaTag> | PostgreSQL JSONB column |
| <SchemaTag type="default">= value</SchemaTag> | Default value |
| <SchemaTag type="cascade">CASCADE</SchemaTag> | Cascade delete |

### Status Indicators

| Indicator | Meaning |
|-----------|---------|
| **Implemented** | Table exists in the database with EF Core migrations applied |
| **Partial** | Table has partial frontend support but no database migration |
| **Planned** | Table is anticipated but not yet implemented |
