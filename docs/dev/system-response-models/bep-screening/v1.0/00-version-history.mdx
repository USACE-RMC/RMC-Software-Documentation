---
title: Version History
---

import NavContainer from '@site/src/components/NavContainer';
import TableVersionHistory from '@site/src/components/TableVersionHistory';

<NavContainer
  link="/dev"
  linkTitle="Developer Resources"
  document="dev/system-response-models/bep-screening"
/>

# Version History

<TableVersionHistory
  versions={['1.0', '1.0', '1.0']}
  dates={['April 2026', 'July 2026', 'August 2026']}
  descriptions={[
    'Initial release.',
    'Updated for the revised model: Bayesian flaw assessment replaces the qualitative flaw-likelihood selection; the unfiltered-exit node combines a confining-layer defect-survival model with blanket-theory blowout; the blowout and heave analyses route to Blanket Theory Case 2 or Case 4 based on the upstream seepage barrier; the infinite-blanket (full-head) simplification is replaced by finite-blanket head loss with a resolved downstream seepage path; and the input contract is expanded (slope-segment embankment geometry, tri-state confining-layer characterization, upstream blanket, downstream seepage berm, optional tailwater, and multiple embankment materials).',
    'Updated for revised inputs: the seepage-path lengths (upstream blanket length and the upstream and downstream confining-layer extents) are entered as single values rather than distributions, leaving the unknown-extent default as the only distributional downstream path; the qualitative relative-density categories map to uniform ranges rather than triangular distributions, and a new Custom category accepts a site-specific relative-density distribution; the inert anisotropy-ratio input is removed.',
  ]}
  modifiedBy={['-', '-', '-']}
  reviewedBy={['-', '-', '-']}
  approvedBy={['-', '-', '-']}
/>
