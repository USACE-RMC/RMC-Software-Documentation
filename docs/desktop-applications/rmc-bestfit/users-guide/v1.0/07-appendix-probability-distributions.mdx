---
title: Appendix B - Probability Distributions
---

import TableAcronyms from "@site/src/components/TableAcronyms";
import NavContainer from "@site/src/components/NavContainer";
import Link from "@docusaurus/Link";
import addBaseUrl from "@docusaurus/useBaseUrl";
import TableBestFitDistributions from "@site/src/components/TableBestFitDistributions";
import EquationNoRef from "@site/src/components/EquationNoRef";
import Citation from "@site/src/components/Citation";
import CitationFootnote from "@site/src/components/CitationFootnote";

<NavContainer
  link="/desktop-applications/rmc-bestfit"
  linkTitle="RMC-BestFit"
  document="desktop-applications/rmc-bestfit/users-guide"
/>

# Appendix B - Probability Distributions

## Normal and Related Distributions

### Normal

<TableBestFitDistributions
  headers={["Field", "Details"]}
  columns={[
    [
      "Description",
      "Parameters",
      "Support",
      "Distribution Functions",
    ],
    [
      { value: (
        <div>
          The Normal (or Gaussian) distribution is a very widely used two-parameter probability distribution. It is fundamental to most statistical 
          modeling due to the Central Limit Theorem. It fits many natural phenomena such as body height, blood pressure, measurement error, and annual 
          rainfall.
        </div>
        ),
      },
      { value: (
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            <span>location (μ)</span>
            <span>scale (σ)</span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <EquationNoRef equation="-∞ < x < +∞" />
            <EquationNoRef equation="-∞ < \mu < +∞" />
            <EquationNoRef equation="0 < \sigma < +∞" />
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <EquationNoRef 
              equation="f(x \mid \mu, \sigma) = \frac{1}{\sigma \sqrt{2\pi}} e^{-\frac{1}{2} \left( \frac{x - \mu}{\sigma} \right)^2}"
              
           />
            <EquationNoRef 
              equation="F(x \mid \mu, \sigma) = \frac{1}{2} \left[ 1 + \operatorname{erf} \left( \frac{x - \mu}{\sigma \sqrt{2}} \right) \right]"
              
           />
            <EquationNoRef 
              equation="F^{-1}(p \mid \mu, \sigma) = \mu + \sigma \sqrt{2} \, \operatorname{erf}^{-1}(2p - 1)"
              
           />
          </div>
        )
      }
    ]
  ]}
  colWidths={["25ch", "auto"]}
/>

### Log-Normal

<TableBestFitDistributions
  headers={["Field", "Details"]}
  columns={[
    [
      "Description",
      "Parameters",
      "Support",
      "Distribution Functions",
    ],
    [
      { value: (
        <div style={{ display: 'flex', flexDirection: 'column' }}>
          <span>The log-Normal distribution is a two-parameter positively skewed distribution that describes a random variable whose logarithm is 
          Normally distributed. A log-Normal process arises from the multiplicative product of many independent random variables, each of 
          which is positive. In hydrology, the log-Normal distribution is used for frequency analysis of annual maximum discharge. In reliability 
          analysis, it is often used to model times to repair a system.</span><br></br>
          <span>RMC-BestFit contains two log-Normal distributions. The first, named “Ln-Normal” is based on the natural logarithm, or log base e. 
          This distribution is parameterized using real-space moments to be more intuitive for multi-disciplinary users of the software. The other 
          distribution, named “Log-Normal” uses log base 10 and is parameterized using log_10 moments, which is consistent with typical practice in 
          hydrologic frequency analysis. Both of these log-Normal distributions are functionally identical, and will produce the same statistical 
          inference.</span>
        </div>
        ),
      },
      { value: (
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            <span>location (μ)</span>
            <span>scale (σ)</span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <EquationNoRef equation="-∞ < x < +∞" />
            <EquationNoRef equation="-∞ < \mu < +∞" />
            <EquationNoRef equation="0 < \sigma < +∞" />
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <EquationNoRef
              equation="f(x \mid \mu, \sigma) = \frac{1}{x \sigma \sqrt{2\pi}} e^{-\frac{1}{2} \left( \frac{\ln x - \mu}{\sigma} \right)^2}"
              />
            <EquationNoRef
              equation="F(x \mid \mu, \sigma) = \frac{1}{2} \left[ 1 + \operatorname{erf} \left( \frac{\ln x - \mu}{\sigma \sqrt{2}} \right) \right]"
              />
            <EquationNoRef
              equation="F^{-1}(p \mid \mu, \sigma) = e^{\mu + \sigma \sqrt{2} \, \operatorname{erf}^{-1}(2p - 1)}"
              />
          </div>
        )
      }
    ]
  ]}
  colWidths={["25ch", "auto"]}
/>

## Gamma Family of Distributions

### Exponential

<TableBestFitDistributions
  headers={["Field", "Details"]}
  columns={[
    [
      "Description",
      "Parameters",
      "Support",
      "Distribution Functions",
    ],
    [
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            <span>
              The two-parameter (or shifted) Exponential distribution is a special case of the Gamma family of distributions, which includes the two-parameter Gamma, Pearson Type III, and Log-Pearson Type III.
              The Exponential distribution describes the time between events in a Poisson process; i.e., a process in which events occur continuously and independently at a constant average rate.
              The Exponential distribution is often used in reliability applications, where it can be used to model data with a constant failure rate.
              This distribution is useful for modeling highly positively skewed data that have a non-zero lower bound.
            </span>
          </div>
        ),
      },
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            <span>location (ξ)</span>
            <span>scale (α)</span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <EquationNoRef
              equation="\xi \leq x < +\infty"
              />
            <EquationNoRef
              equation="-\infty < \xi < +\infty"
              />
            <EquationNoRef
              equation="0 < \alpha < +\infty"
              />
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <EquationNoRef
              equation="f(x \mid \xi, \alpha) = \alpha^{-1} e^{-\left(\frac{x - \xi}{\alpha}\right)}"
              />
            <EquationNoRef
              equation="F(x \mid \xi, \alpha) = 1 - e^{-\left(\frac{x - \xi}{\alpha}\right)}"
              />
            <EquationNoRef
              equation="F^{-1}(p \mid \xi, \alpha) = \xi - \alpha \ln(1 - p)"
              />
          </div>
        )
      }
    ]
  ]}
  colWidths={["25ch", "auto"]}
/>

### Gamma

<TableBestFitDistributions
  headers={["Field", "Details"]}
  columns={[
    [
      "Description",
      "Parameters",
      "Support",
      "Distribution Functions",
    ],
    [
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
            <span>
              The Gamma distribution is a two-parameter, positively-skewed distribution. The Exponential, Erlang, and Chi-squared distributions are special cases of the Gamma distribution.
              There are three different parameterizations for the Gamma distribution in common use:
            </span>
            <ol>
              <li>With a scale parameter θ and a shape parameter κ.</li>
              <li>With an inverse scale parameter β = 1/θ, called a rate parameter, and a shape parameter α = κ.</li>
              <li>With a mean parameter μ = κθ = α/β and a shape parameter κ.</li>
            </ol>
            <span>
              RMC–BestFit uses the first parameterization, with a scale parameter θ and a shape parameter κ.
            </span>
            <span>
              The Gamma distribution has applications in hydrology, econometrics, and other applied fields.
              It is a flexible distribution that is capable of modeling many kinds of data with a mild positive skew and lower bound of zero.
            </span>
          </div>
        ),
      },
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            <span>scale (θ)</span>
            <span>shape (κ)</span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <EquationNoRef
              equation="0 < x < +\infty"
              />
            <EquationNoRef
              equation="0 < \theta < +\infty"
              />
            <EquationNoRef
              equation="0 < \kappa < +\infty"
              />
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">

            <EquationNoRef
              equation="f(x \mid \theta, \kappa) = \frac{1}{\Gamma(\kappa)\theta^\kappa} x^{\kappa - 1} e^{-\frac{x}{\theta}} \text{; where } \Gamma(\cdot)
                \text{ is the gamma function.}"
              />
            <EquationNoRef
              equation="F(x \mid \theta, \kappa) = \frac{1}{\Gamma(\kappa)} \gamma(\kappa, x/\theta) \text{; where } \gamma \text{ is the lower incomplete gamma function.}"
              />
            <EquationNoRef
              equation="F^{-1}(p \mid \theta, \kappa) \text{ has no explicit analytical form}"
              />
          </div>
        )
      }
    ]

]}
colWidths={["25ch", "auto"]}
/>

### Pearson Type III

<TableBestFitDistributions
  headerConfig={[
    { label: "Field", colSpan: 1 },
    { label: "Details", colSpan: 2 },
  ]}
  columns={[
    [
      "Description",
      "Parameters",
      "Support",
      "Distribution Functions",
    ],
    [
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
            <span>The Pearson Type III (PIII) distribution is a three-parameter distribution that is widely used in hydrologic frequency analysis. It has also been used to model 
            the probability of wind speed and rainfall intensity. The PIII distribution is deduced from the two-parameter gamma distribution, and converges to a Normal distribution 
            as its skewness (γ) approaches zero.</span>
            <span>In RMC-BestFit, the PIII distribution is parameterized using the central moments of the distribution mean (μ), standard deviation (σ), and skewness (γ). The true 
            parameters (location, scale, and shape) are computed from the specified moments. This is done because the moments of the distribution are more intuitively defined by 
            end-users familiar with Bulletin 17B <Citation citationKey="USGS1982" /> and Bulletin 17C <Citation citationKey="USGS2018" />. RMC-BestFit uses the same parameterization 
            as <Citation citationKey="HoskingWallis1997" />, with the underlying location parameter ξ, the scale parameter β, and the shape parameter α.</span>
          </div>
        ),
        colSpan: 2,
      },
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            <span>mean: μ</span>
            <span>standard deviation: σ</span>
            <span>skewness: γ</span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <span>
              If <EquationNoRef equation="γ < 0," />
              {'\u00A0'.repeat(6)}
              <EquationNoRef equation="-∞ < x ≤ ξ" />
            </span>
            <span>
              If <EquationNoRef equation="γ = 0," />
              {'\u00A0'.repeat(6)}
              <EquationNoRef equation="-∞ < x < +∞" />
            </span>
            <span>
              If <EquationNoRef equation="γ > 0," />
              {'\u00A0'.repeat(6)}
              <EquationNoRef equation="ξ ≤ x < +∞" /><br></br>{'\u00A0'}
            </span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <span>If <EquationNoRef equation="γ < 0," /><br></br>
            <EquationNoRef equation="f(x | ξ, β, α) = g(ξ - x | β, α)" /><br></br>
            <EquationNoRef equation="F(x | ξ, β, α) = 1 - G(ξ - x | β, α)" /><br></br>
            <EquationNoRef equation="F^{-1}(p | ξ, β, α) = ξ - G^{-1}(1 - p | β, α)" /></span>
            
            <span>If <EquationNoRef equation="γ = 0," /> the distribution is Normal<br></br>
            <EquationNoRef equation="f(x | μ, σ) = n(x | μ, σ)" /><br></br>
            <EquationNoRef equation="F(x | μ, σ) = N(x | μ, σ)" /><br></br>
            <EquationNoRef equation="F^{-1}(p | μ, σ) = N^{-1}(p | μ, σ)" /></span>

            <span>If <EquationNoRef equation="γ > 0," /><br></br>
            <EquationNoRef equation="f(x | ξ, β, α) = g(x - ξ | β, α)" /><br></br>
            <EquationNoRef equation="F(x | ξ, β, α) = G(x - ξ | β, α)" /><br></br>
            <EquationNoRef equation="F^{-1}(p | ξ, β, α) = ξ + G^{-1}(p | β, α)" /></span>
          </div>
        )
      }
    ],
    [
      { value: null },
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            <span>location: <EquationNoRef equation="ξ = μ - \frac{2σ}{γ}" /></span>
            <span>scale: <EquationNoRef equation="β = \frac{1}{2} σγ" /></span>
            <span>shape: <EquationNoRef equation="α = \frac{4}{γ²}" /></span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <span><EquationNoRef equation="-∞ < μ < +∞" /></span>
            <span><EquationNoRef equation="0 < σ < +∞" /></span>
            <span><EquationNoRef equation="-∞ < γ < +∞" />; however, the method of maximum likelihood can only produce a solution if <EquationNoRef equation="-2 ≤ γ ≤ +2" /></span>

          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <span>where:<br></br>
            <EquationNoRef equation="g(∙)" /> is the Gamma distribution probability density function (PDF);<br></br>
            <EquationNoRef equation="G(∙)" /> is the Gamma cumulative distribution function (CDF); and<br></br>
            <EquationNoRef equation="G^{-1}" /> is the Gamma inverse CDF.</span>
            <span>where:<br></br>
            <EquationNoRef equation="n(∙)" /> is the Normal distribution PDF;<br></br>
            <EquationNoRef equation="N(∙)" /> is the Normal CDF; and<br></br>
            <EquationNoRef equation="N^{-1}" /> is the Normal inverse CDF.</span>
            <br></br>{'\u00A0'}
            <br></br>{'\u00A0'}
            <br></br>{'\u00A0'}
          </div>
        )
      }
    ]

]}
colWidths={["25ch", "auto", "auto"]}
/>

### Log-Pearson Type III

<TableBestFitDistributions
  headerConfig={[
    { label: "Field", colSpan: 1 },
    { label: "Details", colSpan: 2 },
  ]}
  columns={[
    [
      "Description",
      "Parameters",
      "Support",
      "Distribution Functions",
    ],
    [
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>            
            <span>The log-Pearson Type III (LPIII) distribution is a flexible three-parameter distribution that describes a random variable whose logarithm 
            is PIII distributed. The LPIII distribution was originally used to model annual maximum flood flows.</span>
            <span>In RMC-BestFit, the LPIII uses log base 10 and is parameterized using log<sub>10</sub> moments of the distribution mean (μ), standard deviation 
            (σ), and skewness (γ). The true parameters (location, scale, and shape) are computed from the specified moments. This is done because the moments of 
            the distribution are more intuitively defined by end-users familiar with Bulletin 17B <Citation citationKey="USGS1982" /> and Bulletin 
            17C <Citation citationKey="USGS2018" />.</span>
          </div>
        ),
        colSpan: 2,
      },
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            <span>mean (of log): μ</span>
            <span>standard deviation (of log): σ</span>
            <span>skewness (of log): γ</span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <span>
              If <EquationNoRef equation="γ < 0," />
              {'\u00A0'.repeat(6)}
              <EquationNoRef equation="0 < x ≤ ξ" />
            </span>
            <span>
              If <EquationNoRef equation="γ = 0," />
              {'\u00A0'.repeat(6)}
              <EquationNoRef equation="0 < x < +∞" />
            </span>
            <span>
              If <EquationNoRef equation="γ > 0," />
              {'\u00A0'.repeat(6)}
              <EquationNoRef equation="ξ ≤ x < +∞" /><br></br>{'\u00A0'}
            </span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <span>If <EquationNoRef equation="γ < 0," /><br></br>
              <EquationNoRef equation="f(x | ξ, β, α) = g(\log_{10}(ξ - x) | β, α) \cdot \frac{K}{x}" /><br></br>
              <EquationNoRef equation="F(x | ξ, β, α) = 1 - G(\log_{10}(ξ - x) | β, α)" /><br></br>
              <EquationNoRef equation="F^{-1}(p | ξ, β, α) = \exp\left(\frac{ξ - G^{-1}(1 - p | β, α)}{K}\right)" />
            </span>

            <span><br></br>
              If <EquationNoRef equation="γ = 0," /> then the distribution is Normal<br></br>
              <EquationNoRef equation="f(x | μ, σ) = n(\log_{10}(x) | μ, σ) \cdot \frac{K}{x}" /><br></br>
              <EquationNoRef equation="F(x | μ, σ) = N(\log_{10}(x) | μ, σ)" /><br></br>
              <EquationNoRef equation="F^{-1}(p | μ, σ) = \exp\left(\frac{N^{-1}(p | μ, σ)}{K}\right)" />
            </span>

            <span>If <EquationNoRef equation="γ > 0," /><br></br>
              <EquationNoRef equation="f(x | ξ, β, α) = g(\log_{10}(x - ξ) | β, α) \cdot \frac{K}{x}" /><br></br>
              <EquationNoRef equation="F(x | ξ, β, α) = G(\log_{10}(x - ξ) | β, α)" /><br></br>
              <EquationNoRef equation="F^{-1}(p | ξ, β, α) = \exp\left(\frac{ξ + G^{-1}(p | β, α)}{K}\right)" />
            </span>
          </div>
        )
      }
    ],
    [
      { value: null },
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            <span>location: <EquationNoRef equation="ξ = μ - \frac{2σ}{γ}" /></span>
            <span>scale: <EquationNoRef equation="β = \frac{1}{2} σγ" /></span>
            <span>shape: <EquationNoRef equation="α = \frac{4}{γ²}" /></span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <span><EquationNoRef equation="-∞ < μ < +∞" /></span>
            <span><EquationNoRef equation="0 < σ < +∞" /></span>
            <span><EquationNoRef equation="-∞ < γ < +∞" />; however, the method of maximum likelihood can only produce a solution if <EquationNoRef equation="-2 ≤ γ ≤ +2" /></span>

          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <span>where:<br></br>
              <EquationNoRef equation="g(∙)" /> is the Gamma distribution probability density function (PDF);<br></br>
              <EquationNoRef equation="G(∙)" /> is the Gamma cumulative distribution function (CDF);<br></br>
              <EquationNoRef equation="G^{-1}" /> is the Gamma inverse CDF;<br></br>
              and <EquationNoRef equation="K = \log_{10}(e)" />.
            </span>
            <span>where:<br></br>
              <EquationNoRef equation="n(∙)" /> is the Normal distribution PDF;<br></br>
              <EquationNoRef equation="N(∙)" /> is the Normal CDF;<br></br>
              and <EquationNoRef equation="N^{-1}" /> is the Normal inverse CDF.
            </span>
            <br></br>{'\u00A0'}
            <br></br>{'\u00A0'}
            <br></br>{'\u00A0'}
          </div>
        )
      }
    ]

]}
colWidths={["25ch", "auto", "auto"]}
/>

## Extreme Value Distributions

### Gumbel (Extreme Value Type I)

<TableBestFitDistributions
  headers={["Field", "Details"]}
  columns={[
    [
      "Description",
      "Parameters",
      "Support",
      "Distribution Functions",
    ],
    [
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
            <span>
              The Gumbel, or Extreme Value Type-I (EVI) distribution, is a two-parameter distribution with a fixed positive skewness of ≈ 1.14. 
              The Gumbel distribution is used to describe the maximum (or minimum) of a number of samples, and has seen widespread use in hydrologic 
              frequency analysis. The Gumbel distribution is a particular case of the Generalized Extreme Value (GEV) distribution when the GEV shape 
              parameter is zero. The Gumbel distribution is also used for a probability plotting scale because it exaggerates the extreme tails of the 
              data.
            </span>
          </div>
        ),
      },
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            <span>location (ξ)</span>
            <span>scale (α)</span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <EquationNoRef equation="-\infty < x < +\infty" />
            <EquationNoRef equation="-\infty < \xi < +\infty" />
            <EquationNoRef equation="0 < \alpha < +\infty" />
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <span>
              <EquationNoRef equation="f(x \mid \xi, \alpha) = \frac{1}{\alpha} e^{-\left(z + e^{-z}\right)}"/>; where <EquationNoRef equation={`z = \\frac{x - \\xi}{\\alpha}`}/>
            </span>
            <EquationNoRef
              equation={`F(x \\mid \\xi, \\alpha) = e^{-e^{-z}}`}
            />
            <EquationNoRef
              equation={`F^{-1}(p \\mid \\xi, \\alpha) = \\xi - \\alpha \\ln(-\\ln(p))`}
            />
          </div>
        ),
      },
    ]
  ]}
  colWidths={["25ch", "auto"]}
/>

### Weibull

<TableBestFitDistributions
  headers={["Field", "Details"]}
  columns={[
    [
      "Description",
      "Parameters",
      "Support",
      "Distribution Functions",
    ],
    [
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
            <span>
              The Weibull distribution is a two-parameter distribution commonly used in reliability analysis. It is related to a number of other
              probability distributions. In particular, the Weibull distribution interpolates between the exponential distribution (for κ=1) and
              the Rayleigh distribution (when κ=2). If the quantity x is the “time to failure”, the Weibull distribution gives the distribution
              for which the failure rate is proportional to a power of time.
            </span>
          </div>
        ),
      },
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            <span>scale (λ)</span>
            <span>shape (κ)</span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <EquationNoRef equation="0 \leq x < +\infty" />
            <EquationNoRef equation="0 < \lambda < +\infty" />
            <EquationNoRef equation="0 < \kappa < +\infty" />
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <EquationNoRef
              equation="f(x \mid \lambda, \kappa) = \frac{\kappa}{\lambda} \left(\frac{x}{\lambda}\right)^{\kappa - 1} e^{-\left(\frac{x}{\lambda}\right)^{\kappa}}"
            />
            <EquationNoRef
              equation="F(x \mid \lambda, \kappa) = 1 - e^{-\left(\frac{x}{\lambda}\right)^{\kappa}}"
            />
            <EquationNoRef
              equation="F^{-1}(p \mid \lambda, \kappa) = \lambda \ln\left(\left(\frac{1}{1 - p}\right)^{1/\kappa}\right)"
            />
          </div>
        ),
      },
    ]
  ]}
  colWidths={["25ch", "auto"]}
/>

### Generalized Extreme Value

<TableBestFitDistributions
  headers={["Field", "Details"]}
  columns={[
    [
      "Description",
      "Parameters",
      "Support",
      "Distribution Functions",
    ],
    [
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
            <span>
              The Generalized Extreme Value (GEV) distribution is a three-parameter distribution that subsumes the three extreme-value
              distributions: Gumbel (EVI), Fréchet (EVII), and Weibull (EVIII). The shape parameter κ determines which sub-distribution the
              GEV represents. The extreme value theorem states that the GEV distribution is the limit distribution of maxima of a sequence of
              independent and identically distributed random values. GEV is used for hydrologic frequency analysis, insurance, and financial risks.
            </span>
            <span>
              RMC–BestFit uses the Hosking’s parameterization <Citation citationKey="HoskingWallis1997" />, in which a negative shape parameter κ 
              has no upper bound, and a positive κ has a fixed upper bound. Other sources adopt the opposite convention, where a negative κ implies 
              an upper bound.
            </span>
          </div>
        ),
      },
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            <span>location (ξ)</span>
            <span>scale (α)</span>
            <span>shape (κ)</span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <span>
            If <EquationNoRef equation="\kappa < 0," />
            {'\u00A0'.repeat(6)}
            <EquationNoRef equation="\xi + \frac{\alpha}{\kappa} \leq x < +\infty" />
            {'\u00A0'.repeat(6)}
            <EquationNoRef equation="-\infty < \xi < +\infty" />
            </span>
            <span>
            If <EquationNoRef equation="\kappa = 0," />
            {'\u00A0'.repeat(6)}
            <EquationNoRef equation="-\infty < x < +\infty" />
            {'\u00A0'.repeat(12)}
            <EquationNoRef equation="0 < \alpha < +\infty" />
            </span>
            <span>
            If <EquationNoRef equation="\kappa > 0," />
            {'\u00A0'.repeat(6)}
            <EquationNoRef equation="-\infty < x \leq \xi + \frac{\alpha}{\kappa}" />
            {'\u00A0'.repeat(6)}
            <EquationNoRef equation="-\infty < \kappa < +\infty" />
            </span>
            
            
            
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <EquationNoRef
              equation="\text{Let } z = \begin{cases}
              - \frac{1}{\kappa} \ln(1 - \kappa \frac{(x - \xi)}{\alpha}), & \text{if } \kappa \ne 0 \\
              \frac{x - \xi}{\alpha}, & \text{if } \kappa = 0
              \end{cases}"
            />
            <EquationNoRef
              equation="f(x \mid \xi, \alpha, \kappa) = \frac{1}{\alpha} e^{-(1 - \kappa)z - e^{-z}}"
            />
            <EquationNoRef
              equation="F(x \mid \xi, \alpha, \kappa) = e^{-e^{-z}}"
            />
            <EquationNoRef
              equation="F^{-1}(p \mid \xi, \alpha, \kappa) = \begin{cases}
              \xi + \frac{\alpha}{\kappa} \left(1 - (-\ln(p))^{\kappa} \right), & \text{if } \kappa \ne 0 \\
              \xi - \alpha \ln(-\ln(p)), & \text{if } \kappa = 0
              \end{cases}"
            />
          </div>
        ),
      },
    ]
  ]}
  colWidths={["25ch", "auto"]}
/>

### Generalized Pareto

<TableBestFitDistributions
  headers={["Field", "Details"]}
  columns={[
    [
      "Description",
      "Parameters",
      "Support",
      "Distribution Functions",
    ],
    [
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
            <span>
              The Generalized Pareto (GPA) distribution is a three-parameter distribution with a fixed lower bound. The GPA is upper bounded
              when the shape parameter is positive. When κ is zero, the distribution reduces to a shifted Exponential distribution. The GPA
              distribution is most often used with peaks-over-threshold data. Hydrologic frequency analysis uses the GPA in cases where rainfall
              or flow maxima exceed a specified threshold.
            </span>
            <span>
              RMC–BestFit uses the Hosking’s parameterization <Citation citationKey="HoskingWallis1997" />, in which a negative shape parameter 
              κ has no upper bound, and a positive κ has a fixed upper bound.
            </span>
          </div>
        ),
      },
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            <span>location (ξ)</span>
            <span>scale (α)</span>
            <span>shape (κ)</span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <span>
              If <EquationNoRef equation="\kappa \le 0," />
              {'\u00A0'.repeat(6)}
              <EquationNoRef equation="\xi \le x < +\infty" />
              {'\u00A0'.repeat(6)}
              <EquationNoRef equation="-\infty < \xi \le \text{minimum of data}" />
            </span>
            <span>
              If <EquationNoRef equation="\kappa > 0," />
              {'\u00A0'.repeat(6)}
              <EquationNoRef equation="\xi \le x \le \xi + \frac{\alpha}{\kappa}" />
              {'\u00A0'.repeat(8)}
              <EquationNoRef equation="0 < \alpha < +\infty" /></span>
                        
            <span>
              {'\u00A0'.repeat(45)}
              <EquationNoRef equation="-\infty < \kappa < +\infty" />
            </span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <EquationNoRef
              equation="\text{Let } z = \begin{cases}
            - \frac{1}{\kappa} \ln(1 - \kappa \frac{(x - \xi)}{\alpha}), & \text{if } \kappa \ne 0 \\
            \frac{x - \xi}{\alpha}, & \text{if } \kappa = 0
            \end{cases}"
            />
            <EquationNoRef
              equation="f(x \mid \xi, \alpha, \kappa) = \frac{1}{\alpha} e^{-(1 - \kappa)z}"
            />
            <EquationNoRef
              equation="F(x \mid \xi, \alpha, \kappa) = 1 - e^{-z}"
            />
            <EquationNoRef
              equation="F^{-1}(p \mid \xi, \alpha, \kappa) = \begin{cases}
            \xi + \frac{\alpha}{\kappa} \left(1 - (1 - p)^{\kappa} \right), & \text{if } \kappa \ne 0 \\
            \xi - \alpha \ln(1 - p), & \text{if } \kappa = 0
            \end{cases}"
            />
          </div>
        ),
      },
    ]
  ]}
  colWidths={["25ch", "auto"]}
/>

## Logistic Distributions

### Logistic

<TableBestFitDistributions
  headers={["Field", "Details"]}
  columns={[
    [
      "Description",
      "Parameters",
      "Support",
      "Distribution Functions",
    ],
    [
      {
        value: (
          <div>
            The Logistic distribution is a two-parameter, symmetric distribution with heavier tails (higher kurtosis) than the Normal distribution.
            The Logistic distribution has applications in hydrology for long duration discharge or rainfall, such as monthly or yearly totals. The most
            common application is in logistic regression where the errors follow a Logistic distribution.
          </div>
        ),
      },
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            <span>location (ξ)</span>
            <span>scale (α)</span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <EquationNoRef equation="-\infty < x < +\infty" />
            <EquationNoRef equation="-\infty < \xi < +\infty" />
            <EquationNoRef equation="0 < \alpha < +\infty" />
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            
            <span>
              <EquationNoRef equation="f(x \mid \xi, \alpha) = \frac{1}{\alpha} \cdot \frac{e^{-z}}{(1 + e^{-z})^2}" />; 
              where <EquationNoRef equation="z = \frac{x - \xi}{\alpha}" />
            </span>
            <EquationNoRef equation="F(x \mid \xi, \alpha) = \frac{1}{1 + e^{-z}}" />
            <EquationNoRef equation="F^{-1}(p \mid \xi, \alpha) = \xi + \alpha \ln\left(\frac{p}{1 - p}\right)" />
          </div>
        ),
      },
    ]
  ]}
  colWidths={["25ch", "auto"]}
/>

### Generalized Logistic

<TableBestFitDistributions
  headers={["Field", "Details"]}
  columns={[
    [
      "Description",
      "Parameters",
      "Support",
      "Distribution Functions",
    ],
    [
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
            <span>
              The Generalized Logistic (GLO) distribution is a heavy-tailed, three-parameter distribution. The GLO distribution has been used to fit values
              of extremes, such as stock return fluctuations and sea levels. It has been used extensively for modeling annual rainfall maxima, and for flood
              frequency analysis.
            </span>
            <span>
              RMC-BestFit uses the Hosking’s parameterization <Citation citationKey="HoskingWallis1997" />, in which a negative shape parameter κ has no
              upper bound, and a positive κ has a fixed upper bound.
            </span>
          </div>
        ),
      },
      {
        value: (
          <div style={{ display: 'flex', flexDirection: 'column' }}>
            <span>location (ξ)</span>
            <span>scale (α)</span>
            <span>shape (κ)</span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <span>
              If <EquationNoRef equation="\kappa < 0," />
              {'\u00A0'.repeat(6)}
              <EquationNoRef equation="\quad \xi + \frac{\alpha}{\kappa} \leq x < +\infty" />
              {'\u00A0'.repeat(6)}
              <EquationNoRef equation="-\infty < \xi < +\infty" />
            </span>
            <span>
              If <EquationNoRef equation="\kappa = 0," />
              {'\u00A0'.repeat(6)}
              <EquationNoRef equation="\quad -\infty < x < +\infty" />
              {'\u00A0'.repeat(12)}
              <EquationNoRef equation="0 < \alpha < +\infty" />
            </span>
            <span>
              If <EquationNoRef equation="\kappa > 0," />
              {'\u00A0'.repeat(6)}
              <EquationNoRef equation="\quad -\infty < x \leq \xi + \frac{\alpha}{\kappa}" />
              {'\u00A0'.repeat(6)}
              <EquationNoRef equation="-\infty < \kappa < +\infty" />
            </span>
          </div>
        ),
      },
      {
        value: (
          <div className="equation-stack">
            <EquationNoRef equation="z = \begin{cases} -\frac{1}{\kappa} \ln(1 - \kappa \cdot \frac{x - \xi}{\alpha}) & \text{if } \kappa \neq 0 \\ \frac{x - \xi}{\alpha} & \text{if } \kappa = 0 \end{cases}" />
            <EquationNoRef equation="f(x \mid \xi, \alpha, \kappa) = \frac{1}{\alpha} \cdot e^{-(1 - \kappa) z} \cdot (1 + e^{-z})^{-2}" />
            <EquationNoRef equation="F(x \mid \xi, \alpha, \kappa) = \frac{1}{1 + e^{-z}}" />
            <EquationNoRef equation="F^{-1}(p \mid \xi, \alpha, \kappa) = \begin{cases} \xi + \frac{\alpha}{\kappa} \left[1 - \left(\frac{1 - p}{p}\right)^{\kappa}\right] & \text{if } \kappa \neq 0 \\ \xi - \alpha \ln\left(\frac{1 - p}{p}\right) & \text{if } \kappa = 0 \end{cases}" />
          </div>
        ),
      },
    ]
  ]}
  colWidths={["25ch", "auto"]}
/>

<CitationFootnote />
