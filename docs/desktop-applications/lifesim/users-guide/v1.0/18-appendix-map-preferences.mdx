---
title: "Appendix B - Map Preferences"
---

import Link from "@docusaurus/Link";
import addBaseUrl from "@docusaurus/useBaseUrl";
import CitationFootnote from "@site/src/components/CitationFootnote";
import CollectionList from "@site/src/components/CollectionList";
import DocTabs from "@site/src/components/DocTabs";
import Figure from "@site/src/components/Figure";
import FigureInline from "@site/src/components/FigureInline";
import FigReference from "@site/src/components/FigureReference";
import NavContainer from "@site/src/components/NavContainer";
import ProcessList from "@site/src/components/ProcessList";
import TableReference from "@site/src/components/TableReference";
import TableVertical from "@site/src/components/TableVertical";
import VersionSelector from "@site/src/components/VersionSelector";

<NavContainer 
  link="/desktop-applications/lifesim"
  linkTitle="LifeSim"
  document="desktop-applications/lifesim/users-guide"
></NavContainer>

# Appendix B - Map Preferences

To help the user visualize the study and results, LifeSim has the capability to display map layers that represent the features of a study. The map
layers displayed in the map window are also listed in the <strong>Map Layers</strong> tab. Map layers provide
a geographical reference for the study area. The LifeSim GUI (<FigReference figKey="figure-231" />) describes the formats of the map layers, and
provides commands that allow the user to configure, manage, and edit map layers.

<Figure
  figKey="figure-231"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure1(4).png"
  alt="LifeSim Main Window"
  caption="LifeSim Main Window"
/>

Map layers are optional and not necessary for computing and reviewing results (see [Viewing LifeSim
Results](./14-viewing-lifesim-results.mdx)). However, to view the results of simulation animations ([View Result Map In the Map
Window](#view-result-map-in-the-map-window)), the map window is useful for helping to facilitate the review of simulation results and discussion with
emergency managers. Furthermore, viewing map layers in the map window is a useful way to examine the validity of imported datasets. For example, a
road network with missing connectivity or incorrect directions on one-way roads can lead to erroneous results. Therefore, editing the road network
dataset can provide a reliable foundation for traffic simulation ([Appendix
C](./19-appendix-editing-road-network-data-for-use-in-lifesim.mdx) provides an example of editing an imported
road network dataset in the map window).

## Map Window

The map window (<FigReference figKey="figure-231" />) provides a way to graphically display LifeSim components. The rendering engine takes advantage
of the computers graphics processing using [Open Graphics Library - OpenGL](https://www.opengl.org/) to draw the map layers. All geographic information system (GIS) data is
re-projected on the fly using [GDAL](https://www.gdal.org/) (Geospatial Data Abstraction Library).

### Map Window Toolbars

There are two toolbars located in the *Map Toolbar* (<FigReference figKey="figure-231" />): the <em>General 
Toolbar</em> and the <em>Editor Toolbar</em> (<FigReference figKey="newfigure-5-app" />). Details about each toolbar are detailed in the following sections.

<Figure
  figKey="newfigure-5-app"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/newfigure-5-app.png"
  alt="LifeSim Map Toolbar: General Toolbar and Editor Toolbar"
  caption="LifeSim Map Toolbar: General Toolbar and Editor Toolbar"
/>

#### General Toolbar

The <em>General Toolbar</em> includes the <strong>Select Tool (B)</strong>, <strong>Pan Tool (P)</strong>, <strong>Zoom In Tool (Z)</strong>, <strong>Zoom to full extents</strong>, <strong>Add Data</strong>, <strong>Query Features Tool (Q)</strong>, and <strong>Measure
Distance Tool (M)</strong>. These tools change the appearance of the cursor, as well as the functionality of the mouse cursor in the map window.

<TableVertical
  tableKey="general-toolbar-table"
  headers={
[
    [
      { value: "Tool", rowSpan: 1, colSpan: 1 },
      { value: "Functionality", rowSpan: 1, colSpan: 1 }
    ]
  ]
  }
  columns={
[
    [
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-f5089b1a.png"/>{" "}
            <strong>Select Tool (B)</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-c6751277.png"/>{" "}
            <strong>Pan Tool (P)</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-6cd2df7a.png"/>{" "}
            <strong>Zoom In Tool (Z)</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-4776282d.png"/>{" "}
            <strong>Fixed Zoom Out Tool (O)</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-ddd93902.png"/>{" "}
            <strong>Zoom to full extents</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-add-data.png"/>{" "}
            <strong>Add Data</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-878d4d2c.png"/>{" "}
            <strong>Query Features Tool (Q)</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-ca04fae1.png"/>{" "}
            <strong>Measure Distance Tool (M)</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      }
    ],
    [
      {
        value: (
          <>
            Select elements displayed in the <em>Map Window</em>. To select features, click on a feature or click,
            hold, and drag a rectangle around features to select them in the <em>Map Window</em>.
            Additionally, the <strong>Select</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-f5089b1a.png"/> tool allows users to access shortcut menus which can be customized when in editing mode.
            Refer to {" "}
            <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences" target="_blank" rel="noopener noreferrer"> 
            Appendix B
            </a> {" "} 
            for more information regarding editing mode.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Pan the <em>Map Window</em>; click the <strong>Pan</strong> tool and click, hold and move the mouse 
            around the <em>Map Window</em>. Also, if the mouse has a wheel, then the user can pan the <em>Map Window</em> by using the wheel 
            (click and hold the wheel to pan).
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Zoom in to areas of the <em>Map Window</em>. To zoom in, hold the mouse button down and outline
            the area of interest to enlarge. Also, if the mouse has a wheel, then the user can zoom in or out using the 
            wheel (wheel up zooms in and wheel down zooms out).
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Zooms out to a fixed distance. Click the <strong>Fixed Zoom Out</strong> tool once and the <em>Map Window</em> view 
            will zoom out a fixed distance. Double-click on the tool icon, and the <em>Map Window</em> view 
            will zoom out twice the fixed distance. Additionally, the user can utilize the shortcut menu option: right-click anywhere 
            in the <em>Map Window</em> to zoom in or out a fixed distance.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            A quick way to re-center the map by zooming out to the full geo-graphical extent of the map
            layers in the <em>Map Window</em>. Web layers do not affect the map extents.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            After clicking the <strong>Add Data</strong> tool, the <em>File Explorer</em> browser window will open. This browser allows 
            users to navigate to layers (including <em>&#42;.shp</em>, <em>&#42;.flt</em>, <em>&#42;.tif</em>, and <em>&#42;.vrt</em>) of 
            interest and add the layer(s) to the map window. 
            <br/><br/>
            If the user clicks the down arrow next to the <strong>Add Data</strong> tool, a shortcut menu will appear which allows 
            the user to <strong>Add New [Data]</strong>, <strong>Add Web Imagery</strong>, or <strong>Create New [Vector Features File]</strong> that 
            is added to the <em>Map Window</em>.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Identifies the geographic feature or place currently selected in the <em>Map Window</em>. Alternatively,
            multiple features can be identified by dragging a box over the desired area. Once a selection is made,
            the <em>Feature Query</em> dialog box will open with the information presented in table format separated 
            by <em>Feature</em> type. Only visible layers in the <em>Map Window</em> will be queried.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Measure the distance along a specified line. With the tool selected, click to
            initiate the measure line. Each subsequent click will add a new point to the measure line. When finished, double-click 
            on the location of the final point. The <em>Measure Results</em> dialog box will open and display the distance in the units 
            the user has chosen (feet, miles, meters, or kilometers).
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      }
    ]
  ]
  }
  alt="LifeSim General Toolbar - Available Tools and Functionality"
  caption="LifeSim General Toolbar - Available Tools and Functionality"
  colWidths={[ "25%", "auto" ]}
/>

#### Editor Toolbar

The set of tools located in the <em>Editor Toolbar</em> become active after users initiate an editing session for a map layer. When a user
right-clicks on a map layer in the **Map Layers** tab, from the shortcut menu (<FigReference figKey="figure-235" />),
click <strong>Edit</strong>, and the tools in the <em>Editor Toolbar</em> become active. Only one map layer can be edited at one time. For
lines and polygons, there are two editing modes: feature edit and vertex edit. Clicking on a selected feature puts the user into the feature edit
mode (move, delete, reverse), and double-clicking on a selected feature puts the user in the vertex edit mode (add, delete, edit vertex points).

There are some tools available during an edit session which are not available from the shortcut menu or the
<em>Editor Toolbar</em>. Clicking <strong>Delete</strong> will delete all currently selected features or currently selected vertices depending
on which mode the user is in. Clicking the <strong>R</strong> key will reverse the direction of a currently selected line feature. Also,
right-clicking during an edit session allows the user to do multiple tasks depending on what is clicked.

<Figure
  figKey="figure-235"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure235.png"
  alt="Individual Map Layer Shortcut Menu"
  caption="Individual Map Layer Shortcut Menu"
/>

<TableVertical
  tableKey="editor-toolbar-table"
  headers={[
    [
      { value: "Tool", rowSpan: 1, colSpan: 1 },
      { value: "Functionality", rowSpan: 1, colSpan: 1 }
    ]
  ]}
  columns={[
    [
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-15ecf4f5.png" />{" "}
            <strong>Add new features (A)</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-7398525a.png" />{" "}
            <strong>Insert vertex (I)</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-e9d16d85.png" />{" "}
            <strong>Break line feature into two... (K)</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-cf9d34fc.png" />{" "}
            <strong>Continue from end (lines only)</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-3da2817d.png" />{" "}
            <strong>Add new line or polygon to existing feature</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d15e7265.png" />{" "}
            <strong>Feature Snapping Settings</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-25fae2d8.png" />{" "}
            <strong>Undo (Ctrl + Z)</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d39f7782.png" />{" "}
            <strong>Redo (Ctrl + Y)</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-8d7596cf.png" />{" "}
            <strong>Save Edits (Ctrl + S)</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d8eb8f4c.png" />{" "}
            <strong>Stop Editing</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      }
    ],
    [
      {
        value: (
          <>
            Add a new feature to the editable map layer by
            left-clicking to add feature data. Selected features can be deleted by either clicking the <strong>Delete</strong>{" "}
            key, or right-click on the selected feature(s) and from the shortcut menu, click{" "}
            <strong>Delete Selected Features</strong>.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Add a vertex to an existing line or polygon map layer.
            The tool becomes available when the selected feature is in vertex edit mode. As the cursor gets close to the line or
            polygon edge, the <em>Map Window</em> will display what the updated feature will look like if a vertex is added at 
            the cursor. Left-click to insert the new vertex.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Split a line feature into two features at the point the user clicked on the line. 
            The user needs to be in the feature edit mode for this tool to be enabled.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Continue a line feature from the end point of the line feature to the point to a second point clicked in 
            the <em>Map Window</em>. Double-click to finish the line continuation edits or right-click and choose{" "}
            <strong>Finish Editing Vertices</strong> or choose a different tool to finish the line continuation. 
            The user needs to be in the vertex edit mode for this tool to be enabled.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Add a polygon or line to an existing selected polygon or line feature. For example, to add a hole to a polygon, 
            click to add points defining the hole and double-click to finish. The user needs to be in the vertex edit mode for 
            this tool to be enabled.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Define snapping rules when editing. Snapping will automatically move the desired action to the nearest snap point 
            when the cursor is close. This can be very useful when trying to line up endpoints of line features or place points 
            directly on top of other features. Enable snapping by clicking <strong>Enable Snapping</strong>. From 
            the <em>Snap to Layer</em> list, select the desired feature to snap to, then select under <em>Snapping Options</em> how 
            snapping will be applied (to all points, end points, or edges).
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Undo changes made during an edit session. Only enabled if there are edits that can be undone.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Redo changes made. Only enabled if there are edits to redo.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Saves all edits made during the editing session. Only enabled if edits have been made.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Stops the edit session. At this point a new map layer can be selected for editing. If edits have
            been made, then the user will be prompted to save them before stopping the edit session.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      }
    ]
  ]}
  alt="LifeSim Editor Toolbar - Available Tools and Functionality"
  caption="LifeSim Editor Toolbar - Available Tools and Functionality"
  colWidths={["25%", "auto"]}
/>

#### Hot Keys

If a tool has a parentheses after the tooltip, the letter in the parentheses represents a hot key (<TableReference tableKey="table-6" />) to call on
the tool without clicking on it. For example, for the <strong>Query Features</strong> tool, if the user clicks <strong>Shift-Q</strong> over the map window, the
<strong>Query Features</strong> tool will be active. 

**NOTE:** for the hot keys to work for tools located in the <em>Editor Toolbar</em>, users must first
initiate an editing session for a map layer.

<TableVertical
  tableKey="table-6"
  headers={
[
    [
      { value: "Icon", rowSpan: 1, colSpan: 1 },
      { value: "Tool Name", rowSpan: 1, colSpan: 1 },
      { value: "Hot Key(s)", rowSpan: 1, colSpan: 1 }
    ]
  ]
  }
  columns={
[
    [
      { value: <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-f5089b1a.png" />, rowSpan: 1, colSpan: 1 },
      { value: <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-c6751277.png" />, rowSpan: 1, colSpan: 1 },
      { value: <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-6cd2df7a.png" />, rowSpan: 1, colSpan: 1 },
      { value: <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-4776282d.png" />, rowSpan: 1, colSpan: 1 },
      { value: <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-878d4d2c.png" />, rowSpan: 1, colSpan: 1 },
      { value: <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-ca04fae1.png" />, rowSpan: 1, colSpan: 1 },
      { value: <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-15ecf4f5.png" />, rowSpan: 1, colSpan: 1 },
      { value: <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-7398525a.png" />, rowSpan: 1, colSpan: 1 },
      { value: <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-e9d16d85.png" />, rowSpan: 1, colSpan: 1 },
      { value: <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-25fae2d8.png" />, rowSpan: 1, colSpan: 1 },
      { value: <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d39f7782.png" />, rowSpan: 1, colSpan: 1 },
      { value: <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-8d7596cf.png" />, rowSpan: 1, colSpan: 1 },
    ],
    [
      { value: "Select Tool", rowSpan: 1, colSpan: 1 },
      { value: "Pan Tool", rowSpan: 1, colSpan: 1 },
      { value: "Zoom In Tool", rowSpan: 1, colSpan: 1 },
      { value: "Fixed Zoom Out Tool", rowSpan: 1, colSpan: 1 },
      { value: "Query Features Tool", rowSpan: 1, colSpan: 1 },
      { value: "Measure Distance Tool", rowSpan: 1, colSpan: 1 },
      { value: "Add new features", rowSpan: 1, colSpan: 1 },
      { value: "Insert vertex", rowSpan: 1, colSpan: 1 },
      { value: "Break Line Feature", rowSpan: 1, colSpan: 1 },
      { value: "Undo", rowSpan: 1, colSpan: 1 },
      { value: "Redo", rowSpan: 1, colSpan: 1 },
      { value: "Save Edits", rowSpan: 1, colSpan: 1 }
    ],
    [
      { value: "(Shift + B)", rowSpan: 1, colSpan: 1 },
      { value: "(Shift + P)", rowSpan: 1, colSpan: 1 },
      { value: "(Shift + Z)", rowSpan: 1, colSpan: 1 },
      { value: "(Shift + O)", rowSpan: 1, colSpan: 1 },
      { value: "(Shift + Q)", rowSpan: 1, colSpan: 1 },
      { value: "(Shift + M)", rowSpan: 1, colSpan: 1 },
      { value: "(Shift + A)", rowSpan: 1, colSpan: 1 },
      { value: "(Shift + I)", rowSpan: 1, colSpan: 1 },
      { value: "(Shift + K)", rowSpan: 1, colSpan: 1 },
      { value: "(Ctrl + Z)", rowSpan: 1, colSpan: 1 },
      { value: "(Ctrl + Y)", rowSpan: 1, colSpan: 1 },
      { value: "(Ctrl + S)", rowSpan: 1, colSpan: 1 }
    ]
  ]
  }
  alt="Map Window Toolbars – Hot Keys"
  caption="Map Window Toolbars – Hot Keys"
  colWidths={[8, 20, 20]}
  widthMode="intrinsic"
/>

### Map Window Properties

To set the projection (coordinate system) associated with the LifeSim study, from the LifeSim main window, from
the <strong>Mapping</strong> menu (<FigReference figKey="figure-236" />), click <strong>Map Properties</strong>. The <em>Map Properties</em>
dialog box will open (<FigReference figKey="figure-237" />). From this dialog box, the user can setup the projection for the LifeSim study.

<Figure
  figKey="figure-236"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure236.png"
  alt="Mapping Menu"
  caption="Mapping Menu"
/>

<ProcessList
items={[
   {
      title:(
         <>
         From the <em>Map Properties</em> dialog box, click <strong>Set Fixed Projection</strong> (<FigReference figKey="figure-237" />).
         </>
      ),
      freeform:(
         <>
         <Figure
            figKey="figure-237"
            src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure237.png"
            alt="Mapping Properties Dialog Box"
            caption="Mapping Properties Dialog Box"
         />
         </>
      )
   },
   {
      title:(
         <>
         An <em>Open</em> browser 
         will open (<FigReference figKey="figure-238" />). From the <em>Open</em> browser, navigate to the projection file of interest (usually any shapefile
         associated with the study area), select a projection file (<em>*.prj</em>), and click <strong>Open</strong>.
         </>
      ),
      freeform:(
         <>
         <Figure
            figKey="figure-238"
            src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure238.png"
            alt="Open Browser"
            caption="Open Browser"
         />
         </>
      )
   },
   {
      title:(
         <>
          The <em>Open</em> browser will close, and the projection for the study will now be displayed in the <em>Map Properties</em> dialog 
          box (<FigReference figKey="figure-239" />). Click <strong>OK</strong>, and the <em>Map Properties</em> dialog box will close.
         </>
      ),
      freeform:(
         <>
         <Figure
            figKey="figure-239"
            src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure239.png"
            alt="Map Properties Dialog Box - Projection Example"
            caption="Map Properties Dialog Box - Projection Example"
         />
         </>
      )
   }
]}
/>


## Map Layers Tab

The <strong>Map Layers</strong> tab (<FigReference figKey="figure-240" />) provides a tree view for the map layers that have been added to an
LifeSim study. However, only selected (<FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-checkbox.png" />) layers 
in the <strong>Map Layers</strong> tab will display in the map window (<FigReference figKey="figure-231" />). The user can turn map layers
on or off, adjust the properties of the layers, and modify the order of the layers for viewing in the map window.

<Figure
  figKey="figure-240"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure240.png"
  alt="Map Layers Tab – Map Layers"
  caption="Map Layers Tab – Map Layers"
/>

## Map Layers

Geographic Information System (GIS) files are the map layer formats supported by LifeSim and include: <em>\*.shp</em>, <em>\*.flt</em>, <em>\*.tif</em>,
and <em>\*.vrt</em>. The user can add map layers, add background map layers, remove map layers, adjust a map layer's position in the view, edit a map
layer's properties, zoom into a layer, and many other functions. The ability to manipulate the different map layers is specific for that map layer
format.

### Adding Map Layers

To add a map layer:

<ProcessList
items={[
   {
      title:(
         <>
         From the LifeSim main window, from the <strong>Mapping</strong> menu (<FigReference figKey="figure-236" />), click <strong>Add Map
         Layer</strong>. Another way is from the <em>General 
         Toolbar</em> (<FigReference figKey="newfigure-5-app" />), click <FigureInline src = "figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-add-data.png"/>. Either 
         way, the <em>Browse
         for files</em> browser (<FigReference figKey="figure-241" />) will open.
         </>
      ),
      freeform: (
        <>
          <Figure
            figKey="figure-241"
            src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure241.png"
            alt="Browse for files Browser Window"
            caption="Browse for files Browser Window"
          />
        </>
      ),
   },
   {
      title:(
         <>
         From the <em>Browse for files</em> browser (<FigReference figKey="figure-241" />), navigate to the GIS file of interest that will be added
         as a map layer. Select the appropriate GIS file (<em>*.shp</em>, <em>*.flt</em>, <em>*.tif</em>, or <em>*.vrt</em>) and the <strong>File
         name</strong> box will now display the selected file name.
         </>
      )
   },
   {
      title:(
         <>
          Click <strong>Open</strong>, and the <em>Browse for files</em> browser (<FigReference figKey="figure-241" />) will close. The selected dataset
          will now be displayed in the map window, and the selected map layer name will appear in the <em>Map Layer Tree</em >.
         </>
      )
   }
]}
/>

### Adding Background Internet Maps

Knowing where the study is located in reference to known geographic positions can be quite helpful with communication. There are two types of internet
background maps: <strong>OpenStreetMap</strong> and <strong>ESRI World Imagery</strong> map. There are two ways to add these background internet
maps.

To add a background internet map to the LifeSim study, from the <strong>Mapping</strong> menu (<FigReference figKey="figure-242" />), point to <strong>Add Web 
Layer</strong>, and select the internet map from the available list that would be a good
background map for the study area. Another way is from the <em>General Toolbar</em>,
click the <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-add-data.png"/> <strong>Add Data</strong> dropdown menu, point 
to <strong>Add Web Imagery</strong> (<FigReference figKey="figure-242" />), and
select the internet map from the available list that would be a good background map for the study area. Either way, the selected background map
displays in the map window (e.g., <strong>ESRI World Imagery</strong> in <FigReference figKey="figure-243" />).

<Figure
  figKey="figure-242"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure242.png"
  alt="Mapping Menu - General Toolbar - Add Web Imagery Options"
  caption="Mapping Menu - General Toolbar - Add Web Imagery Options"
/>

### Map Layer Position in the Map Layer Tree

Once a map layer is added to a LifeSim study, the user can adjust the position of a map layer. Within the LifeSim framework, some map layers represent
a particular LifeSim dataset (i.e., hydraulic data, structure inventories, emergency planning zones), while other map layers provide information.

The <em>Map Layer Tree</em> (<FigReference figKey="figure-240" />) displays the map layers from top to bottom, where the top-most layer
overlays the next one down in the list and continues to the bottom layer. If a background internet map is added from the <strong>Add
Data</strong> dropdown menu <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-0211c61a.png" />, that map layer 
will automatically be added to the bottom of the <em>Map Layer Tree</em> and will be underneath all layers listed above the internet map. At times, it may be 
desirable to have various
map layers drawn in a different order in the map window. 

To move map layers:

<ProcessList
items={[
   {
      title:(
         <>
         From the <em>Map Layer Tree</em> (<FigReference figKey="figure-240" />), right-click on the map layer of interest. From the shortcut menu
         (<FigReference figKey="figure-243" />), either select <strong>Move Up</strong> or <strong>Move Down</strong>.
         </>
      ),
      freeform:(
         <>
         <Figure
            figKey="figure-243"
            src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure243.png"
            alt="LifeSim Main Window - Map Layers Tree - Internet Map Shortcut Menu"
            caption="LifeSim Main Window - Map Layers Tree - Internet Map Shortcut Menu"
            />
         </>
      )
   },
   {
      title:(
         <>
         To move the map layer of interest below other map layers listed in the <em>Map Layer Tree</em>, click <strong>Move Down</strong>. The
         selected map layer will move one step below its current position. Repeat until the map layer of interest is at the desired location within
         the <em>Map Layer Tree</em>.
         </>
      )
   },
   {
      title:(
         <>
         To move the map layer of interest above other map layers listed in the <em>Map Layer Tree</em>, click <strong>Move Up</strong>. The
         selected map layer will move one step above its current position. Repeat until the map layer of interest is at the 
         desired location within the <em>Map Layer Tree</em>.
         </>
      )
   }
]}
/>


Another way to move map layers in the <em>Map Layer Tree</em> is to drag and drop a map layer to the desired location within 
the <em>Map Layer Tree</em>. Click and hold the map layer of interest, and move the cursor up or down the <em>Map Layer 
Tree</em> (the selected map layer will move in conjunction with the mouse). A bar will show the new location where the selected map layer
will be moved (<FigReference figKey="drag-drop-map" />). Release the mouse, and the selected map layer will be in a new position.

<Figure
  figKey="drag-drop-map"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/drag-drop-map.gif"
  alt="Drag and Drop: Moving a Selected Map Layer in the Map Layers Tree"
  caption="Drag and Drop: Moving a Selected Map Layer in the Map Layers Tree"
/>

### Zoom to Layer

This option will set the zoom of the map window based on the extents of the selected map layer. From the <em>Map Layer 
Tree</em>, right-click on the map layer of interest (<FigReference figKey="figure-235" />). From the shortcut menu, click <strong>Zoom to 
Layer</strong>. The map window view will change to include the entire extent for
the selected map layer.

### Map Layer Select Options

There are three options (<strong>Select</strong> <strong>All</strong>, <strong>Select by</strong> <strong>Polygon</strong>, or <strong>Reverse
Selection</strong>) for selecting map layer features from the individual map layer shortcut menu (<FigReference figKey="figure-245" />).
<TableReference tableKey ="select-commands-table" /> describes the Select sub-menu commands. 

<Figure
  figKey="figure-245"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure245.png"
  alt="Individual Map Layer Shortcut Menu - Select Options"
  caption="Individual Map Layer Shortcut Menu - Select Options"
/>

<TableVertical
  tableKey="select-commands-table"
  headers={
[
    [
      { value: "Command", rowSpan: 1, colSpan: 1 },
      { value: "Description", rowSpan: 1, colSpan: 1 }
    ]
  ]
  }
  columns={
[
    [
      {
        value: (
          <>
            <strong>Select All</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <strong>Select by Polygon</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <strong>Reverse Selection</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
    ],
    [
      {
        value: (
          <>
            The <strong>Select All</strong> command will select all of the features within the map layer.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            The <strong>Select by Polygon</strong> command will open the <em>Select by Polygon</em> dialog box (<FigReference figKey="figure-246" />).
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            The <strong>Reverse Selection</strong> command will select the opposite. For example, if all of the features are
            selected in the map layer, then choosing <strong>Reverse Selection</strong> will de-select all of the features.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
    ]
  ]
  }
  alt="Select Sub-Menu Commands"
  caption="Select Sub-Menu Commands"
  colWidths={["20%", "auto"]}
/>

#### Select By Polygon

<ProcessList
items={[
  {
    title: (
      <>
      From the <strong>Map Layers</strong> tab, right-click on a map layer of interest (<FigReference figKey="figure-245" />). From the shortcut menu,
      point to <strong>Select</strong>, and click <strong>Select by Polygon</strong>. The <em>Select by Polygon</em> dialog 
      box will open (<FigReference figKey="figure-246" />).
      </>
    ),
    freeform: (
        <>
          <Figure
            figKey="figure-246"
            src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure246.png"
            alt="Select by Polygon Dialog Box"
            caption="Select by Polygon Dialog Box"
          />
        </>
      ),
  },
  {
    title:(
      <>
        From the <strong>Selection Polygon</strong> box (<FigReference figKey="figure-246" />), the user must select either <strong>Polygon 
        from map</strong> or <strong>Polygon from file</strong>.
      </>
    ),
    child:[
      {
        title:(
          <>
          <strong>Polygon from map</strong> (default): The user will need to select a polygon map layer that is part of the LifeSim study from the <strong>Polygon 
          map layer</strong> list (<FigReference figKey="figure-246" />), which contains a list of the polygon map layers (e.g., the <strong>Emergency Planning Zone(s)</strong> map layer)
          presently in the <strong>Map Layers</strong> tab. Notice, there is an option in the <strong>Polygon from map</strong> section 
          to <strong>Use selected polygons</strong> (<FigReference figKey="figure-246" />). The <strong>Use selected polygons</strong> option will only 
          be available when the identified <strong>Polygon map layer</strong> has
          features selected or if a subset of a layer has been selected (using the <strong>Select By Attribute</strong> option is explained
          in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-layer-attributes-dialog-box">Map Layer 
          Attributes Dialog Box</a>; or the map layer <strong>Select</strong> options are explained 
          in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-layer-select-options">Map Layer 
          Select Options</a>).
          </>
        )
      },
      {
        title:(
          <>
          <strong>Polygon from file</strong>: This allows the user to select a polygon shapefile. When selected, from
          the <strong>Polygon shapefile</strong> box (<FigReference figKey="figure-247" />), 
          click <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-output-file.png"/>. The <em>Select a 
          Polygon Shapefile</em> browser will open. The user will need to navigate to the file of choice and select the 
          desired polygon shapefile (<em>&#42;.shp</em>). Click <strong>Open</strong>, the <em>Select a Polygon Shapefile</em> browser will close, and the name 
          of the selected polygon shapefile will display
          in the <strong>Polygon shapefile</strong> box (<FigReference figKey="figure-247" />).
          </>
        ),
        freeform:(
          <>
          
          <Figure
            figKey="figure-247"
            src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure247.png"
            alt="Select by Polygon Dialog Box - Polygon from file option"
            caption="Select by Polygon Dialog Box - Polygon from file option"
          />
          </>
        )
      }
    ]
  },
  {
    title:(
      <>
      Next, from the <strong>Selection Options</strong> box (<FigReference figKey="figure-246" />), the user needs to set the selection type. The
      choices are <strong>Contains</strong> and <strong>Intersects</strong>.
      </>
    ),
    child:[
      {
        title:(
          <>
          <strong>Contains</strong> (default): this option will select features that are completely within a
          polygon that has been selected. In other words, this option will not select any features which intersected with the border of the selecting polygon.
          </>
        )
      },
      {
        title:(
          <>
          <strong>Intersects</strong>: this option will only select features which intersects the polygon that has
          been selected. This includes features that are completely contained in the selection polygon in addition to intersecting with the border of the
          selecting polygon. 
          <br/> 
          <strong>NOTE:</strong> this option will is not available for point shapefiles.
          </>
        )
      }
    ]
  },
  {
    title:(
      <>
      The last step is the <strong>Selections Method</strong> (<FigReference figKey="figure-246" />). Available options 
      are <strong>Append</strong>, <strong>New Selection</strong>, and <strong>Select from
      Selection</strong>.
      </>
    ),
    child:[
      {
        title:(
          <>
          <strong>Append</strong>: this option adds the selection to the already selected features. This option
          will only be available if there are selected features within the input map layer.
          </>
        )
      },
      {
        title:(
          <>
          <strong>New Selection</strong> (default): this option will clear any previous selection made to the input
          map layer.
          </>
        )
      },
      {
        title:(
          <>
          <strong>Select from Selection</strong>: this option will only select features that are currently selected
          in the input map layer.
          </>
        )
      }
    ]
  },
  {
    title:(
      <>
      Click <strong>OK</strong> (<FigReference figKey="figure-246" />), and the <em>Select by Polygon</em> dialog box will close. 
      Now, the selected
      map layer will have features selected in the map window corresponding to the preferences set in the <em>Select by Polygon</em> dialog box.
      </>
    )
  }
]}
/>

## Map Layer Attributes Dialog Box

Map layer attributes can be accessed for each map layer and provides a table for the dataset referenced by the map layer. 

To access a specific map layer <em>Attributes</em> table: from the **Map Layers** tab, right-click on a map layer of interest. From the 
shortcut menu (<FigReference figKey="figure-245" />), click <strong>Open Attribute
Table</strong>. The <em>Map Layer Attributes</em> dialog box will open (<FigReference figKey="figure-248" />). Editing of the map layer attributes is detailed in
[Editing Map Layer Features and Attributes](./18-appendix-map-preferences.mdx#editing-map-layer-features-and-attributes) of this appendix.

<Figure
  figKey="figure-248"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure248.png"
  alt="Map Layer Attributes Dialog Box"
  caption="Map Layer Attributes Dialog Box"
/>

<strong>NOTE</strong>: from the table, when a single cell is selected (e.g., row 1, column 1 in <FigReference figKey="figure-248" />), the keyboard arrow keys (and the tab and enter keys)
can be utilized to step through the table one cell at a time.

### Attribute Export and Copy Tools

<TableReference tableKey="export-and-copy-tools-table"/> describes the functionality of the *Export and Copy Tools* toolbar.

<TableVertical
  tableKey="export-and-copy-tools-table"
  headers={[
    [
      { value: "Tool", rowSpan: 1, colSpan: 1 },
      { value: "Functionality", rowSpan: 1, colSpan: 1 }
    ]
  ]}
  columns={[
    [
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-57243452.png" />{" "}
            <strong>Select all cells</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-ba7cec4e.png" />{" "}
            <strong>Copy Selected Cells</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-25c86ef2.png" />{" "}
            <strong>Copy Selected Cells with Table Headers</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-94b42839.png" />{" "}
            <strong>Paste from the Clipboard into the Table</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-f28584f3.png" />{" "}
            <strong>Export Table</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      }
    ],
    [
      {
        value: (
          <>
            Selects all cells in the table.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Copies cells selected in the table. The information is copied to the clipboard and needs to be pasted to 
            another application (e.g., Microsoft Excel®) for saving.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Copies cells selected in the table and the table headers. The information is copied to the clipboard and needs 
            to be pasted to another application (e.g., Microsoft Excel®) for saving.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Pastes data copied from another table into the current table.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Opens a <em>Save As</em> browser window, which allows the user to export 
            the data in the table to a Microsoft Excel® (<em>&#42;.xlsx</em>) file.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      }
    ]
  ]}
  alt="LifeSim Export and Copy Tools - Available Tools and Functionality"
  caption="LifeSim Export and Copy Tools - Available Tools and Functionality"
  colWidths={["30%", "auto"]}
/>

### Attribute Edit Tool

<FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-start-edit.png"/> <strong>Start Feature Edit Session</strong>: enables 
editing for the map layer open in the attributes table. For more information regarding editing map layers
and map layer attributes, review [Editing Map Layer Features and
Attributes](./18-appendix-map-preferences.mdx#editing-map-layer-features-and-attributes) and [Editing in the Attribute
Table](./18-appendix-map-preferences.mdx#editing-in-the-attribute-table) respectfully.

### Attribute Selection Tools

<TableReference tableKey="selection-tools-table"/> describes the functionality of the *Selection Tools* toolbar.

<TableVertical
  tableKey="selection-tools-table"
  headers={[
    [
      { value: "Tool", rowSpan: 1, colSpan: 1 },
      { value: "Functionality", rowSpan: 1, colSpan: 1 }
    ]
  ]}
  columns={[
    [
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-47f47369.png" />{" "}
            <strong>Select Rows By Attribute</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-1ccce933.png" />{" "}
            <strong>Show All Rows</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-5abb6c18.png" />{" "}
            <strong>Show Selected Rows Only</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-db2bbbac.png" />{" "}
            <strong>Deselect All Rows</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      }
    ],
    [
      {
        value: (
          <>
            Opens the <em>Select By Attribute</em> dialog box (<FigReference figKey="figure-250"/>), which allows the user to
            perform queries, make selections, and locate features in the table. When a feature is selected in the attribute table,
            that same feature will also be selected in the map window (and vice versa).
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Display all rows (selected or unselected) in the table.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Only display features selected in the table.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Unselects all rows which were selected in the table.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      }
    ]
  ]}
  alt="LifeSim Table Selection Tools - Available Tools and Functionality"
  caption="LifeSim Table Selection Tools - Available Tools and Functionality"
  colWidths={["25%", "auto"]}
/>

<Figure
  figKey="figure-250"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure250.png"
  alt="Select By Attribute Dialog Box"
  caption="Select By Attribute Dialog Box"
/>

### Selecting Features/Rows by Attribute

To access and select specific attributes for a map layer:

<ProcessList
items={[
  {
    title:(
      <>
      From the <strong>Map Layers</strong> tab of the LifeSim main window (<FigReference figKey="figure-231" />), right-click on the map layer of
      interest. From the shortcut menu (<FigReference figKey="figure-245" />), click <strong>Open Attribute
      Table</strong>. The <em>Map Layer Attributes</em> dialog box will open (<FigReference figKey="figure-248" />)
      </>
    )
  },
  {
    title:(
      <>
      Click <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-47f47369.png" />. The <em>Select By
      Attribute</em> dialog box opens (<FigReference figKey="figure-250" />). By selecting certain attributes and using expressions, the user can
      manipulate the selected map layer. The <strong>Available Columns</strong> box (<FigReference figKey="figure-250" />) provides a list of all of the
      attributes fields of the selected map layer. Double-click on an attribute in the <strong>Available Columns</strong> box. This will add the attribute
      to the <strong>Expression</strong> box (<FigReference figKey="figure-252" />).
      </>
    )
  },
  {
    title:(
      <>
      The <strong>Functions</strong> button (<FigReference figKey="figure-250" />) opens a new window providing a selection of all available functions
      in LifeSim. Select a function from the <strong>Functions</strong> list (e.g., <strong>AND</strong> in <FigReference figKey="figure-251" />)
      to view tips for using the function. Click <strong>Insert</strong> to add the selected function 
      to the <strong>Expression</strong> box (<FigReference figKey="figure-252" />).
      </>
    ),
    freeform:(
      <>
      <Figure
        figKey="figure-251"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure251.png"
        alt="Select by Attribute - Functions"
        caption="Select by Attribute - Functions"
      />
      </>
    )
  },
  {
    title:(
      <>
       <FigReference figKey="figure-252" /> provides an example expression for selecting attributes in the table that satisfy the entered expression. The
      expression was entered by selecting <strong>AND</strong> from 
      the <strong>Functions</strong> list (<FigReference figKey="figure-251" />); <strong>Stories_Number</strong> and <strong>DayU65Population</strong> from 
      the <strong>Available Columns</strong> (<FigReference figKey="figure-252" />) list; and has set <strong>Stories_Number</strong> equal 
      to <strong>1</strong> and <strong>DayU65Population</strong> greater than <strong>100</strong>.
      </>
    ),
    freeform:(
      <>
      <Figure
        figKey="figure-252"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure252.png"
        alt="Select By Attribute Dialog Box - Example Expression"
        caption="Select By Attribute Dialog Box - Example Expression"
      />
      A message appears below the <strong>Expression</strong> box (<FigReference figKey="figure-252" />): <em>Example: First data record (row id = 0)
      would NOT get selected</em>. 
      <br/><br/>
      In other words, the first row within the attribute table does not satisfy the expression; therefore, the first row in the attributes table will not
      become selected when the <strong>Execute</strong> (<FigReference figKey="figure-252" />) button is clicked. On the other hand, if the first row did
      satisfy the expression, then the message would say <em>Example: First data record (row id = 0) WOULD get selected</em>, and the first row in the
      attribute table would become selected when the <strong>Execute</strong> (<FigReference figKey="figure-252" />) button is clicked.
      </>
    )
  },
  {
    title:(
      <>
      If the entered expression contains errors, the message section will display the issues with the expression. The message will 
      say <em>Errors found in expression</em> until the expression is completed correctly (<FigReference figKey="error-message" />).
      </>
    ),
    freeform:(
      <>
        <Figure
          figKey="error-message"
          src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/error-message.png"
          alt="Select By Attribute Dialog Box - Error Message"
          caption="Select By Attribute Dialog Box - Error Message"
        />
      </>
    )
  },
  {
    title:(
      <>
      Another resource providing help to complete an expression is the <strong>Error Log</strong> button, which is available when an expression is incorrect.
      Click <strong>Error Log</strong>, and the <em>Errors Encountered in Expression</em> dialog box will open (<FigReference figKey="figure-253" />).
      Information is provided on what the possible issues are with the defined expression.
      </>
    ),
    freeform:(
        <>
        <Figure
          figKey="figure-253"
          src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure253.png"
          alt="Errors Encountered in Expression Dialog Box"
          caption="Errors Encountered in Expression in Dialog Box"
        />
        </>
    )
  },
  {
    title:(
      <>
      Once the expression is correct, click <strong>Execute</strong> to run the expression (<FigReference figKey="figure-252" />). The <em>Select By
      Attribute</em> dialog box (<FigReference figKey="figure-252" />) will close, and the rows in the 
      table of the <em>Map Layer Attributes</em> dialog box where the expression is <em>True</em> will be highlighted in dark teal (<FigReference figKey="figure-254" />).
      Additionally, the <strong>Show Selected Rows Only</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-84ebc582.png" /> and <strong>Deselect All
      Rows</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-a94f55cf.png" /> icons are now active.
      </>
    ),
    freeform:(
      <>
        <Figure
          figKey="figure-254"
          src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure254.png"
          alt="Map Layer Attributes Dialog Box - Results of Executed Expression"
          caption="Map Layer Attributes Dialog Box - Results of Executed Expression"
        />
      </>
    )
  },
  {
    title:(
      <>  
      After a selection is made in the table, the feature is not only highlighted in the table (<FigReference figKey="figure-254" />), but is also
      highlighted in the map window for the selected map layer. For example, in <FigReference figKey="figure-255" />,
      features are highlighted in the map window following the successful execution of the defined expression. The highlighted features will be
      differentiated in bright teal <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-64717291.png" /> (as
      opposed to the original blue <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-667609b7.png" /> color).
      Double-clicking in the map window will deselect the features.
      </>
    ),
    freeform:(
      <>
      <Figure
        figKey="figure-255"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure255.png"
        alt="Map Window - Displaying Selected Features"
        caption="Map Window - Displaying Selected Features"
      />
      </>
    )
  },
  {
    title:(
      <>
      <strong>Show Selected Rows Only</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-84ebc582.png" /> will 
      only display the
      highlighted features in the table of the <em>Map Layer Attributes </em> dialog box (<FigReference figKey="figure-254" />). Clicking
      {"\n"}<FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-1ccce933.png" /> (<strong>Show All Rows</strong>)
      will bring back all of the records for the selected map layer.
      </>
    )
  }
]}
/>

### Attribute Table Shortcut Menu

Other functions and commands are available from the <em>Map Layer Attributes</em> dialog box (<FigReference figKey="figure-254" />).
If the user right-clicks on a selected row in the table, a shortcut menu will display with one option: <strong>Zoom to 
Selected</strong> (left popup within <FigReference figKey="figure-256" />). <strong>Zoom to Selected</strong> will zoom the map window view to the 
features selected in the table.

<Figure
  figKey="figure-256"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure256.png"
  alt="Attribute Table - Selected Rows (left) & Selected Columns (center) Shortcut Menus"
  caption="Attribute Table - Selected Rows (left) & Selected Columns (center) Shortcut Menus"
/>

When the user right-clicks on selected column(s) in the table, a shortcut menu will display with several
available options: **Sort Ascending**, **Sort Descending**, **Remove Sort**, **Summary Statistics**, **Find**, **Field Calculator**, 
and **Delete Column** (center popup within <FigReference figKey="figure-256" />). Other options are
available when the user is in edit mode (described in [Editing in the Attribute
Table](./18-appendix-map-preferences.mdx#editing-in-the-attribute-table)).

<DocTabs
items={[
  {
    title:"Sort Ascending",
    child:(
      <>
      Sorts the selected column in ascending (i.e., low to high; A to F) order. After clicking <strong>Sort
      Ascending</strong>, an up arrow displays in the column header (e.g., <strong>Stories_Number</strong> in <FigReference figKey="figure-256" />) to show 
      the sorting of the selected column. Double-clicking on a selected column will reverse the current sort.
      </>
    )
  },
  {
    title:"Sort Descending", 
    child:(
      <>
      Sorts the selected column in descending (i.e., high to low; F to A) order. After clicking <strong>Sort
      Descending</strong>, a down arrow displays in the column header to show the sorting of the selected column. Double-clicking on a selected column will
      reverse the current sort.
      </>
    )
  },
  {
    title:"Remove Sort", 
    child:(
      <>
       <strong>Remove Sort</strong> does not become active until either <strong>Sort Ascending</strong> or <strong>Sort 
       Descending</strong> has been clicked. <strong>Remove Sort</strong> will revert the selected column back to its original state.
      </>
    )
  },
  {
    title:"Summary Statistics", 
    child:(
      <>
       When clicked, the <em>Attribute Summary Statistics</em> dialog box will open (<FigReference figKey="figure-257" />), providing the summary 
       statistics for the selected column in the table. If the selected column has numeric values, then the summary statistics will include indicative statistics
      of the data and a histogram (similar to <FigReference figKey="figure-204" />). If the selected columns have text data, then the summary statistics
      will have a list of all the unique text values and the frequency of those text values along with a pie chart of the most frequent text values. When
      rows in the table on the <em>Map Layer Attributes</em> dialog box (<FigReference figKey="figure-254" />) are selected,
      the <strong>Selected Rows Only</strong> option (<FigReference figKey="figure-257" />) becomes available in the <em>Attribute Summary
      Statistics</em> dialog box (<FigReference figKey="figure-257" />). If the user clicks this option, the summary statistics displayed are only for
      the selected rows in the <em>Map Layer Attributes</em> table (<FigReference figKey="figure-254" />). The pie chart will lump 
      smaller counts into a new unique value and count entitled <em>Other</em> if there are more than
      four unique items associated with the selected column.

      <Figure
        figKey="figure-257"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure257.png"
        alt="Attribute Summary Statistics Dialog Box"
        caption="Attributes Summary Statistics Dialog Box"
      />
      </>
    )
  },
  {
    title: "Find", 
    child:(
      <>
      The <strong>Find</strong> option is only available when the entire table on the <em>Map Layer Attributes</em> dialog box (<FigReference figKey="figure-254" />) is 
      available. If the user has selected <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-5abb6c18.png" /> <strong>Show 
      Selected Rows Only</strong> (<FigReference figKey="figure-254" />), then the <strong>Find</strong> option will not be available. When <strong>Find</strong> is
      clicked, the <em>Find In: Attribute</em> dialog box (<FigReference figKey="figure-258" />) will open. The user can search for
      specific attributes by entering the attribute name in the <strong>Find</strong> box.
     
      <Figure
        figKey="figure-258"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure258.png"
        alt="Find In: Attribute Dialog Box"
        caption="Find In: Attribute Dialog Box"
      />
      </>
    )
  }
]}
/>

## Create New Map Layers

New map layers can be created in LifeSim using the <em>Create New Vector Features File</em> dialog box (<FigReference figKey="figure-259" />). The new 
map layers created are shapefiles (<em>*.shp</em>) which store non-topological geometry and attribute information for the spatial features
of a dataset.

<Figure
  figKey="figure-259"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure259.png"
  alt="Create New Vector Features File Dialog Box"
  caption="Create New Vector Features File Dialog Box"
/>

To create a new map layer:

<ProcessList
items={[
  {
    title:[
      <>
      From the LifeSim main window, from the <strong>Mapping</strong> menu (<FigReference figKey="figure-260" />
      ), click <strong>Create New Vector Layer</strong>. Another way is from the <em>General Toolbar</em>, click
      the <strong>Add Data</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-add-data.png" /> tool; from the
      shortcut menu, click <strong>Create New</strong> (<FigReference figKey="figure-261" />). Either way, the <em>Create New Vector Features
      File</em> dialog box (<FigReference figKey="figure-259" />) will open.
      </>
    ],
    freeform:(
      <>
        <Figure
          figKey="figure-260"
          src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure260.png"
          alt="Mapping Menu"
          caption="Mapping Menu"
        />
        <Figure
          figKey="figure-261"
          src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure261.png"
          alt="General Toolbar – Add Data Shortcut Menu"
          caption="General Toolbar – Add Data Shortcut Menu"
        />
      </>
    )
  },
  {
    title:[
      <>
        From the <strong>File Output 
        Location</strong> box (<FigReference figKey="figure-259" />), click <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-output-file.png" />. A <em>Save 
        As</em> browser will open (<FigReference figKey="figure-262" />). Navigate to the desired directory for storing the shapefile (<em>*.shp</em>). Enter 
        a name (required) in the <strong>File name</strong> box (<FigReference figKey="figure-262" />) for the new map layer. Click <strong>Save</strong>. The <em>Save
        As</em> browser will close, and the path name and file name of the new map layer will display in the <strong>File
        Output Location</strong> box (<FigReference figKey="figure-259" />).
      </>
    ],
    freeform:(
      <>
        <Figure
          figKey="figure-262"
          src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure262.png"
          alt="Save as Browser"
          caption="Save as Browser"
        />
      </>
    )
  },
  {
    title:[
      <>
      From the <strong>Feature Type</strong> dropdown list (<FigReference figKey="figure-259" />), select the geographic features of the shapefile which can be
      represented as points, polylines (lines), or polygons (areas).
      </>
    ],
    child:[
      {
        title:[
        <>
        <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d5573627.png" /> <strong>Point:</strong> Features
        that are too small to be represented by lines or polygons. Examples include structure inventories and destinations.
        </>
      ]
      },
      {
        title:[
          <>
           <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-a7716e4f.png" /> <strong>Polyline:</strong> Features
            that represent the shape and location of geographic objects, such as the road networks.
          </>
        ]
      },
      {
        title:[
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-929139a6.png" /> <strong>Polygon:</strong> A set of
            many-sided area features which represent the shape and location of a feature or set of features, such as emergency planning zone(s).
          </>
        ]
      }
    ]
  },
  {
    title:[
      <>
       Users have the option to select a projection for the new map layer from another map layer. To add a reference map coordinate system (create a
      projection), from the <strong>Projection</strong> box (<FigReference figKey="figure-259" />), click <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-output-file.png" />. An <em>Open</em> browser 
      will open (<FigReference figKey="figure-238" />). Navigate to the location of the projection file that contains the projection that will be used for the
      new map layer (<em>*.prj</em>), select the file, and click <strong>Open</strong>. The <em>Open</em> browser will close, and the <em>Create 
      New Vector Features File</em> dialog box <strong>Projection</strong> will now contain the file location of the selected projection file (<FigReference figKey="figure-259" />).
      </>
    ]    
  },
  {
    title:[
      <>
       Click <strong>OK</strong>, and the <em>Create New Vector Features File</em> dialog box  (<FigReference figKey="figure-259" />) will close. The
       LifeSim <strong>Map Layers</strong> tab (<FigReference figKey="figure-240" />) will now contain the newly 
       created map layer.
      </>
    ]
  },
  {
    title:[
      <>
      The next step after creating a new map layer is to add new features. Adding new features to the map layer will be discussed in detail 
      in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#editing-map-layer-features-and-attributes">Editing
      Map Layer Features and Attributes</a>.
      </>
    ]
  }
]}
/>

## Remove Map Layers

To remove map layers from a LifeSim study:

<ProcessList
items={[
  {
    title:[
      <>
      From the LifeSim main window, from the <strong>Mapping</strong> menu, point to <strong>Remove Map
      Layer</strong> (<FigReference figKey="figure-263" />). From the list of map layers, click on the map layer that is to be removed from the LifeSim
      study. The map layer is removed.
      </>
    ],
    freeform:(
      <>
      <Figure
        figKey="figure-263"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure263.png"
        alt="Mapping Menu - Remove Map Layer"
        caption="Mapping Menu - Remove Map Layer"
      />
      </>
    )
  },
  {
    title:[
      <>
      Another option is from the LifeSim main window, from the <strong>Map Layers</strong> tab, right-click on
      the map layer of interest (<FigReference figKey="figure-235" />). From the shortcut menu, click <strong>Remove</strong>. The selected map layer will
      be removed from the LifeSim study. <strong>Remove</strong> will allow the user to select multiple map layers for removal.
      </>
    ],
  }
]}
/>


## Map Layer Tools

In LifeSim, there are tools available to perform operations on map layers that can be useful in preparing data and analyzing results (e.g., exporting
shapefiles, clipping features, or creating buffer polygons). Currently, tools are only available for points, lines, and polygons shapefiles but will be
expanded in the future to include gridded data. 

From the <strong>Map Layers</strong> tab on the LifeSim main window (<FigReference figKey="figure-231" />) 
right-click on a map layer of interest. From the shortcut
menu, point on <strong>Tools</strong>, from the
submenu (<FigReference figKey="figure-264" />) are several commands that are available for editing map layers. These commands change according to the
feature type (points, polylines, or polygons).

<Figure
  figKey="figure-264"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure264.png"
  alt="Individual Map Layer Shortcut Menu - Tools Sub-menu Commands"
  caption="Individual Map Layer Shortcut Menu - Tools Sub-menu Commands"
/>

The following is an overview of each command:

<DocTabs
items={[
  {
    title:"Export to New Shape",
    child:(
      <>
      Exports the selected map layer as a new shapefile (i.e., output shapefile), saved with a name provided by the user. There are
      options to save the new shapefile with the map window projected coordinate system or the original coordinate system set for the selected map layer.
      Another alternative is to export only selected features for the map layer when a subset of the map layer's features has been selected. This command
      is available for points, polylines, and polygon feature types. 
      See <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#export-to-new-shapefile">Export to New
      Shapefile</a> for further details on the use of this command.
      </>
    )
  },
  {
    title:"Buffer",
    child:(
      <>
      Creates polygons around features within the selected map layer for a set distance around the object called a buffer (<FigReference figKey="figure-265" />). There 
      is an option to merge the buffered features in the created polygon shapefile. If the merge option
      is selected, all overlapping buffer polygons will be merged into one features and all attribute information will be removed from all of the new
      buffered features of the created polygon shapefile. This command is available for points, polylines, and polygon feature types. 
      See <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#buffer">Buffer</a> for
      further details on the use of this command.

      <Figure
        figKey="figure-265"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure265.png"
        alt="Buffer Tool Options - Illustration by Feature Type"
        caption="Buffer Tool Options - Illustration by Feature Type"
      />
      </>
    )
  },
  {
    title:"Create Centroids Shapefile",
    child:(
      <>
      Calculates the center point for the polygon(s) in the selected map layer and creates a centroids shapefile. An
      additional option allows the user to only include selected features in the centroid’s shapefile. This command is only available for polygon feature
      types. 
      See <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#create-centroids-shapefile">Create 
      Centroids Shapefile</a> for further details on the use of this command.
      </>
    )
  },
  {
    title:"Clip",
    child:(
      <>
      <strong>Clip</strong> can be utilized to extract features from the selected map layer using a polygon map layer or polygon shapefile to clip the input map
      layer features to within the polygon boundaries (<FigReference figKey="figure-266" />). This command is available for polylines and polygon feature
      types. 
      See <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#clip-tool">Clip
      Tool</a> for further details on the use of this command.

      <Figure
        figKey="figure-266"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure266.png"
        alt="Clip Tool - Illustration by Feature Type"
        caption="Clip Tool – Illustration by Feature Type"
      />
      </>
    )
  },
  {
    title:"Simplify",
    child:(
      <>
      <strong>Simplify</strong> is utilized for simplifying the source polyline or polygon map feature by eliminating points based on the <strong>Simplification 
      Method</strong> chosen. There are three methods to choose from: <strong>Visalingam Whyatt</strong>, <strong>Lang</strong>, and <strong>Douglas 
      Peucker</strong>. Based on the <strong>Simplification Method</strong> chosen, the dialog
      box will change to accommodate the parameters for the method of choice. This command is available for polylines and polygon feature types. 
      See <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#simplify">Simplify</a> for 
      further details on the use of this command.
      </>
    )
  }
]}
/>

### Export to New Shapefile

To export a selected map layer to a new shapefile:

<ProcessList
items={[
  {
    title:[
      <>
      From the <strong>Map Layers</strong> tab, right-click on the map layer of
      interest (<FigReference figKey="figure-264" />). From the shortcut menu, click <strong>Export to New Shape</strong>. The <em>Export To
      Shapefile</em> dialog box will open (<FigReference figKey="figure-267" />).
      </>
    ],
    freeform:(
      <>
      <Figure
        figKey="figure-267"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure267.png"
        alt="Export to Shapefile Dialog Box"
        caption="Export to Shapefile Dialog Box"
      />
      </>
    )
  },
  {
    title:[
      <>  
      From the <strong>Output Shapefile</strong> box (<FigReference figKey="figure-267" />), click <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-output-file.png" />. A <em>Save 
      As</em> browser will open (<FigReference figKey="figure-262" />). Navigate to the desired location for storing the new shapefile (<em>*.shp</em>), and enter a 
      name (required) in the <strong>File name</strong> box for the new shapefile.
      </>
    ]
  },
  {
    title:[
      <>
      Click <strong>Save</strong>. The <em>Save As</em> browser (<FigReference figKey="figure-262" />) will close, and the <em>Export To
      Shapefile</em> dialog box (<FigReference figKey="figure-267" />) will now display the path and name for the new shapefile to be saved in
      the <strong>Output Shapefile</strong> box.
      </>
    ],
  },
  {
    title:[
      <>  
       Next, the user needs to select the projection for the new shapefile. From the <strong>Output Projection</strong> box (<FigReference figKey="figure-267" />), the 
       available options are <strong>Use Map Projection</strong> or <strong>Use Source Projection</strong>.
      </>
    ],
    child:[
      {
        title:[
          <>
          <strong>Use Map Projection</strong>: this will set the new shapefile's projected coordinate system to that of the LifeSim study.
          </>
        ]
      },
      {
        title:[
          <>
          <strong>Use Source Projection</strong>: this will set the new shapefile's projected coordinate system to that of the selected map layer.
          </>
        ]
      }
    ]
  },
  {
    title:[
      <>
        An optional step -- <strong>Export Selected Features</strong> (<FigReference figKey="figure-267" />) -- allows the user to save the new shapefile with only features
        that have been selected for the selected map layer (i.e., using the <strong>Select By Attribute</strong> option explained 
        in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-layer-attributes-dialog-box">Map Layer Attributes Dialog
        Box</a>; or the <strong>Select</strong> options explained 
        in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-layer-select-options">Map Layer Select
        Options</a>).
      </>
    ]
  },
  {
    title:[
      <>
      Click <strong>OK</strong>, and the <em>Verify Add Results</em> message window (<FigReference figKey="figure-268" />) will open asking the user
      if the newly created shapefile should be added to the map window. Click <strong>Yes</strong> to add the new
      shapefile to the map window, and the <em>Verify Add Results</em> message window will close.
      </>
    ],
    freeform:(
      <>
      <Figure
        figKey="figure-268"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure268.png"
        alt="Verify Add Results Message Window"
        caption="Verify Add Results Message Window"
      />
      </>
    )
  }
]}
/>

### Create Centroids Shapefile

To create a centroids shapefile from a selected map layer (polygon shapefile):

<ProcessList
items={[
  {
    title:[
      <>
      From the <strong>Map Layers</strong> tab, right-click on the map layer of
      interest (<FigReference figKey="figure-264" />). From the shortcut menu, click <strong>Create Centroids Shapefile</strong>. The <em>Create
      Centroids</em> dialog box will open (<FigReference figKey="figure-269" />).
      </>
    ],
    freeform:(
      <>
      <Figure
        figKey="figure-269"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure269.png"
        alt="Create Centroids Dialog Box"
        caption="Create Centroids Dialog Box"
      />
      </>
    )
  },
  {
    title:[
      <>
      From the <strong>Output Shapefile</strong> box (<FigReference figKey="figure-269" />), click <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-output-file.png" />. A <em>Save 
      As</em> browser will open (<FigReference figKey="figure-262" />). Navigate to the desired location for storing the new centroids shapefile (<em>*.shp</em>), and enter a 
      name (required) in the <strong>File name</strong> box for the new centroids shapefile.
      </>
    ]
  },
  {
    title:[
      <>
      Click <strong>Save</strong>, and the <em>Save As</em> browser (<FigReference figKey="figure-262" />) will close. The <em>Create
      Centroids</em> dialog box (<FigReference figKey="figure-269" />) will now display the path and shapefile name for the new centroids shapefile to
      be saved in the <strong>Output Shapefile</strong> box.
      </>
    ]
  },
  {
    title:[
      <>
      Next, the user needs to select the projection for the new centroids shapefile. From the <strong>Output Projection</strong> box (<FigReference figKey="figure-269" />), the 
      available options are <strong>Use Map Projection</strong> or <strong>Use Source Projection</strong>.
      </>
    ],
    child:[
      {
        title:[
          <>
          <strong>Use Map Projection</strong>: this will set the new shapefile's projected coordinate system to that of the LifeSim study.
          </>
        ]
      },
      {
        title:[
          <>
          <strong>Use Source Projection</strong>: this will set the new shapefile's projected coordinate system to that of the selected map layer.
          </>
        ]
      }
    ]
  },
  {
    title:[
      <>
        An optional step -- <strong>Only for Selected Features</strong> (<FigReference figKey="figure-269" />) -- allows the user to save the new shapefile with only features
        that have been selected for the selected map layer (i.e., using the <strong>Select By Attribute</strong> option explained 
        in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-layer-attributes-dialog-box">Map Layer Attributes Dialog
        Box</a>; or the <strong>Select</strong> options explained 
        in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-layer-select-options">Map Layer Select
        Options</a>).
      </>
    ]
  },
  {
    title:[
      <>
      Click <strong>OK</strong>. The <em>Verify Add Results</em> message window (<FigReference figKey="figure-268" />) will open asking the user
      if the newly created shapefile should be added to the map window. Click <strong>Yes</strong> to add the new
      shapefile to the map window. The <em>Verify Add Results</em> message window will close, and the new centroid shapefile will display.
      </>
    ]
  }
]}
/>


### Buffer

To create a new shapefile that is based one set distance of a selected map layer:

<ProcessList
items={[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab, right-click on the map layer of
        interest (<FigReference figKey="figure-264" />). From the shortcut menu, click <strong>Buffer</strong>. The <em>Create
        Buffers Polygon(s)</em> dialog box will open (<FigReference figKey="figure-271" />).
      </>
    ],
    freeform:(
      <>
      <Figure
        figKey="figure-271"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure271.png"
        alt="Create Buffer Polygon(s) Dialog Box"
        caption="Create Buffer Polygon(s) Dialog Box"
      />
      </>
    )
  },
  {
    title:[
      <>
        From the <strong>Output Shapefile</strong> box (<FigReference figKey="figure-271" />), click <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-output-file.png" />. A <em>Save 
        As</em> browser will open (<FigReference figKey="figure-262" />). Navigate to the desired location for storing the new shapefile (<em>*.shp</em>), and enter a 
        name (required) in the <strong>File name</strong> box for the new shapefile.
      </>
    ]
  },
  {
    title:[
      <>
        Click <strong>Save</strong>, and the <em>Save As</em> browser (<FigReference figKey="figure-262" />) will close. The <em>Create
        Buffer Polygon(s)</em> dialog box (<FigReference figKey="figure-271" />) will now display the path and shapefile name for the new shapefile to
        be saved in the <strong>Output Shapefile</strong> box.
      </>
    ]
  },
  {
    title:[
      <>
      Next, the user needs to select the projection for the new shapefile. From the <strong>Output Projection</strong> box (<FigReference figKey="figure-271" />), the 
      available options are <strong>Use Map Projection</strong> or <strong>Use Source Projection</strong>.
      </>
    ],
    child:[
      {
        title:[
          <>
          <strong>Use Map Projection</strong>: this will set the new shapefile's projected coordinate system to that of the LifeSim study.
          </>
        ]
      },
      {
        title:[
          <>
          <strong>Use Source Projection</strong>: this will set the new shapefile's projected coordinate system to that of the selected map layer.
          </>
        ]
      }
    ]
  },
  {
    title:[
      <>
      The user will need to enter the buffer distance for the new shapefile. Under <strong>Buffer Options</strong> (<FigReference figKey="figure-271" />), in 
      the <strong>Buffer Distance</strong> box, enter the buffer distance in feet (e.g., <strong>50</strong>).
      </>
    ]
  },
  {
    title:[
      <>
        An optional step -- <strong>Only Buffer Selected Features</strong> (<FigReference figKey="figure-271" />) -- allows the user to save the new shapefile with only features
        that have been selected for the selected map layer (i.e., using the <strong>Select By Attribute</strong> option explained 
        in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-layer-attributes-dialog-box">Map Layer Attributes Dialog
        Box</a>; or the <strong>Select</strong> options explained 
        in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-layer-select-options">Map Layer Select
        Options</a>).
      </>
    ]
  },
  {
    title:[
      <>
        Click <strong>OK</strong>. The <em>Verify Add Results</em> message window (<FigReference figKey="figure-268" />) will open asking the user
      if the newly created shapefile should be added to the map window. Click <strong>Yes</strong> to add the new
      shapefile to the map window. The <em>Verify Add Results</em> message window will close, and the new shapefile will display.
      </>
    ]
  }
]}
/>


### Clip Tool

To create a shapefile of a selected map layer that has been clipped (points and polygons):

<ProcessList
items={[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab, right-click on the map layer of
        interest (<FigReference figKey="figure-264" />). From the shortcut menu, click <strong>Clip</strong>. The <em>Clip by 
        Polygon</em> dialog box will open (<FigReference figKey="figure-272" />).
      </>
    ],
    freeform:(
      <>
      <Figure
        figKey="figure-272"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure272.png"
        alt="Clip by Polygon Dialog Box"
        caption="Clip by Polygon Dialog Box"
      />
      </>
    )
  },
  {
    title:[
      <>
        From the <strong>Output Shapefile</strong> box (<FigReference figKey="figure-272" />), click <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-output-file.png" />. A <em>Save 
        As</em> browser will open (<FigReference figKey="figure-262" />). Navigate to the desired location for storing the new shapefile (<em>*.shp</em>), and enter a 
        name (required) in the <strong>File name</strong> box for the new shapefile.
      </>
    ],
  },
  {
    title:[
      <>
        Click <strong>Save</strong>, and the <em>Save As</em> browser (<FigReference figKey="figure-262" />) will close. The <em>Clip by 
        Polygon</em> dialog box (<FigReference figKey="figure-272" />) will now display the path and shapefile name for the new shapefile to
        be saved in the <strong>Output Shapefile</strong> box.
      </>
    ]
  },
  {
    title:[
      <>
       Next, the user needs to clip the selected map layer based on polygon shapefiles that are part of the LifeSim study or from other polygon
      shapefiles. The available options are: <strong>Polygon From Map</strong> or <strong>Polygon From File</strong>.
      </>
    ],
    child:[
      {
        title:[
          <>
            <strong>Polygon From Map</strong>: under the <strong>Clip Polygon</strong> box (<FigReference figKey="figure-272" />), from the <strong>Clip Polygon 
            Shapefile</strong> list, all polygon map layers (e.g., an Emergency Planning Zone) imported in the current LifeSim study will
            be available. Select the desired polygon map layer to clip the selected map layer. An optional feature is <strong>Use Selected polygons 
            for clip</strong> (<FigReference figKey="figure-272" />), which is only available when the selected map layer has features selected (i.e., using the <strong>Select 
            By Attribute</strong> option explained 
            in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-layer-attributes-dialog-box">Map Layer Attributes Dialog
            Box</a>; or the <strong>Select</strong> options explained 
            in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-layer-select-options">Map Layer Select
            Options</a>).
          </>
        ]
    },
    {
      title:[
        <>
          <strong>Polygon From File</strong>: under the <strong>Clip Polygon</strong> box (<FigReference figKey="figure-273" />), select <strong>Polygon From
          File</strong>. From the <strong>Clip
          Polygon Shapefile</strong> box, click <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-output-file.png" />. The <em>Please 
          select a clip polygon shapefile</em> browser will open. Navigate to the file of choice
          and select the desired polygon shapefile (<em>&#42;.shp</em>). Click <strong>Open</strong>. If the selected file is a polygon shapefile,
          the <em>Please select a clip polygon shapefile</em> browser will close, and the selected file's path and name will display 
          in the <strong>Clip Polygon Shapefile</strong> box (<FigReference figKey="figure-273" />). However, if the file selected is 
          not a polygon shapefile, then a warning message window will open (<FigReference figKey="figure-274" />).
        </>
      ],
      freeform:[
        <>
        <Figure
          figKey="figure-273"
          src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure273.png"
          alt="Clip by Polygon Dialog Box - Polygon from File Option"
          caption="Clip by Polygon Dialog Box - Polygon from File Option"
        />
        <Figure
          figKey="figure-274"
          src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure274.png"
          alt="OpenGLMapping Warning Message"
          caption="OpenGLMapping Warning Message"
        />
        </>
      ]
    }
    ]
  },
  {
    title:[
      <>
      Next, the user needs to select the projection for the new shapefile. From the <strong>Output Projection</strong> box (<FigReference figKey="figure-272" />), the 
      available options are <strong>Use Map Projection</strong> or <strong>Use Source Projection</strong>.
      </>
    ],
    child:[
      {
        title:[
          <>
          <strong>Use Map Projection</strong>: this will set the new shapefile's projected coordinate system to that of the LifeSim study.
          </>
        ]
      },
      {
        title:[
          <>
          <strong>Use Source Projection</strong>: this will set the new shapefile's projected coordinate system to that of the selected map layer.
          </>
        ]
      }
    ]
  },
  {
    title:[
      <>
        An optional step -- <strong>Only Clip Selected Features</strong> (<FigReference figKey="figure-272" />) -- allows the user to save the new shapefile with only features
        that have been selected for the selected map layer (i.e., using the <strong>Select By Attribute</strong> option explained 
        in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-layer-attributes-dialog-box">Map Layer Attributes Dialog
        Box</a>; or the <strong>Select</strong> options explained 
        in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-layer-select-options">Map Layer Select
        Options</a>).
      </>
    ]
  },
  {
    title:[
      <>
        Click <strong>OK</strong>. The <em>Verify Add Results</em> message window (<FigReference figKey="figure-268" />) will open asking the user
      if the newly created shapefile should be added to the map window. Click <strong>Yes</strong> to add the new
      shapefile to the map window. The <em>Verify Add Results</em> message window will close, and the new shapefile will display.
      </>
    ]
  }
]}
/>

### Simplify

To create a shapefile of a selected polygon map layer that has been simplified (polylines and polygons):

<ProcessList
items= {[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab, right-click on the map layer of
        interest (<FigReference figKey="figure-264" />). From the shortcut menu, click <strong>Simplify</strong>. The <em>Simplify Polygon(s)</em> dialog 
        box (<FigReference figKey="figure-275" />) will open.
      </>
    ],
    freeform:(
      <>
      <Figure
        figKey="figure-275"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure275.png"
        alt="Simplify Polygon(s) Dialog Box"
        caption="Simplify Polygon(s) Dialog Box"
      />
      </>
    )
  },
  {
    title:[
      <>
        From the <strong>Output Shapefile</strong> box (<FigReference figKey="figure-275" />), click <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-output-file.png" />. A <em>Save 
        As</em> browser will open (<FigReference figKey="figure-262" />). Navigate to the desired location for storing the new shapefile (<em>*.shp</em>), and enter a 
        name (required) in the <strong>File name</strong> box for the new shapefile.
      </>
    ]
  },
  {
    title:[
      <>
        Click <strong>Save</strong>, and the <em>Save As</em> browser (<FigReference figKey="figure-262" />) will close. The <em>Simplify
        Polygon(s)</em> dialog box (<FigReference figKey="figure-275" />) will now display the path and shapefile name for the new shapefile to
        be saved in the <strong>Output Shapefile</strong> box.
      </>
    ]
  },
  {
    title:[
      <>
      Next, the user needs to select the projection for the new shapefile. From the <strong>Output Projection</strong> box (<FigReference figKey="figure-275" />), the 
      available options are <strong>Use Map Projection</strong> or <strong>Use Source Projection</strong>.
      </>
    ],
    child:[
      {
        title:[
          <>
          <strong>Use Map Projection</strong>: this will set the new shapefile's projected coordinate system to that of the LifeSim study.
          </>
        ]
      },
      {
        title:[
          <>
          <strong>Use Source Projection</strong>: this will set the new shapefile's projected coordinate system to that of the selected map layer.
          </>
        ]
      }
    ]
  },
  {
    title:[
      <>
      The user must select method for simplifying the selected map layer. Under the <strong>Simplify Options</strong> box (<FigReference figKey="figure-275" />), from 
      the <strong>Simplification Method</strong> list, select the
      appropriate simplification method. Each method has its own set of inputs. The simplification methods are briefly described:
      </>
    ],
    child:[
      {
        title:[
          <>
          <strong>Visalingam Whyatt</strong>: An area-based algorithm to eliminate points based on each individual points effective area (area within the
          polygon the point effects). The point(s) which affect the polygon the least will be removed first. The user must enter the maximum number of points
          (greater than four and less than two points than the total number of points in the polygon) to be eliminated (<strong>Number of Points</strong> box,
          {"\n"}<FigReference figKey="figure-275" />). If the number of points entered is greater than the total number of points in the
          polygon, then an identical copy of the polygon will be made.
          </>
        ]
      },
      {
        title:[
          <>
          <strong>Lang</strong>: A perpendicular distance-based algorithm to eliminate points based on the perpendicular distance of the points. The
          perpendicular distance is compared to a user entered tolerance limit for intermediate points between the end point and the entered number of look
          ahead points. The algorithm works based on the simplify inputs (<strong>Number of Points</strong> box, <FigReference figKey="figure-275" />),
          tolerance, and look ahead points that the user enters. The <strong>Tolerance</strong> value must be greater than zero and the <strong>Look Ahead
          Points</strong> number must be less than or equal to the total number of points in the polyline or polygon shapefile. The point(s) which have a
          perpendicular distance less than the <strong>Tolerance</strong> limit for a set number of <strong>Look Ahead Points</strong> will be eliminated until
          there are no more intermediate points. If the number of <strong>Look Ahead Points</strong> entered is greater than the total number
          of points in the polygon or polyline shapefile, then an identical copy of the selected shapefile will be made.
          </>
        ]
      },
      {
        title:[
          <>
          <strong>Douglas Peucker</strong>: Similar to the <strong>Lang</strong> method, in that the points are eliminated based on the points distance from
          a simplification line. The simplification line is a straight line between key points. Key points are set for points greater than
          the <strong>Tolerance</strong> level defined by the user. The <strong>Tolerance</strong> level is the <strong>Simplify Inputs</strong> for
          the <strong>Douglas Peucker</strong> method, and the value must be greater than zero. In the end, only the key points will remain after this simplification
          method is used.
          </>
        ]
      }
    ]
  },
  {
    title:[
      <>
        An optional step -- <strong>Simplify Only Selected Features</strong> (<FigReference figKey="figure-275" />) -- allows the user to save the new shapefile with only features
        that have been selected for the selected map layer (i.e., using the <strong>Select By Attribute</strong> option explained 
        in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-layer-attributes-dialog-box">Map Layer Attributes Dialog
        Box</a>; or the <strong>Select</strong> options explained 
        in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-layer-select-options">Map Layer Select
        Options</a>).
      </>
    ]
  },
  {
    title:[
      <>
        Click <strong>OK</strong>. The <em>Verify Add Results</em> message window (<FigReference figKey="figure-268" />) will open asking the user
        if the newly created shapefile should be added to the map window. Click <strong>Yes</strong> to add the new
        shapefile to the map window. The <em>Verify Add Results</em> message window will close, and the new shapefile will display.
      </>
    ]
  }
]}
/>

## Map Layers Properties

The properties of map layers can be viewed or edited in the <em>Map Layer Properties</em> dialog box (<FigReference figKey="figure-276" />) of the selected 
map layer. Depending on the map layer feature type (point, polyline, polygon, or gridded
data), the options available in the <em>Map Layer Properties</em> dialog box will vary accordingly. The <em>Map Layer Properties</em> dialog
box is organized into two tabs which allow the user to view and modify map window display features (<strong>Feature Draw Properties</strong> tab) and to 
view the map layer source information (<strong>Source Information</strong> tab).

### Feature Draw Properties Tab

The <strong>Feature Draw Properties</strong> tab (<FigReference figKey="figure-276" />) provides the user with the ability to edit map layer
symbology (type, color, size, and other options).

<Figure
  figKey="figure-276"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure276.png"
  alt="Map Layer Properties Dialog Box - Feature Draw Properties Tab"
  caption="Map Layer Properties Dialog Box - Feature Draw Properties Tab"
/>

#### Draw Style

<DocTabs
items={[
  {
    title:"Uniform",
    child:(
      <>
        Only the symbol can be modified directly. In other words, all features will be drawn with the same symbology.
      </>
    )
  },
  {
    title:"Category", 
    child:(
      <>
        The symbol(s) can be edited by unique attribute names or values. For example, one-way roads can be drawn differently
        than two-way roads.
      </>
    )
  },
  {
    title:"Value", 
    child:(
      <>
        The symbol(s) can be modified for a range of attribute values. For example, structures can be drawn differently for larger
        populations than smaller populations.
      </>
    )
  },
  {
    title:"Heat", 
    child:(
      <>
        The symbol(s) can be displayed as a heat map for a range of attribute values. For example, structures can be drawn
        differently for lower life loss than higher life loss.
      </>
    )
  }
]}
/>

#### Line and Fill Color

The user can modify the color for <strong>Line</strong> or <strong>Fill</strong> color settings.

<Figure
  figKey="figure-277"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure277.png"
  alt="Map Layer Properties - Select a Color Dialog Box"
  caption="Map Layer Properties - Select a Color Dialog Box"
/>

<DocTabs
items={[
  {
    title:"Preview",
    child:(
      <>
        This display box provides a preview of the selected color.
      </>
    )
  },
  {
    title:"Color Pallet", 
    child:(
      <>
        This display area provides saturation (amount of black) options for the
        chosen color in the <strong>Color Bar</strong>. Notice 
        the <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-3a0eaea1.png" /> white-black 
        circle in the <strong>Color Bar</strong> display area. This circle shows the location of the selected color. Click anywhere 
        inside the <strong>Color Pallet</strong> area, and the white circle will be moved to the new location and 
        the <strong>Color Properties</strong> values
        will change automatically.
      </>
    )
  },
  {
    title:"Color Bar", 
    child:(
      <>
        This vertical slider bar allows the user to choose the color from the options
        available (ROYGBIVR).
      </>
    )
  },
  {
    title:"Transparency", 
    child:(
      <>
        The <strong>Transparency</strong> horizontal slider bar changes the
        transparency of the color (also known as <strong>A</strong> for Alpha). For maximum color (zero transparency of the color), slide the bar
        completely to the right (<em>Alpha</em> will change to <strong>255</strong> as in <FigReference figKey="figure-277" />). For zero 
        color (or maximum transparency of the color), slide the bar completely to the
        left (<em>Alpha</em> will change to <strong>0</strong>).
      </>
    )
  },
  {
    title:"Color Properties", 
    child:(
      <>
        Provides value boxes for <strong>A</strong> (Alpha, measure of 
        opacity), <strong>R</strong> (Red), <strong>G</strong>(Green), and <strong>B</strong> (Blue). Each box is a measure
        (from 0 to 255) of opacity or color. The maximum value is 255 and minimum value is zero. Modifying these numbers also 
        affects the saturation (amount
        of black; the lower the value, the more black). 
        <br/><br/>
        Example of <strong>Color Properties</strong> values (for all examples, the <em>Alpha</em> value = 255):
        <br/><br/>
        <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-4e78c6c7.png" /> Bright Red: Red 255, Green 0, Blue 0
        <br/>
        <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-dfdada00.png" /> White: Red 255, Green 255, Blue 255
        <br/>
        <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-a1a40cac.png" /> Bright Yellow: Red 255, Green 255, Blue 0
        <br/>
        <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-34a2f93c.png" /> Bright Teal: Red 0, Green 255, Blue 255
      </>
    )
  },
  {
    title:"Common Colors", 
    child:(
      <>
        Provides twelve commonly used colors; click on a color and the <em>Select a Color</em> dialog box (<FigReference figKey="figure-277" />) will 
        update with the color chosen.
      </>
    )
  }
]}
/>

#### Symbol Size & Line Width

The **Symbol Size** (for point map layers) or **Line Width** (for polyline of polygon map layers) is a number representing the 
size/width of the symbol/line being displayed (<FigReference figKey="figure-276" />). The numerical value of 
size/width ranges from 1 to 50. The smaller the number, the smaller/thinner the symbol/line will be.

#### Uniform Draw Style

To adjust symbol type, symbol size, and the symbol line and fill color for a point map layer:

<ProcessList
items={[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab, right-click on the map layer of
        interest (<FigReference figKey="figure-264" />). From the shortcut menu, click <strong>Properties</strong>. The <em>Map Layer
        Properties</em> dialog box for the selected map layer will open (<FigReference figKey="figure-276" />).
      </>
    ]
  },
  {
    title:[
      <>
      The first option available to the user to modify the point map layer is to change the <strong>Draw Style</strong> definitions. For example, the
      default setting for point (as well as polygon, and polyline) map layer properties is <strong>Draw Style: Uniform</strong> (<FigReference figKey="figure-276" />). At 
      this point, keep the default <strong>Draw Style: Uniform</strong> setting.
      </>
    ]
  },
  {
    title:[
      <>
      To change the symbol type from the default blue 
      house (<FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-667609b7.png" /> ), select a new symbol from the
      available <strong>Symbols</strong> box (<FigReference figKey="figure-278" />). Selecting a new symbol will automatically adjust the symbol preview
      (symbol in the top-right box of the <em>Map Layer Properties</em> dialog box).
      </>
    ],
    freeform:[
      <>  
      <Figure
        figKey="figure-278"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure278.png"
        alt="Map Layer Properties Dialog Box - Symbols"
        caption="Map Layer Properties Dialog Box - Symbols"
      />
      </>
    ]
  },
  {
    title:[
      <>
      Click <strong>Apply</strong> (<FigReference figKey="figure-276" />). All of the features in the map layer will be updated to the newly 
      selected symbol. Once <strong>Apply</strong> is clicked, this change cannot be undone.
      </>
    ],
  },
  {
    title:[
      <>
       To adjust the map symbol line color, 
       select <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-unchecked.png"/> <strong>Line Color</strong>. Then, click 
       the <em>Line Color preview</em> box. The <em>Select a Color</em> dialog box will open (<FigReference figKey="click-line-color" />).
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="click-line-color"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/click-line-color.gif"
        alt="Properties Dialog Box - Selecting a Line Color - Select a Color Dialog Box"
        caption="Properties Dialog Box - Selecting a Line Color - Select a Color Dialog Box"
      />
      </>
    ]
  },
  {
    title:[
      <>
      Modify the <strong>Selected Color</strong> in the <em>Select a Color</em> dialog box (<FigReference figKey="click-line-color" />) until the
      desired color is achieved. Click <strong>OK</strong>, and the <em>Select a Color</em> dialog box will close. The user 
      will be returned to the <em>Map Layer Properties</em> dialog box (<FigReference figKey="figure-276" />) where the new
      color will be displayed in the <strong>Line Color</strong> box and displayed in the symbol preview box.
      </>
    ]
  },
  {
    title:[
      <>
      Repeat Steps 5 and 6 for the <strong>Fill Color</strong> (<FigReference figKey="figure-276" />) if the <strong>Fill Color</strong> needs to
      be modified.
      </>
    ]
  },
  {
    title:[
      <>
      To modify the size of the symbol to be displayed in the map window, enter the new size in the <strong>Size</strong> box (<FigReference figKey="figure-276" />).
      </>
    ],
    child:[
      {
        title:[
          <>
          For polylines and polygon shapefiles, the <strong>Size</strong> box is labeled <strong>Line Width</strong> and modifies the line (or polygon outline)
          width.
          </>
        ]
      },
      {
        title:[
          <>
          An extra option for a polyline <em>Map Layer Properties</em> dialog box is the option for
          adding an arrow at the end of lines. To add an arrow at the end of the lines, select the <strong>Draw End Arrow</strong> option located under 
          the <strong>Line Width</strong> box (<FigReference figKey="draw-end-arrow" />).
          </>
        ],
        freeform:[
          <>
          <Figure
            figKey="draw-end-arrow"
            src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/draw-end-arrow.png"
            alt="Polyline Properties Dialog Box - Draw End Arrow"
            caption="Polyline Properties Dialog Box - Draw End Arrow"
          />
          </>
        ]
      }
    ]
  },
  {
    title:[
      <>
      Once editing of the symbol type, symbol size, and the symbol line and fill color for a point map layer is complete, 
      click <strong>Apply</strong> (<FigReference figKey="figure-276" />), and
      the <em>Map Layer Properties</em> dialog box will close.
      </>
    ]
  }
]}
/>

#### Category Draw Style

To adjust <strong>Draw Style: Category</strong> (<FigReference figKey="figure-276" />) map layer property options for a point map layer:

<ProcessList
items={[
  {
    title:[
      <>
      From the <strong>Map Layers</strong> tab, right-click on the point map layer
      of interest. From the shortcut menu (<FigReference figKey="figure-235" />), click <strong>Properties</strong>. The <em>Map Layer 
      Properties</em> dialog box for the selected map layer will open (<FigReference figKey="figure-276" />).
      </>
    ]
  },
  {
    title:[
      <>
      The first option available to the user to modify the point map layer is to change <strong>Draw Style</strong>. From the <strong>Draw 
      Style</strong> list (<FigReference figKey="figure-276" />), the default value is <strong>Uniform</strong>. From
      the list, the user can select other options.
      </>
    ]
  },
  {
    title:[
      <>
      From the <strong>Draw Style</strong> list (<FigReference figKey="figure-276" />), select <strong>Category</strong>. Now
      the <strong>Attribute</strong> list is available (<FigReference figKey="figure-280" />). The <strong>Attribute</strong> list contains all of 
      the attributes contained in the selected map layer.
      </>
    ],
    freeform:[
      <>
        <Figure
          figKey="figure-280"
          src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure280.png"
          alt="Map Layer Properties Dialog Box - Category Draw Style - Attribute List"
          caption="Map Layer Properties Dialog Box - Category Draw Style - Attribute List"
        />
      </>
    ]
  },
  {
    title:[
      <>
      From the <strong>Attribute</strong> list (<FigReference figKey="figure-280" />), select the attribute of interest to draw the map layer by.
      </>
    ]
  },
  {
    title:[
      <>
      For the attribute that is selected, from the <em>Category-Symbol</em> table (<FigReference figKey="figure-281" />), the
      table will update with the range of values (text or numerical) for the selected attribute. <FigReference figKey="figure-281" /> provides an
      example for the <strong>Type_Construction</strong> attribute selection, with the range of values equal 
      to <strong>Manufactured</strong>, <strong>Wood</strong>, <strong>Masonry</strong>, <strong>Concrete</strong>, and <strong>Steel</strong>. By 
      default, the <strong>Excluded Values</strong> category is included at the bottom of the
      table with a default color and size.
      </>
    ],
    freeform:[
      <>
        <Figure
          figKey="figure-281"
          src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure281.png"
          alt="Map Layer Properties Dialog Box – Category Draw Style - Category-Symbol Table"
          caption="Map Layer Properties Dialog Box – Category Draw Style - Category-Symbol Table"
        />
      </>
    ]
  },
  {
    title:[
      <>
      The example shown in <FigReference figKey="figure-281" /> also displays the default symbol assignment for the range of values present. These
    defaults can be modified by the user and must be edited individually.
      </>
    ]
  },
  {
    title:[
      <>
      To change a symbol, select a row in the <em>Category-Symbol</em> table (<FigReference figKey="figure-281" />), and the
      selected symbol will be displayed in the <em>Preview</em> box. From the <strong>Symbols</strong> box, click on the desired new symbol. 
      The <em>Preview</em> box and the symbol in the <em>Category-Symbol</em> table will automatically update with the newly selected
      symbol.
      </>
    ],
  },
  {
    title:[
      <>
        To adjust the map symbol line color, 
        select <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-unchecked.png"/> <strong>Line Color</strong>. Then, click 
        the <em>Line Color preview</em> box. The <em>Select a Color</em> dialog box will open (<FigReference figKey="click-line-color" />).
      </>
    ]
  },
  {
    title:[
      <>
       Select the desired color in the <em>Select a Color</em> dialog box (<FigReference figKey="click-line-color" />). Click <strong>OK</strong>, and
      the <em>Select a Color</em> dialog box will close. The user will be 
      returned to the <em>Map Layer Properties</em> dialog box (<FigReference figKey="figure-281" />) where the new color will be 
      displayed in the <strong>Line Color</strong> box and displayed in the symbol
      preview box.
      </>
    ]
  },
  {
    title:[
      <>
      Repeat Steps 7 through 9 for all of the desired values listed in the <em>Category-Symbol</em> table.
      </>
    ]
  },
  {
    title:[
      <>
      Once editing of the <strong>Draw Style: Category</strong> for a point map layer is complete, click <strong>Apply</strong> to
      view the changes in the map window.
      <br/><br/>
      <strong>NOTE:</strong> Make sure the map layer is selected to be viewed in the map window and is above other map layers. Once 
      the desired drawing style is achieved, click <strong>Apply</strong> and then click <strong>Close</strong>. The <em>Map Layer
      Properties</em> dialog box (<FigReference figKey="figure-281" />) will close.
      </>
    ],
  },
  {
    title:[
      <>
      Editing the <strong>Draw Style: Category</strong> for polyline and polygon shapefiles is mainly the same as point shapefiles. The only
      difference in the previously mentioned options is users can change the <strong>Line Width</strong> (polylines and polygons) instead
      of <strong>Size</strong> (points), and there is an added option to <strong>Draw End Arrow</strong> (polylines only).
      See Step 8a of <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#uniform-draw-style">Uniform
      Draw Style</a> for details.
      </>
    ]
  }
]}
/>


#### Value Draw Style

Another option for modifying a map layer's properties is to adjust the <strong>Draw Style</strong> option to <strong>Value</strong> for a
specific attribute.

<ProcessList
items={[
  {
    title:[
      <>
      From the <strong>Map Layers</strong> tab, right-click on the point map layer
      of interest. From the shortcut menu (<FigReference figKey="figure-235" />), click <strong>Properties</strong>. The <em>Map Layer 
      Properties</em> dialog box for the selected map layer will open (<FigReference figKey="figure-276" />).
      </>
    ]
  },
  {
    title:[
      <>
      The first option available to the user to modify the point map layer is to change <strong>Draw Style</strong>. From the <strong>Draw 
      Style</strong> list, the default value is <strong>Uniform</strong>. From
      the list, the user can select other options.
      </>
    ]
  },
  {
    title:[
      <>
      From the <strong>Draw Style</strong> list, select <strong>Value</strong> (<FigReference figKey="figure-283" />). Now
      the <strong>Attribute</strong> list is available. The <strong>Attribute</strong> list contains all of 
      the attributes contained in the selected map layer.
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="figure-283"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure283.png"
        alt="Map Layer Properties Dialog Box – Value Draw Style - Attribute List"
        caption="Map Layer Properties Dialog Box – Value Draw Style - Attribute List"
      />
      </>
    ]
  },
  {
    title:[
      <>
      From the <strong>Attribute</strong> list, select the attribute of interest to draw the map layer by. Notice
      that the list of <strong>Attributes</strong> for <strong>Draw Style: Value</strong> is
      limited to only the ones with numerical values (<FigReference figKey="figure-283" />).
      </>
    ]
  },
  {
    title:[
      <>
      For the attribute that is selected, the parameters in the <strong>Draw By Value Bin 
      Definitions</strong> box (<FigReference figKey="figure-284" />) become available and will update with the range of 
      quantitative values for the selected attribute.
      <br/><br/>
      The values entered for <strong>Number</strong>, <strong>Minimum</strong>, and <strong>Maximum</strong> are used to define bins in equal intervals
      between the <strong>Minimum</strong> and <strong>Maximum</strong> values for the selected attribute. The total number of breaks (set at equal
      intervals by default) are used to determine the range of values (in the <em>Category-Symbol</em> table) by the value entered
      in the <strong>Number</strong> box.
      <br/><br/>
      The <strong>Excluded Values</strong> row (<FigReference figKey="figure-284" />) in the <em>Category-Symbol</em> table represents
      all values that are less than the <strong>Minimum</strong> or greater than the <strong>Maximum</strong> values. By default,
      the <strong>Minimum</strong> and <strong>Maximum</strong> values are set to the minimum and maximum values of the data, 
      so no features are excluded.
      <br/><br/>
      <FigReference figKey="figure-284" /> provides an example with the default, and three examples that modify the <strong>Number</strong> and
      the <strong>Maximum</strong> value fields and how those changes modify the bins displayed in the <em>Category-Symbol</em> table.
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="figure-284"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure284.png"
        alt="Map Layer Properties Dialog Box – Value Draw Style - Draw by Value Bin Definitions Box"
        caption="Map Layer Properties Dialog Box – Value Draw Style - Draw by Value Bin Definitions Box"
      />
      </>
    ]
  },
  {
    title:[
      <>
      Individual bin ranges can be adjusted in the <em>Category-Symbol</em> table by
      entering the maximum value for each range, manually. To enter the maximum value manually, double-click 
      a <em>Category-Symbol</em> row. The row
      is now editable. Then, over-write the current value and the range will update (maximum value for 
      the over-written value and the minimum value for the
      next row in the <em>Category-Symbol</em> table) (<FigReference figKey="overwrite" />).
      </>
    ],
    freeform:[
      <>
        <Figure
          figKey="overwrite"
          src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/overwrite.gif"
          alt="Map Layer Properties Dialog Box: Overwriting a value in the Category-Symbol table."
          caption="Map Layer Properties Dialog Box: Overwriting a value in the Category-Symbol table."
        />
      </>
    ],
  },
  {
    title:[
      <>
        A hint for users: sometimes the best way to determine the <strong>Draw By Value Bin Definitions</strong> is to review the 
        summary statistic. From the <em>Map Layer Attributes</em> dialog box (<FigReference figKey="figure-248" />), from the 
        shortcut menu (<FigReference figKey="figure-256" />), click <strong>Summary Statistics</strong>. For the selected 
        attribute (i.e., <strong>NightO65Population</strong>), the <em>Attribute Summary Statistics</em> dialog box 
        will open (<FigReference figKey="figure-285" />). 
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="figure-285"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure285.png"
        alt="Attribute Summary Statistics Dialog Box"
        caption="Attribute Summary Statistics Dialog Box"
      />
      </>
    ]
  },
  {
    title:[
      <>
        The examples shown in <FigReference figKey="figure-284" /> present the default symbol assignments for the range of values. These defaults
        can be modified by the user for each selected row in the <em>Category-Symbol</em> table. Each
        symbol the user wishes to modify from the default must be edited individually.
      </>
    ]
  },
  {
    title:[
      <>
      To change a symbol, select a row in the <em>Category-Symbol</em> table (<FigReference figKey="figure-281" />), and the
      selected symbol will be displayed in the <em>Preview</em> box. From the <strong>Symbols</strong> box, click on the desired new symbol. 
      The <em>Preview</em> box and the symbol in the <em>Category-Symbol</em> table will automatically update with the newly selected
      symbol.
      </>
    ]
  },
  {
    title:[
      <>
      To adjust the map symbol line color, 
      select <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-unchecked.png"/> <strong>Line Color</strong>. Then, click 
      the <em>Line Color preview</em> box. The <em>Select a Color</em> dialog box will open (<FigReference figKey="click-line-color" />).
      </>
    ]
  },
  {
    title:[
      <>
        Select the desired color in the <em>Select a Color</em> dialog box (<FigReference figKey="click-line-color" />). Click <strong>OK</strong>, and
        the <em>Select a Color</em> dialog box will close. The user will be 
        returned to the <em>Map Layer Properties</em> dialog box (<FigReference figKey="figure-281" />) where the new color will be 
        displayed in the <strong>Line Color</strong> box and displayed in the symbol
        preview box.
      </>
    ]
  },
  {
    title:[
      <>
      Repeat Steps 9 through 11 for all of the desired values listed in the <em>Category-Symbol</em> table.
      </>
    ]
  },
  {
    title:[
      <>
      Once editing of the <strong>Draw Style: Value</strong> for a point map layer is complete, click <strong>Apply</strong> and
      the <em>Map Layer Properties</em> dialog box (<FigReference figKey="overwrite" />) will close.
      </>
    ]
  }
]}
/>

#### Heat Draw Style

Another option for modifying a map layer's properties is to adjust the <strong>Draw Style</strong> option to create a heat map for a specific
attribute.

<strong>NOTE:</strong> The <strong>Heat</strong> draw style is only available for point map layers.

<ProcessList
items={[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab, right-click on the point map layer
        of interest. From the shortcut menu (<FigReference figKey="figure-235" />), click <strong>Properties</strong>. The <em>Map Layer 
        Properties</em> dialog box for the selected map layer will open (<FigReference figKey="figure-276" />).
      </>
    ]
  },
  {
    title:[
      <>
        The first option available to the user to modify the point map layer is to change <strong>Draw Style</strong>. From the <strong>Draw 
        Style</strong> list, the default value is <strong>Uniform</strong>. From
        the list, the user can select other options.
      </>
    ]
  },
  {
    title:[
      <>
        From the <strong>Draw Style</strong> list, select <strong>Heat</strong>. The <em>Map Layer Properties</em> dialog 
        box updates (<FigReference figKey="figure-286" />)
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="figure-286"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure286.png"
        alt="Map Layer Properties Dialog Box - Heat Draw Style"
        caption="Map Layer Properties Dialog Box - Heat Draw Style"
      />
      </>
    ]
  },
  {
    title:[
      <>
      From the <strong>Color Ramp</strong> list, users can select a color ramp for the heat map. The default color
      ramp is red to green (<FigReference figKey="figure-286" />). The user can change this from the dropdown <strong>Color Ramp</strong> list 
      of options (<FigReference figKey="figure-287" />).
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="figure-287"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure287.png"
        alt="Heat Draw Style – Color Ramp"
        caption="Heat Draw Style – Color Ramp"
      />
      </>
    ]
  },
  {
    title:[
      <>
      If the <strong>Color Ramp</strong> selection is not correct, 
      select <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-unchecked.png"/> <strong>Invert Colors</strong> (<FigReference figKey="figure-286" />) to
      invert the color ramp colors representation. For the example provided in <FigReference figKey="figure-288" />, the default color 
      ramp (red = <strong>More</strong>, green = <strong>Less</strong>) was inverted (red = <strong>Less</strong>, green = <strong>More</strong>).
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="figure-288"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure288.png"
        alt="Example – Invert Colors"
        caption="Example – Invert Colors"
      />
      </>
    ]
  },
  {
    title:[
      <>
      By default, the <strong>Heat</strong> <strong>Radius</strong> is set to <strong>12</strong> (<FigReference figKey="figure-286" />). Set a
   different radius by entering a different value in the <strong>Heat</strong> <strong>Radius</strong> box.
      </>
    ]
  },
  {
    title:[
      <>
      By default, the <strong>Blur</strong> <strong>Radius</strong> is set to <strong>10</strong> (<FigReference figKey="figure-286" />). Set a
   different radius by entering a different value in the <strong>Blur</strong> <strong>Radius</strong> box.
      </>
    ]
  },
  {
    title:[
      <>
      Users can weight the heat map by an attribute in the all the numerical attributes contained in the selected map layer. To add a weight to the heat
      map, check the <strong>Weight Heat 
      By</strong> checkbox <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-checkbox.png"/> (<FigReference figKey="figure-289" />) to 
      enable the list of attributes. From the list (e.g., similar to <FigReference figKey="figure-283" />), select the desired 
      attribute (e.g., <strong>Max_Depth</strong> in <FigReference figKey="figure-289" />). The <strong>Minimum 
      Heat</strong> and <strong>Maximum Heat</strong> values are set by default using the data minimum and maximum, respectfully. 
      Set a different minimum and/or maximum heat by
      entering a different value in the <strong>Minimum</strong> and/or <strong>Maximum Heat</strong> boxes.
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="figure-289"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure289.png"
        alt="Example: Heat Draw Style – Weight Heat By Enabled"
        caption="Example: Heat Draw Style – Weight Heat By Enabled"
      />
      </>
    ]
  },
  {
    title:[
      <>
      The transparency of the heat map can be changed (from the default value of fully opaque). In 
      the <strong>Transparency</strong> box (<FigReference figKey="figure-286" />), the user can enter a value from 
      zero to 100 (percent) to increase the transparency of the map layer (the maximum value of 100 percent is maximum translucency).
      </>
    ]
  },
  {
    title:[
      <>
      Once editing of the symbology (type, color ramp, and other options) for a point map layer is complete, click <strong>Apply</strong>, and the
      selected gridded map layer is updated.
      </>
    ]
  }
]}
/>

### Source Information Tab

The <strong>Source Information</strong> tab (<FigReference figKey="figure-290" />) provides the **Projection** (the coordinate system associated), 
the **Horizontal Units**, and the **Extent** of the selected map layer.

<Figure
  figKey="figure-290"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure290.png"
  alt="Map Layer Properties Dialog Box – Source Information Tab"
  caption="Map Layer Properties Dialog Box – Source Information Tab"
/>

## Gridded Map Layer Properties

The properties of gridded map layers (<em>\*.tif</em>) can be viewed or edited in the <em>Map Layer Properties</em> dialog 
box (<FigReference figKey="figure-291" />) of a selected gridded map layer. The <em>Map Layer Properties</em> dialog box
is organized into two tabs which allow the user to view and modify map window display features (<strong>Raster Draw Properties</strong> tab) and 
to view the map layer source information (<strong>Source Information</strong> tab).

### Raster Draw Properties Tab

The <strong>Raster Draw Properties</strong> tab (<FigReference figKey="figure-291" />) provides the user with the ability to edit gridded map layer
symbology (type, color or color ramp, and other options). 

<Figure
  figKey="figure-291"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure291.png"
  alt="Gridded Map Layers: Map Layer Properties Dialog Box – Raster Draw Properties Tab"
  caption="Gridded Map Layers: Map Layer Properties Dialog Box – Raster Draw Properties Tab"
/>

There are three sections in the <strong>Raster Draw Properties</strong> tab.

<DocTabs
items={[
  {
    title:"Continuous Ramp",
    child:(
      <>
        Allows the user to select a continuous <strong>Color Ramp</strong> for the gridded map layer based on the 
        selected <strong>Stretch Type</strong>.
      </>
    )
  },
  {
    title:"Stretch Type and Color Ramp",
    child:(
      <>
        The <strong>Stretch Type</strong> list (<FigReference figKey="figure-291" />) contains four options: <strong>None</strong>, <strong>Minimum 
        Maximum</strong>, <strong>Standard Deviations</strong>, and <strong>Histogram-Equalization</strong>. Depending on the 
        selection, the <strong>Max</strong>, <strong>Min</strong> and/or <strong>Deviations</strong> boxes will become active.
        <br/><br/>
        The <strong>Color Ramp</strong> list contains nine options for the user to select for the color ramp of
        the gridded map layer.
        <br/><br/>
        There are two additional options for modifying the <strong>Color Ramp</strong>. The first 
        is <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-unchecked.png" /> <strong>Invert 
        Colors</strong>, which when selected,
        will invert the selected color ramp scheme. The second is <strong>Transparency</strong>. The user will enter a transparency value (percentage) to
        modify the translucency of the selected color scheme. Lower numbers are higher opacity and higher numbers are more transparent (one hundred
        effectively removes the map layer from view).
      </>
    )
  },
  {
    title:"Specified Values",
    child:(
      <>
        <strong>Specified Values</strong> allow the user to set the number of <strong>Bins</strong>, which updates 
        the <em>Value-Symbol</em> table (<FigReference figKey="figure-291" />).

          The preset <strong>Symbol</strong> color scheme ranges from black to white (low to high values). To change the preset symbol color,
          click on the color box and the <em>Select a Color</em> dialog will open (<FigReference figKey="figure-277" />). The available 
          options for the <em>Select a Color</em> dialog box are very similar to what is described 
          in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#feature-draw-properties-tab">Feature Draw Properties
          Tab</a>.
      </>
    )
  }
]}
/>

#### Continuous Ramp Rendering

The <strong>Continuous Ramp</strong> rendering options for gridded map layers and allows the user to adjust the symbology (type, color or color ramp,
and other options) for a selected gridded map layer:

<ProcessList
items={[
  {
    title:[
      <>
        From the <em>Map Layer Properties</em> dialog box for the selected gridded map layer (<FigReference figKey="figure-291" />), from
        the <strong>Stretch Type</strong> and <strong>Color Ramp</strong> panel, users must decide which stretch type to
        select from the <strong>Stretch Type</strong> list:
      </>
    ],
    child:[
      {
        title:[
          <>
          <strong>None</strong>: The colors in the selected color ramp will be linearly distributed based on the cell values relation to the minimum and
          maximum data values.
          </>
        ]
      },
      {
        title:[
          <>
           <strong>Minimum Maximum</strong>: This option activates 
           the <strong>Min</strong> and <strong>Max</strong> boxes (<FigReference figKey="figure-291" />). The selected color ramp will 
           be stretched between the defined minimum and maximum values. This can be
          useful when the user wants data to be stretched over a very small data range. The user must use the default values or 
          input different values. If the
          default minimum and maximum data values are chosen, the gridded data will render the same as the <strong>None</strong> option.
          </>
        ]
      },
      {
        title:[
          <>
            <strong>Standard Deviations</strong> (default): This option activates 
            the <strong>Deviations</strong> box (<FigReference figKey="figure-291" />). The default <strong>Deviations</strong> value 
            is <strong>2</strong>. Users can delete and enter different deviations in the box. The ramp is stretched by
            the number of deviations from the mean data value.
          </>
        ]
      }
    ]
  },
  {
    title:[
      <>
      The default color ramp is black fading to white. The user can change this from the <strong>Color 
      Ramp</strong> list (<FigReference figKey="figure-291" />). To determine if the color ramp scheme is correct, 
      click <strong>Apply</strong> (<FigReference figKey="figure-291" />) and view the updated gridded map layer in the 
      map window (<FigReference figKey="figure-231" />). Once <strong>Apply</strong> is clicked, this change cannot be undone.
      </>
    ]
  },
  {
    title:[
      <>
        If the <strong>Color Ramp</strong> selection is not correct (e.g., higher water velocity with green colors and lower flow velocity 
        with red colors), then 
        select <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-unchecked.png" /> <strong>Invert Colors</strong> (<FigReference figKey="figure-291" />). Click <strong>Apply</strong> and 
        view the updated gridded map
        layer in the map window to confirm the color scheme selection.
      </>
    ]
  },
  {
    title:[
      <>
      The transparency of the entire gridded/raster map layer can be changed (from the default value of fully opaque). In
      the <strong>Transparency</strong> box (<FigReference figKey="figure-291" />), the user can enter a value 
      from zero to 100 (percent) to increase the
      transparency of the map layer (the maximum value of 100 percent is maximum translucency).
      </>
    ]
  },
  {
    title:[
      <>
      Once editing of the symbology (type, color or color ramp, and other options) for a gridded map layer is complete, 
      click <strong>Apply</strong> and the selected gridded map layer is updated.
      </>
    ]
  }
]}
/>

#### Rendering with Specified Values

To use <strong>Specified Values</strong> for rendering gridded map layers and adjust the symbology (type, color or color ramp, and other options) for
the selected gridded map layer:

<ProcessList
items={[
  {
    title:[
      <>
      From the <em>Map Layer Properties</em> dialog box (<FigReference figKey="figure-291" />) for the
      selected gridded map layer, select <strong>Specified Values</strong>. The <strong>Continuous 
       Ramp</strong> selection is no longer available, while the <strong>Specified 
       Values</strong> parameters become available (<FigReference figKey="figure-294" />)
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="figure-294"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure294.png"
        alt="Gridded Map Layer: Map Layer Properties Dialog Box – Specified Values Selected"
        caption="Gridded Map Layer: Map Layer Properties Dialog Box – Specified Values Selected"
      />
      </>
    ]
  },
  {
    title:[
      <>
        For <strong>Specified Values</strong>, the user needs to enter the number of bins (<FigReference figKey="figure-294" />). The default 
        number of bins is five. Values entered in
        the <strong>Bins</strong> box cannot be less than one or greater than fifty. When the number of bins is entered,
        the <em>Value-Symbol</em> table (<FigReference figKey="figure-294" />) is modified with a range of quantitative values
        (set at equal numbers) based on the number of bins specified and the total range of values in the map layer.
      </>
    ]
  },
  {
    title:[
      <>
        Individual bin ranges can be adjusted in the <em>Value-Symbol</em> table (<FigReference figKey="figure-294" />) by
        entering the maximum value for each range, manually. To enter the maximum value manually, double-click a <em>Value-Symbol</em> row. The row is
        now editable. Over-write the current value and the range will update (maximum value for the over-written value and the minimum value for the
        next row in the <em>Value-Symbol</em> table). See <FigReference figKey="overwrite" /> for an example.
      </>
    ]
  },
  {
    title:[
      <>
        The default color ramp is black fading to white. The user can change this from the <strong>Color 
        Ramp</strong> list (<FigReference figKey="figure-291" />). To determine if the color ramp scheme is correct, 
        click <strong>Apply</strong> (<FigReference figKey="figure-291" />) and view the updated gridded map layer in the 
        map window (<FigReference figKey="figure-231" />). Once <strong>Apply</strong> is clicked, this change cannot be undone.
      </>
    ]
  },
  {
    title:[
      <>
        If the <strong>Color Ramp</strong> selection is not correct (e.g., higher water velocity with green colors and lower flow velocity 
        with red colors), then 
        select <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-unchecked.png" /> <strong>Invert Colors</strong> (<FigReference figKey="figure-291" />). Click <strong>Apply</strong> and 
        view the updated gridded map
        layer in the map window to confirm the color scheme selection.
      </>
    ]
  },
  {
    title:[
      <>
      To adjust the color for a range of values, in the <em>Value-Symbol</em> table (<FigReference figKey="figure-294" />),
      click on a specific color in the table. The <em>Select a Color</em> dialog box will open (<FigReference figKey="figure-277" />).
      For details on the <em>Select a Color</em> dialog box, 
      see <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#feature-draw-properties-tab">Feature Draw Properties
      Tab</a>.
      </>
    ]
  },
  {
    title:[
      <>
        Modify the <strong>Selected Color</strong> in the <em>Select a Color</em> dialog box (<FigReference figKey="figure-277" />) until the
        desired color is achieved. Click <strong>OK</strong>, and the <em>Select a Color</em> dialog box will close. The user 
        will be returned to the <em>Map Layer Properties</em> dialog box (<FigReference figKey="figure-291" />) where the new
        color will be displayed in the <em>Value-Symbol</em> table (<FigReference figKey="figure-294" />).
      </>
    ]
  },
  {
    title:[
      <>
      Repeat Steps 6 and 7 for all of the desired values listed in the <em>Value-Symbol</em> table.
      </>
    ]
  },
  {
    title:[
      <>
      The transparency of the entire gridded/raster map layer can be changed (from the default value of fully opaque). In
      the <strong>Transparency</strong> box (<FigReference figKey="figure-291" />), the user can enter a value from zero to 100 (percent) to increase the
      transparency of the map layer (the maximum value of 100% is maximum translucency).
      </>
    ]
  },
  {
    title:[
      <>
      Once editing of the symbology (type, color or color ramp, and other options) for a gridded map layer is complete, click <strong>Apply</strong> and
      the selected gridded map layer is updated.
      </>
    ]
  }
]}
/>

### Source Information Tab

Similar to <FigReference figKey="figure-290" />, the <strong>Source Information</strong> tab
for gridded map layers provides 
the **Projection** (the coordinate system associated), 
the **Horizontal Units**, and the **Extent** of the selected map layer.

## Editing Map Layer Features and Attributes

As explained in [Map Window Toolbars](./18-appendix-map-preferences.mdx#map-window-toolbars), the map window provides several editing tools in the
*Editor Toolbar*. Additional map layer editing can be accomplished from the *Map Layer Attributes* dialog box (<FigReference figKey="figure-295" />). The
editing capabilities of the map window are described in [Adding Features to a New
Shapefile](./18-appendix-map-preferences.mdx#adding-features-to-a-new-shapefile) for newly created shapefiles, [Editing Point Map
Layers](./18-appendix-map-preferences.mdx#editing-point-map-layers) for point map layers, and [Editing Polyline or Polygon Map
Layers](./18-appendix-map-preferences.mdx#editing-polyline-or-polygon-map-layers) for polyline and polygon map layers. 

This section describes editing
map layers from the attributes table.

The map window (<FigReference figKey="figure-231" />) and *Map Layer Attributes* dialog box (<FigReference figKey="figure-295" />) editing tools 
are designed to facilitate in adjusting the geometric and attribute data directly within the
software to increase overall productivity. For example, moving structures or editing roads for use in the simulation can be done quickly and easily from
the LifeSim software interface.

It is important to note that although more than one attribute table can be open at one time, only one map layer can be edited at a time. Also, it is
beneficial to save often while editing a map layer.

<Figure
  figKey="figure-295"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure295.png"
  alt="Map Layer Attribute Dialog Box – Active Editing Session"
  caption="Map Layer Attribute Dialog Box – Active Editing Session"
/>

### Editing in the Attribute Table

Each map layer (shapefile) has an attribute table which displays the dataset for the selected map layer. From the <em>Map Layer 
Attributes</em> dialog box (<FigReference figKey="figure-295" />), there are editing tools that allow the user to edit the attributes.

From the **Map Layers** tab on the LifeSim main window (<FigReference figKey="figure-235" />), right-click on the map layer of interest. From the
shortcut menu, click **Open Attribute Table**. The *Map Layer Attributes* dialog 
box (<FigReference figKey="figure-295" />) will open for the selected map layer. For non-editing attribute table functionality, refer to [Map Layer
Attributes Dialog Box](./18-appendix-map-preferences.mdx#map-layer-attributes-dialog-box).

**NOTE**: When a single cell in the attribute table (<FigReference figKey="figure-295" />) is selected (e.g., row 1, column 1), the keyboard arrow keys
(and the tab and enter keys) can be utilized to step through the table one cell at a time.


#### Attribute Editing Tools

<TableVertical
  tableKey="attribute-tools-table"
  headers={[
    [
      { value: "Tool", rowSpan: 1, colSpan: 1 },
      { value: "Functionality", rowSpan: 1, colSpan: 1 }
    ]
  ]}
  columns={[
    [
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-51111cd9.png" />{" "}
            <strong>Open Field Calculator</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-start-edit.png" />{" "}
            <strong>Start Feature Edit Session</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-bd3c083e.png" />{" "}
            <strong>Stop Feature Edit Session</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      }
    ],
    [
      {
        value: (
          <>
            Allows the user to create a new field or update an existing field with a calculated result based on a specified expression.
            The <strong>Open Field Calculator</strong> tool is only enabled if feature editing has been enabled.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Start a feature editing session, which allows the user to edit cells and add/delete columns in the
            attribute table as well as edit the feature data. Once an editing session has been started, the <strong>Start Feature Edit 
            Session</strong> button will be replaced with <strong>Stop Feature Edit Session</strong>.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            End an active editing session of the attribute table. Once an editing session has been stopped,
            the <strong>Stop Feature Edit Session</strong> button will be replaced with <strong>Start Feature Edit Session</strong>.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      }
    ]
  ]}
  alt="Attribute Editing Tools"
  caption="Attribute Editing Tools"
  colWidths={["25%", "auto"]}
/>

#### Editing Attribute Table Shortcut Menu

While editing mode is active, selected column(s) in the attribute table have several editing options. Right-click on a column, and a shortcut menu will
appear (<FigReference figKey="figure-296" />). From the shortcut menu, the user can sort the column, calculate statistics, do a find for defined
items, calculate fields, and the selected column can be deleted.

<Figure
  figKey="figure-296"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure296.png"
  alt="Attributes Table – Column Shortcut Menu"
  caption="Attributes Table – Column Shortcut Menu"
/>

Other options, which become available in edit mode, are the standard cut/copy/paste functionality. Information in a selected cell can be edited or
replaced by typing directly into the selected cell. However, depending on the column type: <strong>String</strong> (e.g., “Masonry”),
<strong>Integer</strong> (e.g., 10), or <strong>Double</strong> (e.g., 0.5); only like types can be entered. In other words, only integers can be
entered into integer cell(s) or column(s), with the exception of string columns which can contain integers or doubles. To identify the type of data a
column can contain, hover over the column header for the tooltip that contains the type (<FigReference figKey="figure-297" />).

<Figure
  figKey="figure-297"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure297.png"
  alt="Attribute Table – Column Tooltips"
  caption="Attribute Table – Column Tooltips"
/>

Furthermore, depending on the character limit, an entered value could be truncated by limits. For example, if <em>ManufacturedLogCabin</em> is entered
into cell that has a character limit of 12 characters, then after <strong>Stop Feature Edit 
Session</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-bd3c083e.png" /> is clicked, then 
the extra characters
beyond the character limit will automatically be removed and only <em>Manufactured</em> will remain.

<TableVertical
  tableKey="column-tools"
  headers={[
    [
      { value: "Active Edit Session Column Tool", rowSpan: 1, colSpan: 1 },
      { value: "Functionality", rowSpan: 1, colSpan: 1 }
    ]
  ]}
  columns={[
    [
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-51111cd9.png" />{" "}
            <strong>Field Calculator</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-delete-col.png" />{" "}
            <strong>Delete Column</strong>
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
    ],
    [
      {
        value: (
          <>
            This menu command functions similarly to <strong>Open Field Calculator</strong>. The difference is any action
            will be done on the selected column, and new fields cannot be created.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
      {
        value: (
          <>
            Deletes the selected column.
          </>
        ),
        rowSpan: 1,
        colSpan: 1
      },
    ]
  ]}
  alt="Active Edit Session - Column Selection Tools"
  caption="Active Edit Session - Column Selection Tools"
  colWidths={["25%", "auto"]}
/>

#### Attribute Field Calculator

The <strong>Open Field Calculator</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-51111cd9.png" /> tool (<FigReference figKey="figure-296" />) can 
be utilized to add new calculated fields or update existing fields
in the selected column during an active editing session.

<Figure
  figKey="figure-298"
  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure298.png"
  alt="Field Calculator Dialog Box"
  caption="Field Calculator Dialog Box"
/>

To create a new field:

<ProcessList
items={[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab, right-click on the point map layer
        of interest. From the shortcut menu (<FigReference figKey="figure-235" />), click <strong>Open Attribute Table</strong>. The <em>Map Layer 
        Properties</em> dialog box for the selected map layer will open (<FigReference figKey="figure-256" />).
      </>
    ]
  },
  {
    title:[
      <>
      Start an editing session by clicking <strong>Start Feature Edit 
      Session</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-start-edit.png" />. Alternatively, 
      users can activate an editing session by right-clicking on the map layer of interest, and from the shortcut 
      menu (<FigReference figKey="figure-235" />), click <strong>Edit</strong>. The user
      can now edit the map layer of interest, and only one map layer/map attribute table can be edited at one time.
      </>
    ]
  },
  {
    title:[
      <>
      Click <strong>Open Field 
      Calculator</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-51111cd9.png" />, and 
      the <em>Field Calculator</em> dialog box (<FigReference figKey="figure-298" />) will open.
      </>
    ]
  },
  {
    title:[
      <>
        To add a new calculated field for the selected map layer, select <strong>Create New Column</strong>.
        Enter a name for the new column (has a limit of ten characters).
      </>
    ]
  },
  {
    title:[
      <>
      For the new field, create an expression in the <strong>Expression</strong> box. For example, in <FigReference figKey="figure-299" />, a new 
      field <em>FoundHtNew</em> has been created. To create the expression for the new field based on an
      existing field, double-click on the existing field of interest from the <strong>Data 
      Columns</strong> box (e.g., <strong>Height_Foundation</strong> in <FigReference figKey="figure-299" />) to add the field 
      name to the <strong>Expression</strong> box. 
      <br/><br/>
      The <strong>Functions</strong> button opens the <em>Available Functions</em> window (<FigReference figKey="figure-251" />), which provides
      commands and functions to use in creating the executable expression.
      <br/><br/>
      An example <strong>Expression</strong> for creating the new column, <em>FoundHtNew</em>, is displayed 
      in <FigReference figKey="figure-299" />: <strong>[Height_Foundation]+0.2</strong>. The result of the expression is 
      displayed at the bottom of the <em>Field Calculator</em> dialog box: <em>First record (row 0) = '0.7'</em>, meaning the value of Height_Foundation
      for row 0 is 0.5. 
      <br/><br/>
      <strong>NOTE</strong>: When creating a new attribute field that is a string field (e.g., text, symbols, numbers, or any sequence of characters),
      LifeSim requires that the text in the executable expression box be contained in double quotation marks.
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="figure-299"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure299.png"
        alt="Field Calculator Dialog Box – Create New Column"
        caption="Field Calculator Dialog Box – Create New Column"
      />
      </>
    ]
  },
  {
    title:[
      <>
      When <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-unchecked.png" /> <strong>Match 
      Case</strong> (<FigReference figKey="figure-299" />) is selected, the entered expression will be case-sensitive.
      </>
    ]
  },
  {
    title:[
      <>
      If the user selects <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-unchecked.png" /> <strong>Apply 
      to selected records only</strong> (<FigReference figKey="figure-299" />), then the expression will only be
      applied to features selected in the <em>Map Layer Attributes</em> dialog box (<FigReference figKey="figure-295" />) and will not
      alter features that were not selected. The <strong>Apply to selected records only</strong> option will only be available for existing columns and will
      not be enabled at the time a new field is created. Also, if rows are not selected in the attribute table, then the <strong>Apply to selected records
      only</strong> option will be disabled.
      </>
    ]
  },
  {
    title:[
      <>
      Click <strong>Execute</strong>, and the <em>Field Calculator</em> dialog box closes. The user will be returned
      to the <em>Map Layer Attributes</em> dialog box (<FigReference figKey="figure-295" />) where a newly calculated field will be
      present (e.g., <em>FoundHtNew</em>). New fields will be added to the end of the attribute table.
      </>
    ]
  },
  {
    title:[
      <>
      Once editing is complete, from the <em>Map Layer Attributes</em> dialog box (<FigReference figKey="figure-295" />),
      click <strong>Stop Feature Edit 
      Session</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-bd3c083e.png" />. Alternatively, 
      click <strong>Save</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" /> from 
      the <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-window-toolbars"><em>Editor
      Toolbar</em></a>.
      </>
    ]
  }
]}
/>

Updating an existing field will overwrite the original data in the field, so caution must be taken when using this option. However, changes made to
existing fields can be undone by clicking 
the **Undo** <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-f3fafd5e.png" /> button from 
the [<em>Editor Toolbar</em>](./18-appendix-map-preferences.mdx#map-window-toolbars). 

To edit an existing field:

<ProcessList
items={[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab, right-click on the point map layer
        of interest. From the shortcut menu (<FigReference figKey="figure-235" />), click <strong>Open Attribute Table</strong>. The <em>Map Layer 
        Properties</em> dialog box for the selected map layer will open (<FigReference figKey="figure-256" />).
      </>
    ]
  },
  {
    title:[
      <>
      Start an editing session by clicking <strong>Start Feature Edit 
      Session</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-start-edit.png" />. Alternatively, 
      users can activate an editing session by right-clicking on the map layer of interest, and from the shortcut 
      menu (<FigReference figKey="figure-235" />), click <strong>Edit</strong>. The user
      can now edit the map layer of interest, and only one map layer/map attribute table can be edited at one time.
      </>
    ]
  },
  {
    title:[
      <>
      Click <strong>Open Field 
      Calculator</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-51111cd9.png" />, and 
      the <em>Field Calculator</em> dialog box (<FigReference figKey="figure-298" />) will open.
      </>
    ]
  },
  {
    title:[
      <>
      Select <strong>Update Existing Column</strong>. From
      the <strong>Update Existing Column</strong> list, select the field that needs to be 
      updated (e.g., <strong>Height_Foundation</strong> in <FigReference figKey="figure-300" />).
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="figure-300"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure300.png"
        alt="Field Calculator Dialog Box – Update Existing Column"
        caption="Field Calculator Dialog Box – Update Existing Column"
      />
      </>
    ]
  },
  {
    title:[
      <>
      Create an expression in the <strong>Expression</strong> box. To update the
      existing field based on another existing field, double-click on the field of interest from the <strong>Data 
      Columns</strong> box (e.g., <strong>Height_Foundation</strong> in <FigReference figKey="figure-300" />) to add the field name.
      <br/><br/>
      The <strong>Functions</strong> button opens the <em>Available Functions</em> window (<FigReference figKey="figure-251" />), which 
      provides commands and functions to use in creating the executable expression.
      <br/><br/>
      An example expression updating the field <strong>Height_Foundation</strong> is displayed 
      in <FigReference figKey="figure-300" />: <strong>[Height_Foundation]+0.1</strong>. The
      result of the entered expression is shown at the bottom of the <em>Field Calculator</em> dialog box: <em>First record (row 0) = '0.6'</em>, meaning
      the value of Height_Foundation for row 0 is currently 0.5.
      <br/><br/>
      <strong>NOTE</strong>: When editing an existing column that is a string field (e.g., text, symbols, numbers, or any sequence of characters),
      LifeSim requires that the text in the executable expression box be contained in double quotation marks.
      </>
    ]
  },
  {
    title:[
      <>
        When <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-unchecked.png" /> <strong>Match 
      Case</strong> (<FigReference figKey="figure-300" />) is selected, the entered expression will be case-sensitive.
      </>
    ]
  },
  {
    title:[
      <>
        If the user selects <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-unchecked.png" /> <strong>Apply 
      to selected records only</strong> (<FigReference figKey="figure-300" />), then the expression will only be
      applied to features selected in the <em>Map Layer Attributes</em> dialog box (<FigReference figKey="figure-295" />) and will not
      alter features that were not selected. 
      </>
    ]
  },
  {
    title:[
      <>
      Click <strong>Execute</strong>, and the <em>Field Calculator</em> dialog box (<FigReference figKey="figure-300" />) closes. The user 
      will be returned
      to the <em>Map Layer Attributes</em> dialog box (<FigReference figKey="figure-295" />) where the updated
      field will be present (e.g., <strong>Height_Foundation</strong> values will now be +0.1 greater from executing 
      the expression in <FigReference figKey="figure-300" />).
      </>
    ]
  },
  {
    title:[
      <>
      For the updated field, the user can 
      click <strong>Undo</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-f3fafd5e.png" /> from 
      the <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-window-toolbars"><em>Editor
      Toolbar</em></a> to revert back to the original stated of the selected field. 
      The <strong>Redo</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-856a13ee.png" /> button will
      then become active, and the user can redo the change made to the field.
      </>
    ]
  },
  {
    title:[
      <>
        Once editing is complete, from the <em>Map Layer Attributes</em> dialog box (<FigReference figKey="figure-295" />),
      click <strong>Stop Feature Edit 
      Session</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-bd3c083e.png" />. Alternatively, 
      click <strong>Save</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" /> from 
      the <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#map-window-toolbars"><em>Editor
      Toolbar</em></a>.
      </>
    ]
  }
]}
/>

### Adding Features to a New Shapefile

After creating a new shapefile (described in Section [Create New Map Layers](./18-appendix-map-preferences.mdx#create-new-map-layers)), the user can add features by:

<ProcessList
items={[
  {
    title:[
      <>
      From the <strong>Map Layers</strong> tab on the LifeSim main window, right-click on the newly created map layer. From the
      shortcut menu (<FigReference figKey="figure-235" />), click <strong>Edit</strong>. The selected map layer is now in an active editing 
      session, and the <em>Editor Toolbar</em> (<FigReference figKey="newfigure-5-app" />) is active. 
      Review <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#editor-toolbar">Editor 
      Toolbar</a> for an overview of the toolbar and <TableReference tableKey="editor-toolbar-table" /> for the list of hot keys.
      </>
    ]
  },
  {
    title:[
      <>
        To add new features to the selected map layer, click the <strong>Add new 
        features (A)</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-15ecf4f5.png" /> tool. 
        The cursor will change
        to <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-f13ac387.png" /> (note the 
        dot in the center of the cursor). Click inside the map window and new points will be placed at the center of the cross-hairs (where the
        dot is located). 
        Review <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#editing-point-map-layers">Editing 
        Point Map Layers</a> for more information on added or editing
        point features. Click <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-25fae2d8.png" /> to undo newly added features.
        <br/><br/>
        Polylines and polygons start at the first click and each subsequent mouse click will create a new vertex point in the polyline or polygon. To end
        the polyline or polygon, double-click the mouse at the desired location. 
        Review <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#editing-polyline-or-polygon-map-layers">Editing Polyline or Polygon Map
        Layers</a> for more information on adding or editing polyline or polygon features.
        <br/><br/>
        <strong>NOTE</strong>: When a new feature is added, a new row is added to the bottom of the map layer's <em>Attributes</em> table with empty
        cells for text columns and zeros for numeric columns.
      </>
    ]
  },
  {
    title:[
      <>
      Once adding new features is complete, from the <em>Editor Toolbar</em>, click <strong>Save 
      Edits (Ctrl+S)</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" />. Then 
      click <strong>Stop Editing</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d8eb8f4c.png" />. If 
      the editing session is ended 
      before <strong>Save Edits</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" /> has been 
      clicked, a <em>Save Map Edits</em> message window (<FigReference figKey="figure-301" />) will open asking the user if the edits 
      need to be saved. Click <strong>Yes</strong> to save the
      edits made to the map layer, and the <em>Save Map Edits</em> message window will close. If the user
      did not intend to exit the editing session, click <strong>Cancel</strong> to return to the editing session.
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="figure-301"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure301.png"
        alt="Save Map Edits Message Window"
        caption="Save Map Edits Message Window"
      />
      </>
    ]
  }
]}
/>

## Editing Point Map Layers

The user can remove, move, or add points in newly created map layers (described in [Create New Map
Layers](./18-appendix-map-preferences.mdx#create-new-map-layers)) or existing point map layers.

### Add New Point Features

To add new point features:

<ProcessList
items={[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab on the LifeSim main window, right-click on the point map layer
        (newly created or existing). From the
        shortcut menu (<FigReference figKey="figure-235" />), click <strong>Edit</strong>. The selected map layer is now in an active editing 
        session, and the <em>Editor Toolbar</em> (<FigReference figKey="newfigure-5-app" />) is active. 
      </>
    ]
  },
  {
    title:[
      <>
        To add new features to the selected map layer, click the <strong>Add new 
        features (A)</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-15ecf4f5.png" /> tool. 
        The cursor will change
        to <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-f13ac387.png" /> (note the 
        dot in the center of the cursor). Click inside the map window and new points will be placed at the center of the cross-hairs (or wherever the dot is 
        located).
        <br/><br/>
        With snapping enabled (refer 
        to <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#add-features-to-the-destination-locations-map-layer">Add Features
        to the Destination Locations Map Layer</a> for more information), when the cursor gets close to the
        snap-to feature map layer, the dot in the middle of the cursor will "snap" to the snap-to feature, and new points will be placed at the snap point.
        <br/><br/>
        Click at the desired location, and a point will be added with default properties (<FigReference figKey="add-point" />).
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="add-point"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/add-point.gif"
        alt="Add New Point Feature(s)"
        caption="Add New Point Feature(s)"
      />
      </>
    ]
  },
  {
    title:[
      <>
      Continue clicking in the map window to add new points at desired locations.
      </>
    ],
  },
  {
    title:[
      <>
        Once the user is done adding new point features, from the <em>Editor Toolbar</em>, click <strong>Save 
      Edits (Ctrl+S)</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" />. Then 
      click <strong>Stop Editing</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d8eb8f4c.png" />. If 
      the editing session is ended 
      before <strong>Save Edits</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" /> has been 
      clicked, a <em>Save Map Edits</em> message window (<FigReference figKey="figure-301" />) will open asking the user if the edits 
      need to be saved. Click <strong>Yes</strong> to save the
      edits made to the map layer, and the <em>Save Map Edits</em> message window will close. If the user
      did not intend to exit the editing session, click <strong>Cancel</strong> to return to the editing session.
      </>
    ]
  }
]}
/>

### Move Point Features

To move an already existing point (or points) in the map window:

<ProcessList
items={[
  {
    title:[
      <>
      From the <strong>Map Layers</strong> tab on the LifeSim main window, right-click on the point map layer
        (newly created or existing) containing the feature(s) to be moved. From the
        shortcut menu (<FigReference figKey="figure-235" />), click <strong>Edit</strong>. The selected map layer is now in an active editing 
        session, and the <em>Editor Toolbar</em> (<FigReference figKey="newfigure-5-app" />) is active. 
      </>
    ]
  },
  {
    title:[
      <>
        From the <em>General Toolbar</em>, click the <strong>Select</strong> tool <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-select.png" />. Then, 
        click on the feature to be moved or click + hold + drag
        a rectangle around the feature(s). With the point selected, click and drag the point to the desired location (<FigReference figKey="move-structure" />). The user can move also
        multiple points at once.
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="move-structure"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/move-structure.gif"
        alt="Select and Move Points"
        caption="Select and Move Points"
      />
      </>
    ]
  },
  {
    title:[
      <>
      Once the user is done moving point features, from the <em>Editor Toolbar</em>, click <strong>Save 
      Edits (Ctrl+S)</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" />. Then 
      click <strong>Stop Editing</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d8eb8f4c.png" />. If 
      the editing session is ended 
      before <strong>Save Edits</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" /> has been 
      clicked, a <em>Save Map Edits</em> message window (<FigReference figKey="figure-301" />) will open asking the user if the edits 
      need to be saved. Click <strong>Yes</strong> to save the
      edits made to the map layer, and the <em>Save Map Edits</em> message window will close. If the user
      did not intend to exit the editing session, click <strong>Cancel</strong> to return to the editing session.
      </>
    ]
  }
]}
/>

### Delete Point Features

To remove an already existing point (or points) in the map window:

<ProcessList 
items={[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab on the LifeSim main window, right-click on the point map layer
        containing the feature(s) to be deleted. From the
        shortcut menu (<FigReference figKey="figure-235" />), click <strong>Edit</strong>. The selected map layer is now in an active editing 
        session, and the <em>Editor Toolbar</em> (<FigReference figKey="newfigure-5-app" />) is active. 
      </>
    ]
  },
  {
    title:[
      <>
        From the <em>General Toolbar</em>, click the <strong>Select</strong> tool <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-select.png" />. Then, 
        click on the feature to be deleted or click + hold + drag
        a rectangle around the feature(s). Use the <strong>Delete</strong> key to delete the selected point feature(s). Another option for deleting the selected points
        is right-click in the map
        window. Then from the shortcut menu, click <strong>Delete Selected Features</strong> (<FigReference figKey="delete-points" />). Either way, the selected
        features are deleted from the selected map layer.
        The user can also delete
        multiple points at once.
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="delete-points"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/delete-points.gif"
        alt="Delete Selected Point Feature(s) via Map Window Shortcut Menu"
        caption="Delete Selected Point Feature(s) via Map Window Shortcut Menu"
      />
      </>
    ]
  },
  {
    title:[
      <>
      Once the user is done deleting point features, from the <em>Editor Toolbar</em>, click <strong>Save 
      Edits (Ctrl+S)</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" />. Then 
      click <strong>Stop Editing</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d8eb8f4c.png" />. If 
      the editing session is ended 
      before <strong>Save Edits</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" /> has been 
      clicked, a <em>Save Map Edits</em> message window (<FigReference figKey="figure-301" />) will open asking the user if the edits 
      need to be saved. Click <strong>Yes</strong> to save the
      edits made to the map layer, and the <em>Save Map Edits</em> message window will close. If the user
      did not intend to exit the editing session, click <strong>Cancel</strong> to return to the editing session.
      </>
    ]
  }
]}
/>

## Editing Polyline or Polygon Map Layers

The user can remove, move, or add vertices in polyline or polygon map layers (shapefiles).

### Add Vertex Points

To add new vertex points:

<ProcessList
items={[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab on the LifeSim main window, right-click on the point map layer
        to be edited. From the
        shortcut menu (<FigReference figKey="figure-235" />), click <strong>Edit</strong>. The selected map layer is now in an active editing 
        session, and the <em>Editor Toolbar</em> (<FigReference figKey="newfigure-5-app" />) is active. 
      </>
    ]
  },
  {
    title:[
      <>
        From the <em>General Toolbar</em>, click 
        the <strong>Select</strong> tool <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-select.png" />.
        Double-click on a specific polyline or polygon feature in the selected map layer. 
      </>
    ]
  },
  {
    title:[
      <>
      When the user double-clicks on a polyline or polygon feature, the vertex points (black squares) will display, and the user will be in the vertex
      edit mode. At this point -- depending on the feature (polyline or polygon) -- several map window editing tools will become available in the <em>Editor 
      Toolbar</em>. See <FigReference figKey="polygon-edit" /> for a polygon edit, and <FigReference figKey="polyline-edit" /> for a polyline edit.
      </>
    ],
    freeform:[
      <>  
        <Figure
          figKey="polygon-edit"
          src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/polygon-edit.gif"
          alt="Vertex Editing Mode – Polygon Features"
          caption="Vertex Editing Mode – Polygon Features"
        />
        <Figure
          figKey="polyline-edit"
          src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/polyline-edit.gif"
          alt="Vertex Editing Mode – Polyline Features"
          caption="Vertex Editing Mode – Polyline Features"
        />
      </>
    ]
  },
  {
    title:[
      <>
      To add a new vertex point, from the <em>Editor Toolbar</em>, click <strong>Insert Vertex (I) </strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-7398525a.png" />. Locate on 
      the map window where
      the user wants to add a new vertex point, click the location, and a new vertex point will be added (<FigReference figKey="insert-vertex" />). Alternatively, 
      the user can add a new vertex in the
      vertex edit mode by right-clicking on the location where a vertex point is to be added, and from the shortcut 
      menu (<FigReference figKey="insert-vertex-shortcut" />), click <strong>Insert Vertex</strong>. A new vertex point will be added.
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="insert-vertex"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/insert-vertex.gif"
        alt="Vertex Edit Mode - Insert Vertex"
        caption="Vertex Edit Mode - Insert Vertex"
      />
      <Figure
        figKey="insert-vertex-shortcut"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/insert-vertex-shortcut.gif"
        alt="Vertex Edit Mode - Shortcut Menu - Insert Vertex"
        caption="Vertex Edit Mode - Shortcut Menu - Insert Vertex"
      />
      </>
    ]
  },
  {
    title:[
      <>
        Once the user is done adding vertex points, from the <em>Editor Toolbar</em>, click <strong>Save 
        Edits (Ctrl+S)</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" />. Then 
        click <strong>Stop Editing</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d8eb8f4c.png" />. If 
        the editing session is ended 
        before <strong>Save Edits</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" /> has been 
        clicked, a <em>Save Map Edits</em> message window (<FigReference figKey="figure-301" />) will open asking the user if the edits 
        need to be saved. Click <strong>Yes</strong> to save the
        edits made to the map layer, and the <em>Save Map Edits</em> message window will close. If the user
        did not intend to exit the editing session, click <strong>Cancel</strong> to return to the editing session.
      </>
    ]
  }
]}
/>

### Delete Vertex Points

To delete vertex points:

<ProcessList
items={[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab on the LifeSim main window, right-click on the point map layer
        containing the vertex point(s) to be deleted. From the
        shortcut menu (<FigReference figKey="figure-235" />), click <strong>Edit</strong>. The selected map layer is now in an active editing 
        session, and the <em>Editor Toolbar</em> (<FigReference figKey="newfigure-5-app" />) is active. 
      </>
    ]
  },
  {
    title:[
      <>    
        From the <em>General Toolbar</em>, click 
        the <strong>Select</strong> tool <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-select.png" />.
        Double-click on a specific polyline or polygon feature in the selected map layer. 
      </>
    ]
  },
  {
    title:[
      <>
      To remove a vertex or vertices points, select the vertex or vertices points to be deleted, and click the <strong>Delete</strong> key on your keyboard. Another way is
      right-click on the vertex point that is to be deleted, and from the shortcut menu (<FigReference figKey="delete-vertex" />), click <strong>Delete 
      Vertices</strong> or <strong>Delete Selected
      Features</strong>. Either way, the vertex point is deleted.
      </>
    ],
    freeform:[
      <>
        <Figure
          figKey="delete-vertex"
          src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/delete-vertex.gif"
          alt="Vertex Edit Mode - Delete Vertex"
          caption="Vertex Edit Mode - Delete Vertex"
        />
      </>
    ]
  },
  {
    title:[
      <>
        Once the user is done deleting vertex points, from the <em>Editor Toolbar</em>, click <strong>Save 
        Edits (Ctrl+S)</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" />. Then 
        click <strong>Stop Editing</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d8eb8f4c.png" />. If 
        the editing session is ended 
        before <strong>Save Edits</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" /> has been 
        clicked, a <em>Save Map Edits</em> message window (<FigReference figKey="figure-301" />) will open asking the user if the edits 
        need to be saved. Click <strong>Yes</strong> to save the
        edits made to the map layer, and the <em>Save Map Edits</em> message window will close. If the user
        did not intend to exit the editing session, click <strong>Cancel</strong> to return to the editing session.
      </>
    ]
  }
]}
/>

### Edit a Line Feature – Shorten

The user can shorten the length of line features in newly created (described in [Create New Map
Layers](./18-appendix-map-preferences.mdx#create-new-map-layers)) or existing polyline map layer by:

<ProcessList
items={[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab on the LifeSim main window, right-click on the point map layer
        containing the line feature to be edited. From the
        shortcut menu (<FigReference figKey="figure-235" />), click <strong>Edit</strong>. The selected map layer is now in an active editing 
        session, and the <em>Editor Toolbar</em> (<FigReference figKey="newfigure-5-app" />) is active. 
      </>
    ]
  },
  {
    title:[
      <>
        From the <em>General Toolbar</em>, click 
        the <strong>Select</strong> tool <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-select.png" />.
        Click on a specific line feature in the selected map layer. 
      </>
    ]
  },
  {
    title:[
      <>  
      From the <em>Editor Toolbar</em>, select <strong>Break 
      Line Feature</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-break.png" />. The cursor will 
      change to <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-f13ac387.png" /> (note the dot
      which follows the cursor along the selected line feature). 
      </>
    ]
  },
  {
    title:[
      <>
        Hover the cursor over the exact location to break the selected line feature, and a dot will appear on the line feature. Move the dot to the
        location where the line will be broken.
        Wherever the dot is located on the line feature, when the mouse is clicked, that is where the break in the line will occur. As an example, the line section 
        outside of a Emergency Planning Zone (EPZ) can be deleted if the user
        breaks the line at the edge of the EPZ, and then selects the line outside of the EPZ. Click the <strong>Delete</strong> key or right-click, and from the
        shortcut menu (<FigReference figKey="break-line" />), click <strong>Delete Selected Features</strong>.
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="break-line"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/break-line.gif"
        alt="Break Line Feature Tool"
        caption="Break Line Feature Tool"
      />
      </>
    ]
  },
  {
    title:[
      <>
         Once the user is done modifying map layer features, from the <em>Editor Toolbar</em>, click <strong>Save 
        Edits (Ctrl+S)</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" />. Then 
        click <strong>Stop Editing</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d8eb8f4c.png" />. If 
        the editing session is ended 
        before <strong>Save Edits</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" /> has been 
        clicked, a <em>Save Map Edits</em> message window (<FigReference figKey="figure-301" />) will open asking the user if the edits 
        need to be saved. Click <strong>Yes</strong> to save the
        edits made to the map layer, and the <em>Save Map Edits</em> message window will close. If the user
        did not intend to exit the editing session, click <strong>Cancel</strong> to return to the editing session.
      </>
    ]
  }
]}
/>

### Edit a Line Feature – Lengthen

The user can lengthen a line feature in newly created (described in [Create New Map Layers](./18-appendix-map-preferences.mdx#create-new-map-layers))
or existing polyline map layer by:

<ProcessList
items={[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab on the LifeSim main window, right-click on the point map layer
        containing the line feature to be edited. From the
        shortcut menu (<FigReference figKey="figure-235" />), click <strong>Edit</strong>. The selected map layer is now in an active editing 
        session, and the <em>Editor Toolbar</em> (<FigReference figKey="newfigure-5-app" />) is active. 
      </>
    ]
  },
  {
    title:[
      <>
        From the <em>General Toolbar</em>, click 
        the <strong>Select</strong> tool <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-select.png" />.
        Click on a specific line feature in the selected map layer. 
      </>
    ]
  },
  {
    title:[
    <>
      From the <em>Editor Toolbar</em>, click <strong>Continue from 
      end (lines only)</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-cf9d34fc.png" />. The curser will change 
      to <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-f13ac387.png" />, with a line connected to the end
      of the selected polyline feature. Click the cursor at the desired location to continue the line and
      double-click to end the lengthened line. Note, the user can reverse the line end. From the shortcut menu, click <strong>Reverse Selected 
      Features</strong> (<FigReference figKey="lengthen-reverse" />).
    </>
    ],
    freeform:[
      <>
      <Figure
        figKey="lengthen-reverse"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/lengthen-reverse.gif"
        alt="Lengthen a Line Feature + Reverse Selected Features"
        caption="Lengthen a Line Feature + Reverse Selected Features"
      />
      </>
    ]
  },
  {
    title:[
      <>
        Once the user is done modifying map layer features, from the <em>Editor Toolbar</em>, click <strong>Save 
        Edits (Ctrl+S)</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" />. Then 
        click <strong>Stop Editing</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d8eb8f4c.png" />. If 
        the editing session is ended 
        before <strong>Save Edits</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" /> has been 
        clicked, a <em>Save Map Edits</em> message window (<FigReference figKey="figure-301" />) will open asking the user if the edits 
        need to be saved. Click <strong>Yes</strong> to save the
        edits made to the map layer, and the <em>Save Map Edits</em> message window will close. If the user
        did not intend to exit the editing session, click <strong>Cancel</strong> to return to the editing session.
      </>
    ]
  }
]}
/>
 

### Adding New Features

To add new features to a polyline or polygon:

<ProcessList
items={[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab on the LifeSim main window, right-click on the point map layer
        containing the line feature to be edited. From the
        shortcut menu (<FigReference figKey="figure-235" />), click <strong>Edit</strong>. The selected map layer is now in an active editing 
        session, and the <em>Editor Toolbar</em> (<FigReference figKey="newfigure-5-app" />) is active. 
      </>
    ]
  },
  {
    title:[
      <>
        From the <em>General Toolbar</em>, click 
        the <strong>Select</strong> tool <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-select.png" />.
        Click on a specific line feature in the selected map layer. 
      </>
    ]
  },
  {
    title:[
      <>
      From the <em>Editor Toolbar</em>, click <strong>Add new line or polygon to existing 
      feature </strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-3da2817d.png" />. The curser will change to
      {"\n"}<FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-f13ac387.png" /> (note the dot in the center of
      the cursor). Click the desired location where the user will begin drawing the new line or polygon to the existing selected feature. Each subsequent
      click of the cursor will create a new vertex point to the polyline or polygon (e.g., polygon in <FigReference figKey="add-feature" />). Double-click to complete the
      new addition to the existing feature.
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="add-feature"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/add-feature.gif"
        alt="Add New Line or Polygon to Existing Feature Tool"
        caption="Add New Line or Polygon to Existing Feature Tool"
      />
      </>
    ]
  },
  {
    title:[
      <>
        Once the user is done modifying map layer features, from the <em>Editor Toolbar</em>, click <strong>Save 
        Edits (Ctrl+S)</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" />. Then 
        click <strong>Stop Editing</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d8eb8f4c.png" />. If 
        the editing session is ended 
        before <strong>Save Edits</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" /> has been 
        clicked, a <em>Save Map Edits</em> message window (<FigReference figKey="figure-301" />) will open asking the user if the edits 
        need to be saved. Click <strong>Yes</strong> to save the
        edits made to the map layer, and the <em>Save Map Edits</em> message window will close. If the user
        did not intend to exit the editing session, click <strong>Cancel</strong> to return to the editing session.
      </>
    ]
  }
]}
/>

## Create Destination Locations Example

The following sections gives an example of creating a destination location shapefile for use in LifeSim. Destinations data is required in a LifeSim
study to represent possible evacuation locations during flooding events. The destination data is represented as points in space. LifeSim will find the
nearest road from a destination point and consider that the evacuation location. In other words, destination points do not need to connect with a
road for LifeSim simulations to be successful.

### Add Features to the Destination Locations Map Layer

After creating the destination shapefile (described in [Create New Map Layers](./18-appendix-map-preferences.mdx#create-new-map-layers)), the user can
add features.

<ProcessList
items={[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab on the LifeSim main window, right-click on the Destinations data map layer. From the
        shortcut menu (<FigReference figKey="figure-235" />), click <strong>Edit</strong>. The selected map layer is now in an active editing 
        session, and the <em>Editor Toolbar</em> (<FigReference figKey="newfigure-5-app" />) is active. 
      </>
    ]
  },
  {
    title:[
      <>
      This step is optional as LifeSim will find the nearest road from a destination point and consider that the evacuation route for the destination
      point. 
      </>
    ],
    child:[
      {
        title:
          [
            <>
             From the <em>Editor Toolbar</em>, click the <strong>Feature Snapping 
             Settings</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d15e7265.png" /> tool. A <em>Snapping
             Settings</em> window will display (<FigReference figKey="figure-310" />). From the <strong>Snap To Layer</strong> list, the user will select a
             map layer to base the newly added feature points to snap to (e.g., <strong>Ex_RoadNetwork</strong>).
             If the <strong>Snap To Feature</strong> is a polyline map layer, then all snapping options will be available. Also, the map layer 
             selected (e.g., <strong>Ex_RoadNetwork</strong>) needs to be active in the LifeSim study.
            </>
          ],
          freeform:[
            <>
              <Figure
                  figKey="figure-310"
                  src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure310.png"
                  alt="Snapping Settings Window"
                  caption="Snapping Settings Window"
              />
            </>
          ],
      },
    ]
  },
  {
    title:[
      <>
      To add new features, from the <em>Editor Toolbar</em>, click <strong>Add new 
      features (A)</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-15ecf4f5.png" />. Notice in the map window, the cursor 
      will change to <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-f13ac387.png" /> (there will be a dot in the
      center of the cursor). Click inside the map window, and new points will be placed at the center of the cross-hairs (<FigReference figKey="add-dest" />).
      </>
    ],
    freeform:[
      <>
        <Figure
          figKey="add-dest"
          src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/add-dest.gif"
          alt="Add New Features Tool - Add Destination Point"
          caption="Add New Features Tool - Add Destination Point"
        />
        If snapping is enabled in Step 2 (if desired), then when the cursor gets close to the snap-to-feature map layer, the dot in the middle of the
        cursor will "snap" to the snap to feature and new points will be placed at the snap point. Click at the desired location and a point will be added
        with default properties.
      </>
    ]
  },
  {
    title:[
      <>
      Destinations typically are viewed in the map window with 
      a <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-01aa817a.png" /> star symbol. The properties
      for the newly created destinations map layer (e.g., <strong>Example_Destinations</strong>) can be modified from the default general point layer symbol.
      Alternatively, users can modify the map layer properties before adding any new points to the newly created map layer.
      </>
    ],
    child: [
      {
        title: (
          <>
            From the <strong>Map Layers</strong> tab on the LifeSim main window, right-click on the point map layer of
            interest (e.g., <strong>Example_Destinations</strong>). From the shortcut 
            menu (<FigReference figKey="figure-235" />), click <strong>Properties</strong>. The <em>Map Layer Properties</em> dialog box 
            will open (<FigReference figKey="figure-276" />).
          </>
        ),
      },
      {
        title: (
          <>
            To change the symbol type, from the <em>Map Layer Properties</em> dialog box (<FigReference figKey="figure-276" />), from 
            the <strong>Symbols</strong> box (<FigReference figKey="figure-278" />), select a new symbol.
          </>
        ),
      },
      {
        title: (
          <>
            Change the <strong>Size</strong> of the symbol (e.g., size <strong>11</strong>) by entering the number in 
            the <strong>Size</strong> box (<FigReference figKey="figure-276" />).
          </>
        ),
      },
      {
        title: (
          <>
            Change the fill color by clicking the color preview window next to <strong>Fill Color</strong> (<FigReference figKey="figure-276" />). The <em>Select a
            Color</em> dialog box will open (<FigReference figKey="figure-277" />). Select a color using the options described 
            in <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/appendix-map-preferences#line-and-fill-color">Line
            and Fill Color</a>. Click <strong>OK</strong>. The <em>Select a Color</em> dialog
            box will close, and the user will be returned to the <em>Map Layer Properties</em> dialog box (<FigReference figKey="figure-276" />).
          </>
        ),
      },
      {
        title: (
          <>
             From the <em>Map Layer Properties</em> dialog box (<FigReference figKey="figure-276" />), click <strong>Apply</strong>. Check to see if the 
             desired changes were made to the map layer in the map window. Once the user has entered all of the desired features, click <strong>Close</strong>. Changes will be
            saved to the map layer (e.g., <strong>Example_Destinations</strong>) and the <em>Map Layer Properties</em> dialog box will close.
          </>
        ),
      },
    ],
  }
]}
/>

### Add Attributes to the Destinations Locations Map Layer

After all of the necessary destination points have been added to the map layer (e.g., <strong>Example_Destinations</strong>) -- as described in [Add Features to
the Destination Locations Map Layer](#add-features-to-the-destination-locations-map-layer) -- the attributes for the new map layer must be entered by:

<ProcessList
items={[
  {
    title:[
      <>
        From the <strong>Map Layers</strong> tab on the LifeSim main window, right-click on the Destinations data map layer. From the
        shortcut menu (<FigReference figKey="figure-235" />), click <strong>Open Attribute Table</strong>. The <em>Map
        Layer Attribute Table</em> dialog box (<FigReference figKey="figure-295" />) will open in an active editing session.
      </>
    ]
  },
  {
    title:[
      <>
      Click the <strong>Open Field Calculator</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-51111cd9.png" /> tool. 
      The <em>Field Calculator</em> dialog box (<FigReference figKey="figure-298" />) will open, which has two options: <strong>Create New 
      Column</strong> or <strong>Update Existing Column</strong>. The <strong>Create New Column</strong> is selected by default.
      </>
    ]
  },
  {
    title:[
      <>
      Enter a name in the <strong>Create New Column</strong> box. The attribute name is
      limited to ten characters.
      </>
    ]
  },
  {
    title:[
      <>
      When the expression for the new field is entered in the <strong>Expression</strong> box (<FigReference figKey="figure-299" />) and that entered
      expression is executed (<strong>Execute</strong> button is clicked), the new column is created, and the <em>Field Type</em> for the column is
      set. Depending on the expression, the <em>Field Type</em> will be either: <strong>String</strong> (e.g., “Masonry”), <strong>Integer</strong> (e.g., 
      10), or <strong>Double</strong> (e.g., 0.5). Furthermore, the total number of characters entered in
      the <strong>Expression</strong> box will set the character limit for the new column (e.g., “Masonry” will set
      the character limit for the new column to 7 characters).
      <br/><br/>
      <strong>NOTE</strong>: When creating a new attribute field that is a string field (e.g., text, symbols, numbers, or any sequence of characters),
      then LifeSim requires that the text in the executable expression box be contained in double quotation marks.
      </>
    ]
  },
  {
    title:[
      <>
      Click <strong>Execute</strong> (<FigReference figKey="figure-298" />) to create the new field (e.g., <strong>Name</strong>). The <em>Field
      Calculator</em> dialog box (<FigReference figKey="figure-298" />) will close, and the new field will be added to 
      the <em>Attributes</em> table (<FigReference figKey="figure-312" />).
      </>
    ]
  },
  {
    title:[
      <>
       Now the names of the destinations need to be entered. One way to add names to destinations is by using the cardinal 
       directions (<FigReference figKey="figure-312" />). Utilizing the cardinal directions for the study area to name destinations can be very useful for projects
        that contain multiple emergency planning zones. Also, using cardinal directions can be useful when completing 
        the <a href="/RMC-Software-Documentation/docs/desktop-applications/lifesim/users-guide/v1.0/alternatives#destination-assignments">Destination Assignments</a> tab of
        the <em>Alternative 
        Editor</em>.
      </>
    ],
    freeform:[
      <>
      <Figure
        figKey="figure-312"
        src="figures/desktop-applications/lifesim/users-guide/v1.0/figures/figure312.png"
        alt="Example Attributes Table – Destination Locations"
        caption="Example Attributes Table – Destination Locations"
      />
      </>
    ]
  },
  {
    title:[
      <>
      The user can manually type (or copy and paste) an appropriate name (e.g., <strong>I-515 N</strong>; <strong>North Windsor 6</strong>) in the newly created column for each
      destination that has been added.
      </>
    ]
  },
  {
    title:[
      <>
        Once the user is done adding new features, from the <em>Editor Toolbar</em>, click <strong>Save 
        Edits (Ctrl+S)</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" />. Then 
        click <strong>Stop Editing</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-d8eb8f4c.png" />. If 
        the editing session is ended 
        before <strong>Save Edits</strong> <FigureInline src="figures/desktop-applications/lifesim/users-guide/v1.0/inline-images/inline-save-edit.png" /> has been 
        clicked, a <em>Save Map Edits</em> message window (<FigReference figKey="figure-301" />) will open asking the user if the edits 
        need to be saved. Click <strong>Yes</strong> to save the
        edits made to the map layer, and the <em>Save Map Edits</em> message window will close. If the user
        did not intend to exit the editing session, click <strong>Cancel</strong> to return to the editing session.
      </>
    ]
  }
]}
/>

<CitationFootnote />
