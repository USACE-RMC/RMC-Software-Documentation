import React, { useEffect, useState } from "react";

const ReportDocumentation({ reportDate, reportType, reportTitle, reportSubTitle, reportAuthors, reportAbstract, reportSubjectTerms, responsiblePersonName, responsiblePersonNumber }) => {
    return (
        <div className="report-documentation-table">

        </div>
    )
};

export default ReportDocumentation;