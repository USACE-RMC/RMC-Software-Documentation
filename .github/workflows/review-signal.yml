name: Review Signal
on:
  pull_request_review:
    types: [submitted, dismissed]
permissions: {}
jobs:
  signal:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Review activity will be reconciled by the trusted default-branch controller.'
